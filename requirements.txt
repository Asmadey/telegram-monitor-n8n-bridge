telethon>=1.36.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0
rich>=13.7.0
tabulate>=0.9.0

# Фаза 1 (PLAN.md, задача 1.1): Postgres, миграции, конфиг, безопасность
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
aiosqlite>=0.20.0
alembic>=1.13.0
pydantic-settings>=2.2.0
# валидация email в pydantic EmailStr (задача 2.4: signup/login)
email-validator>=2.1.0
# bcrypt напрямую, без passlib: passlib не поддерживается с 2020 и несовместим
# с bcrypt>=4.1 (падает в detect_wrap_bug). Формат хеша тот же $2b$ (задача 2.1)
bcrypt>=4.1.0
itsdangerous>=2.2.0
cryptography>=42.0.0
slowapi>=0.1.9
# Фаза 6 (PLAN.md): верификация Google ID-токенов через firebase_admin.
# Импорт ленивый (app/services/google_auth.py) — без конфигурации Firebase
# web-процесс поднимается, но POST /auth/google падает громко, не 401.
firebase-admin>=6.5.0
