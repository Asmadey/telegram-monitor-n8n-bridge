-r requirements.txt
# Инструменты разработчика. rich нужен CLI-скриптам в корне (auth.py,
# reader.py, fetch_sample.py): боевые процессы их не запускают, а в образе
# пакет стоил 2.9 МБ на каждый деплой и лишнюю строку в pip-audit.
rich>=13.7.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.4.0
mypy>=1.10.0
bandit>=1.7.9
pip-audit>=2.7.0