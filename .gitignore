# Virtual Environment
.venv/
venv/
ENV/

# Secrets & Configuration
.env
.env.local
.env.*.local
key.md

# Telegram Telethon Sessions
*.session
*.session-journal

# Database & Backups
storage.db
storage.db-journal
monitors.json
monitors.json.bak
exports/

# Dev-аутбокс писем (задача 2.9): внутри токены сброса
tmp/

# Python Cache
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# OS / IDE
.DS_Store
.vscode/
.idea/

# Справочный Rails-шаблон, не часть проекта
Ruby/

# Тестовый кэш
.pytest_cache/
tests/__pycache__/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Слито из корневого .gitignore при разворачивании дерева 2026-09-10.
# `.env.*` шире, чем `.env.local`, поэтому нужна и отмена для примера.
.env.*
!.env.example

# Личные резервные копии: внутри боевые storage.db и personal_account.session.
# Каталог переехал в корень репозитория — без этой строки один `git add .`
# закоммитил бы MTProto-сессию.
private-backups/

# Персональные настройки Claude Code этой машины (имя файла говорит: local).
.claude/settings.local.json
