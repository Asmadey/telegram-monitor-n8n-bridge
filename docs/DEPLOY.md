# Развёртывание: фронтенд на Vercel, бэкенд на Railway

## Схема

```
браузер ──► Vercel (статика + переписывание /api, /auth) ──► Railway: web
                                                              Railway: worker
                                                              Railway: Postgres
```

Два процесса Railway из одного образа - `web` (`uvicorn app.main:app`) и
`worker` (`python -m app.worker`), общий Postgres. Фронтенд — статические файлы
из `static/`, без сборки.

## Почему запросы идут через переписывание Vercel, а не напрямую

`*.vercel.app` и `*.up.railway.app` — разные регистрируемые домены, оба в
Public Suffix List. Общей cookie между ними не бывает даже в принципе, поэтому
прямой вызов фронтенда к API — межсайтовый запрос:

- cookie с `SameSite=Lax` браузер в таком запросе **не отправляет вовсе** —
  пользователь просто разлогинен;
- `SameSite=None; Secure` отправляется, но это сторонняя cookie: **Safari
  блокирует такие по умолчанию**, Chrome сворачивает их поддержку.

Переписывание на стороне Vercel убирает проблему целиком: браузер видит один
origin, cookie остаются первой стороной, CORS не нужен. Цена — один сетевой
переход и расход функций Vercel.

**Прямой режим поддержан** (CORS + `SameSite=None`), но включается явно и не
работает в Safari. Годится, только если фронтенд и API стоят на поддоменах
одного домена — `app.example.com` и `api.example.com`. Тогда это уже не
межсайтовый запрос, и режим равноценен переписыванию.

## Настройка

### Railway - сервис `web`

| Переменная | Значение |
|---|---|
| `DATABASE_URL` | `${{Postgres.DATABASE_URL}}` |
| `APP_ENCRYPTION_KEY` | `openssl rand -base64 32` — **менять нельзя**, см. ниже |
| `SECRET_KEY` | `openssl rand -base64 32` |
| `ENVIRONMENT` | `production` |
| `APP_BASE_URL` | адрес **фронтенда** — по нему строится ссылка в письме сброса |
| ~~`TELEGRAM_API_ID`, `TELEGRAM_API_HASH`~~ | приложению не нужны: ключи вносит пользователь в кабинете (см. ниже). Задавайте только для переноса старой базы |
| `RESEND_API_KEY` | почта для сброса пароля |
| `FRONTEND_ORIGINS` | **пусто** в режиме переписывания; список origin через запятую в прямом режиме |
| `API_ORIGIN` | публичный адрес API — по нему определяется, межсайтовый ли запрос |

Для существующего сервиса `web` задайте в Railway:

- Build: Dockerfile из корня репозитория;
- Pre-deploy command: `alembic upgrade head`;
- Start command: `sh -c "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"`;
- Healthcheck path: `/health`.

**Оболочка в команде запуска обязательна.** Railway исполняет Start command
не через shell: без `sh -c` строка `$PORT` уходит в uvicorn литералом, и
контейнер падает циклом `Invalid value for '--port': '$PORT' is not a valid
integer`. Снаружи это выглядит как «сервис не поднялся» — healthcheck просто
не отвечает, причина видна только в логах деплоя. Проверено первым живым
деплоем 2026-09-07; правило держит `test_70_deploy.py`.

`/railway.json` сохраняет те же настройки как конфигурацию по умолчанию, но у
фактически созданного сервиса Config File Path не задан. После деплоя сверяйте
применённые команды в Deployment Details, а не считайте наличие файла
подтверждением настройки облачного сервиса. Миграции должен применять только
`web`: если их запустят оба сервиса, они пойдут одновременно.

### Railway - сервис `worker`

Те же переменные, команда `python -m app.worker`, **без** `preDeployCommand`
и HTTP healthcheck. Команда задаётся прямо в Settings сервиса `worker`.
После деплоя проверьте в Deployment Details, что Railway применил именно её.

### Ключи MTProto задаёт пользователь, а не оператор

С 2026-09-07 `api_id`/`api_hash` принадлежат пользователю: он получает свои на
my.telegram.org и сохраняет в кабинете (⚙️ MTProto → «Ключи вашего
приложения»). `api_hash` шифруется в базе и обратно в браузер не возвращается.

Общего ключа сервиса нет намеренно. Один `api_id`, через который логинятся
сотни чужих аккаунтов, Telegram ограничивает целиком — и вход перестаёт
работать у всех сразу. Отката на ключ из ENV в приложении тоже нет: он
выглядел бы как «работает» ровно до этого дня.

### Vercel

1. Корень проекта — каталог `FastAPI` (или переместите `static/` в отдельный
   репозиторий фронтенда).
2. `vercel.json` уже направляет `/api`, `/auth` и `/health` на сервис `web`:
   `https://web-production-d2e997.up.railway.app`.
3. Build Command пустой, Output Directory — `static`.

### Вход через Google (необязательно)

Функция выключена, пока не заданы **все три** переменные — кнопка на страницах
входа скрыта, а CSP не пускает сторонние origin. Включение — три шага, и
пропуск третьего даёт молчаливую поломку: кнопка появится, а браузер откажется
грузить SDK.

1. **Railway (`web` и `worker`)** — публичные значения из настроек
   веб-приложения в консоли Firebase:

   | Переменная | Пример |
   |---|---|
   | `FIREBASE_API_KEY` | `AIzaSy...` |
   | `FIREBASE_AUTH_DOMAIN` | `<project>.firebaseapp.com` |
   | `FIREBASE_PROJECT_ID` | `<project>` |

   Это публичные значения: `apiKey` идентифицирует проект, а не даёт доступ —
   защищают правила и список разрешённых доменов. Приватный ключ **сервисного
   аккаунта** для проверки входа не нужен: используются публичные сертификаты Google.

2. **Firebase console → Authentication → Settings → Authorized domains** —
   добавить домен Vercel. Без этого popup входа закроется с ошибкой.

3. **`vercel.json`** — расширить CSP. Фронтенд на Vercel отдаётся статикой со
   своим заголовком, и он не знает о настройках Railway: политика остаётся
   узкой по умолчанию и расширяется руками ровно тогда, когда функция нужна.
   В `Content-Security-Policy` дописать:

   ```
   script-src 'self' https://www.gstatic.com https://apis.google.com;
   connect-src 'self' https://identitytoolkit.googleapis.com https://securetoken.googleapis.com;
   frame-src https://accounts.google.com https://<project>.firebaseapp.com
   ```

   `'unsafe-inline'` не добавлять ни при каких обстоятельствах — Firebase его
   не требует, а он вернул бы исполнение инлайн-скриптов.

## Проверка после первого деплоя

1. `GET /health` через адрес Vercel → `{"status":"ok"}` (переписывание работает).
2. Регистрация → вход → перезагрузка страницы: сессия жива (cookie первой стороны).
3. Любое изменение (добавить канал) → проходит (CSRF-заголовок доезжает).
4. Сброс пароля → ссылка в письме ведёт на домен Vercel, а не на Railway.
5. Редеплой `web` → повторная авторизация в Telegram **не** требуется:
   сессия лежит в базе зашифрованной, а не в файле.
6. Воркер: добавить канал → «Запустить» → в течение полуминуты появляется
   запись в ленте и в журнале. Пустая лента при статусе задачи `pending`
   означает, что сервис `worker` не поднят.
7. Если включён вход через Google — кнопка видна на `/login`, и в консоли
   браузера нет ошибок CSP (иначе не выполнен шаг 3 выше).

## Чего нельзя делать

**Менять `APP_ENCRYPTION_KEY` после первого запуска.** Им зашифрованы
MTProto-сессии пользователей: новая пара ключей делает их нечитаемыми, то есть
выбрасывает всех из подключённых Telegram-аккаунтов. Ротация — отдельная
процедура с перешифровкой, а не смена переменной.

**Выдавать `Access-Control-Allow-Origin: *`.** Вместе с учётными данными
браузер такое сочетание отвергает, а если бы принимал — API с сессионными
cookie был бы открыт любому сайту. Список origin задаётся поимённо.

## Конфигурации первого релиза

- Для GitHub-репозитория, в корне которого уже находятся `vercel.json` и
  `static/`, Root Directory оставляется пустым. Framework Preset: Other;
  `framework: null` в конфигурации исключает автоматическое определение FastAPI.
- В текущих сервисах Railway команды заданы прямо в Settings: `web` запускает
  Uvicorn и применяет миграции перед деплоем, `worker` запускает
  `python -m app.worker` без HTTP healthcheck и команды миграций. Запускайте
  worker после успешного применения миграций сервисом `web`.
- Обычный `${{Postgres.DATABASE_URL}}` преобразуется в URL драйвера asyncpg
  одинаково в приложении и Alembic. Пароли с URL-кодированием сохраняются.
- CSP уже разрешает Firebase-проект `mtproto-ai`. При смене проекта обновите
  `frame-src` вместе с `FIREBASE_AUTH_DOMAIN`.
- Google-вход проверяет Firebase ID-токены официальным `google-auth` по
  публичным сертификатам Google. `FIREBASE_PROJECT_ID=mtproto-ai` обязателен;
  сервисный аккаунт и `GOOGLE_APPLICATION_CREDENTIALS` для этого входа не нужны.
  Проверяются подпись, срок, audience, issuer, subject и auth_time.

## Наблюдение за состоянием

`GET /health` — публичный и намеренно голый: `{"status": "ok"}`. Он говорит
только «web-процесс отвечает» и ничего не знает ни о базе, ни о воркере.

`GET /api/ops/health` — под входом, три числа, которых не хватало:

| Поле | Что значит |
|---|---|
| `database.ok` | база отвечает на `SELECT 1` |
| `worker.alive` | воркер отметился не позже 180 с назад (тик — 30 с) |
| `worker.seen` | воркер отмечался хоть раз: `false` = сервис не поднимался |
| `jobs.pending` / `jobs.oldest_pending_seconds` | размер и возраст очереди |

То же самое видно в интерфейсе на вкладке «Журнал», строкой сверху.

Молчащий воркер — самый вероятный отказ этой архитектуры, и снаружи он
выглядит как «ничего не происходит»: лента пуста, задача висит в
`pending`, ошибок нет нигде. Растущий `oldest_pending_seconds` при
`worker.alive = false` — это он.

## Зелёный CI не значит «выложено»

Найдено 9 сентября. Слияние PR #25 прошло, все пять джобов зелёные, Vercel
выложил фронтенд за секунды — а бэкенд остался на предыдущей версии:
деплой сервиса `web` завершился со статусом FAILED. Сайт при этом работал
как ни в чём не бывало: Railway продолжает держать последний УДАЧНЫЙ
деплой, поэтому снаружи отказ не виден вообще ничем.

Отдельно сбивает с толку, что `worker` из того же образа и того же коммита
поднялся успешно: собранный образ был исправен, не поднялся именно
веб-процесс.

**Поэтому слияние — не последний шаг.** После каждого влитого PR, меняющего
бэкенд, проверяется не статус CI, а живой ответ: запросите поле, которого в
прежней версии не было. Для 9.14 это `webhook_url` в ответе `/api/webhook`;
для любой другой задачи — что-нибудь столь же однозначное.

```bash
railway status                     # web должен быть SUCCESS, а не FAILED
railway logs --deployment          # логи упавшего деплоя
```

Если логов у упавшего деплоя нет вовсе — ни билда, ни рантайма, — значит
процесс не начинал писать. Проверять в таком порядке:

1. `preDeployCommand` из `railway.json` (`alembic upgrade head`) — он
   выполняется ДО старта приложения и в логи самого приложения не попадает;
   упавшая миграция валит деплой молча;
2. переменные окружения сервиса: приложение отказывается стартовать без
   `APP_ENCRYPTION_KEY` (задача 3.4) — и правильно делает;
3. если ни то ни другое — сбой платформы.

**Так и оказалось в тот раз.** Причина видна ТОЛЬКО в панели Railway, в
раскрытой карточке упавшего деплоя — ни CLI, ни MCP её не отдают:

> Deploy › Pre deploy command — The registry could not be reached to pull
> the image `production-dpsjc.railway-registry.com/...`

Образ собрался и был запушен, а контейнер пред-деплоя не смог его оттуда
вытянуть: у Railway в это время шло обслуживание («brief intermittent
errors possible»). Логов при этом нет никаких — процесс не стартовал, и
писать было нечему.

Лечится повторным запуском: карточка деплоя → ⋮ → **Redeploy**. Два
слияния подряд напоролись на одно и то же окно обслуживания, третья
попытка прошла. Смена настроек в панели редеплой НЕ запускает — проверено;
запускает только новый коммит или Redeploy.

`railway.json` перекрывает панель при каждом деплое: правки healthcheck и
команды запуска имеет смысл делать только в нём.
